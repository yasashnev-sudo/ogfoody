# 🚀 Инструкция по деплою viewport fix

## Проблема с терминалом AI
У меня возникла техническая проблема с терминалом - команды не возвращают вывод. Но исправление готово!

---

## ✅ Что уже готово:

1. **Баг найден и исправлен:**
   - Файл: `app/layout.tsx`
   - Добавлен viewport для корректного отображения на iPhone
   
2. **Код закоммичен и запушен:**
   - Коммит: `c29a2d4f`
   - GitHub: обновлен ✅

3. **Готов скрипт для деплоя:**
   - Файл: `deploy-viewport-fix.sh`

---

## 🛠️ Вариант 1: Автоматический деплой (рекомендуется)

Запустите готовый скрипт в терминале:

```bash
cd "/Users/sergejasasnev/Downloads/my-project (1)"
./deploy-viewport-fix.sh
```

Скрипт автоматически:
- Подключится к серверу
- Загрузит изменения с GitHub
- Соберет проект
- Перезапустит PM2
- Проверит статус

---

## 🛠️ Вариант 2: Ручной деплой

### Шаг 1: Подключитесь к серверу
```bash
ssh root@5.129.194.168
```
**Пароль:** `pULRoAvF@P-@4Y`

### Шаг 2: Загрузите изменения
```bash
cd /var/www/ogfoody
git pull origin main
```

Должно показать:
```
Updating dd789299..c29a2d4f
 app/layout.tsx | 8 +++++++-
 ...
```

### Шаг 3: Соберите проект
```bash
npm run build
```

Ждите ~15-20 секунд. Должно показать:
```
✓ Compiled successfully
```

### Шаг 4: Перезапустите PM2
```bash
pm2 restart ogfoody
```

Должно показать:
```
[PM2] [ogfoody](0) ✓
```

### Шаг 5: Проверьте статус
```bash
pm2 list
curl -I http://localhost:3000
```

Первая команда покажет status: `online`  
Вторая покажет: `HTTP/1.1 200 OK`

### Шаг 6: Выйдите
```bash
exit
```

---

## ✅ Проверка на iPhone

После деплоя:

1. Откройте **Safari** на iPhone
2. Перейдите на https://ogfoody.ru
3. **Очистите кеш:** Settings → Safari → Clear History and Website Data
4. Обновите страницу

### Ожидаемый результат:
- ✅ Страница открывается в масштабе 1:1 (не увеличена)
- ✅ При нажатии на input поля масштаб не меняется
- ✅ Жест пинч-зум не работает (как в PWA приложении)

---

## 🐛 Что исправлено

**Было:**
```typescript
// Отсутствовал viewport
export const metadata: Metadata = {
  title: "SWITCH — доставка домашней еды",
  ...
}
```

**Стало:**
```typescript
// Добавлен viewport
export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
}
```

Это генерирует HTML:
```html
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
```

---

## 📝 Документация

- **Детальное описание:** `FIX_MOBILE_VIEWPORT_ZOOM_2026_01_13.md`
- **Все баги сегодня:** `BUG_FIXES_2026_01_13.md`

---

## 🆘 Если что-то пошло не так

### Сайт не загружается:
```bash
ssh root@5.129.194.168
pm2 logs ogfoody --lines 50
```

### Откат на предыдущую версию:
```bash
ssh root@5.129.194.168
cd /var/www/ogfoody
git reset --hard dd789299  # Предыдущий рабочий коммит
npm run build
pm2 restart ogfoody
```

---

**Готово к деплою!** 🚀

Запустите `./deploy-viewport-fix.sh` или следуйте инструкции выше.

