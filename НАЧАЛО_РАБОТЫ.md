# 🚀 Начало работы с проектом ogfoody.ru

## ✅ Всё уже настроено!

### Что сделано:
- ✅ `.env.local` создан с правильными переменными
- ✅ Подключение к NocoDB настроено
- ✅ `.gitignore` обновлён (секреты в безопасности)
- ✅ Скрипты для работы готовы

---

## 🏁 Быстрый старт

### 1. Запусти dev сервер

```bash
npm run dev
```

Откроется на **http://localhost:3000**

### 2. Начни разрабатывать! 🎨

Вноси изменения в код - они применятся автоматически (hot reload).

---

## 📚 Важные документы

### Для разработки:
- **`SETUP_LOCAL_DEV.md`** ⭐ - Полная инструкция для локальной разработки
- **`AI_AGENT_WORKFLOW.md`** ⭐ - Workflow для AI агента (как дебажить, деплоить)

### Для работы с данными:
- **`NOCODB_ИНСТРУКЦИЯ.md`** - Как работать с базой данных
- **`ENV_TEMPLATE.md`** - Шаблон переменных окружения

### Для деплоя:
- **`DEPLOYMENT_GUIDE_OGFOODY.md`** - Полный гайд по деплою

---

## 🔄 Типичный рабочий процесс

### Разработка
```bash
# 1. Запусти dev сервер
npm run dev

# 2. Редактируй код
# Изменения появятся автоматически

# 3. Проверь что всё работает
# Открой http://localhost:3000
```

### Коммит
```bash
# 1. Проверь что меняется
git status

# 2. Добавь изменения
git add .

# 3. Закоммить с понятным сообщением
git commit -m "feat: добавил новую функцию"

# 4. Запуш в GitHub
git push origin main
```

### Деплой на сервер
```bash
# Подключись к серверу
ssh root@5.129.194.168

# Обнови код
cd /var/www/ogfoody
git pull origin main

# Пересобери (ОБЯЗАТЕЛЬНО!)
npm run build

# Перезапусти
pm2 restart ogfoody

# Проверь что работает
pm2 logs ogfoody --lines 50
```

---

## 🔐 Переменные окружения

### ✅ Локально (`.env.local`) - уже настроено
```bash
NOCODB_URL=https://noco.povarnakolesah.ru
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### ✅ На сервере (`.env.production`) - уже настроено
```bash
NOCODB_URL=http://172.20.0.2:8080
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://ogfoody.ru
```

**⚠️ Важно:** Эти файлы НЕ попадают в Git!

---

## 🛠️ Полезные команды

### Разработка
```bash
npm run dev          # Запустить dev сервер
npm run build        # Собрать для продакшена
npm run start        # Запустить production сборку
npm run lint         # Проверить код линтером
```

### Работа с .env
```bash
# Скачать свежий .env с сервера
./download-env.expect

# Или вручную
scp root@5.129.194.168:/var/www/ogfoody/.env.production ./.env.local
```

### Работа с сервером
```bash
# SSH подключение
ssh root@5.129.194.168

# Логи приложения
pm2 logs ogfoody

# Статус
pm2 status

# Перезапуск
pm2 restart ogfoody
```

---

## 🐛 Если что-то не работает

### "Cannot connect to NocoDB"
1. Проверь `.env.local` - там должен быть `NOCODB_URL=https://noco.povarnakolesah.ru`
2. Проверь что NocoDB доступен: `curl https://noco.povarnakolesah.ru`

### "Port 3000 already in use"
```bash
# Используй другой порт
npm run dev -- -p 3001
```

### "Changes don't appear"
1. Проверь что dev сервер запущен (`npm run dev`)
2. Очисти кэш браузера (Ctrl+Shift+R)
3. Проверь консоль браузера на ошибки (F12)

### "Production not working after deploy"
```bash
# Не забыл пересобрать?
ssh root@5.129.194.168
cd /var/www/ogfoody
npm run build  # ⚠️ ОБЯЗАТЕЛЬНО!
pm2 restart ogfoody
```

---

## 📞 Нужна помощь?

1. **Проблемы с кодом** → Смотри `AI_AGENT_WORKFLOW.md`
2. **Проблемы с базой** → Смотри `NOCODB_ИНСТРУКЦИЯ.md`
3. **Проблемы с деплоем** → Смотри `DEPLOYMENT_GUIDE_OGFOODY.md`

---

## 🎯 Ты готов! Начинай разработку:

```bash
npm run dev
```

**Удачи! 🚀**

