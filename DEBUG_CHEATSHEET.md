# 🐞 Debug Black Box - Памятка

## Быстрый доступ

### Открыть консоль отладки
👉 Кликните на кнопку **🐞** (справа внизу на экране)

### Отправить отчет вручную
1. Кликните 🐞
2. Нажмите **"Send Debug Report"**
3. Проверьте `debug_reports/`

### Просмотреть отчеты

```bash
# Последний лог
cat $(ls -t debug_reports/*_logs.txt | head -1)

# Последний скриншот
open $(ls -t debug_reports/*_screenshot.png | head -1)

# Все отчеты
ls -la debug_reports/
```

## Автоматический захват

Ошибки в `handleAutoCheckout` захватываются автоматически:
- ❌ Нет района → отчет
- ❌ Пустой persons → отчет  
- ❌ Ошибка API → отчет
- ❌ Любое исключение → отчет

## Воспроизведение бага "гостя"

1. Инкогнито → создать заказ
2. Авторизоваться
3. Заполнить профиль
4. При ошибке → автоматический отчет в `debug_reports/`

## Что искать в отчете

✅ `pendingCheckout` сохранился?  
✅ `persons` не пустой?  
✅ `paymentMethod` в payload? (должен отсутствовать!)  
✅ Что вернул сервер?  
✅ Скриншот - что видел пользователь?

## Очистка

```bash
# Удалить все отчеты
rm -rf debug_reports/*
```

## Документация

📚 Полная: `DEBUG_BLACK_BOX_SYSTEM.md`  
🚀 Быстрый старт: `QUICK_START_DEBUG_BLACK_BOX.md`  
📊 Отчет о внедрении: `DEBUG_BLACK_BOX_IMPLEMENTATION_REPORT.md`

---
✅ Готово к использованию!


