# Инструкция по тестированию исправления отмены заказа

## ✅ Что было исправлено

**Проблема**: После отмены заказа, баллы правильно списывались/возвращались, но заказ продолжал отображаться в календаре.

**Причина**: Race condition в функции `handleCancelOrder` - синхронное и асинхронное обновление состояния конфликтовали.

**Решение**: Изменена логика обновления состояния, чтобы для авторизованных пользователей использовалась только асинхронная перезагрузка из БД с фильтрацией отмененных заказов.

## 📋 Шаги для тестирования

### 1. Подготовка
1. Откройте приложение в браузере: http://localhost:3000
2. Войдите в систему как авторизованный пользователь

### 2. Создание тестового заказа
1. В календаре выберите дату в будущем (например, завтра или послезавтра)
2. Создайте заказ:
   - Добавьте минимум 1 персону
   - Выберите блюда для хотя бы одного дня
3. Сохраните заказ **БЕЗ ОПЛАТЫ** (оставьте "Наличные")
4. Убедитесь, что заказ отображается в календаре (фиолетовая иконка грузовика)

### 3. Тестирование отмены неоплаченного заказа
1. Откройте созданный заказ (кликните на дату в календаре)
2. Нажмите кнопку "Отменить заказ"
3. **Ожидаемый результат**:
   - ✅ Показывается анимация загрузки (~1 сек)
   - ✅ Заказ **немедленно исчезает** из календаря
   - ✅ Показывается диалог "Заказ отменён"
   - ✅ Баллы обновляются (если были начислены, они списываются)

### 4. Проверка после перезагрузки
1. Перезагрузите страницу (F5 или Cmd+R)
2. **Ожидаемый результат**:
   - ✅ Отмененный заказ НЕ появляется снова в календаре
   - ✅ Баланс баллов остается правильным

### 5. Тестирование отмены оплаченного заказа (опционально)
1. Создайте новый заказ на другую дату
2. **Оплатите** его (картой или СБП)
3. После успешной оплаты откройте заказ
4. Нажмите "Отменить заказ"
5. **Ожидаемый результат**:
   - ✅ Заказ немедленно исчезает из календаря
   - ✅ Баллы **возвращаются** (не списываются)
   - ✅ Показывается сообщение о возврате денег
   - ✅ После перезагрузки заказ не появляется

### 6. Проверка истории заказов
1. Переключитесь на вкладку "История"
2. **Ожидаемый результат**:
   - ✅ Отмененные заказы НЕ отображаются в истории
   - ✅ Показываются только активные заказы

## 🔍 Что проверять в консоли браузера (F12)

Откройте консоль разработчика (F12) и следите за логами при отмене:

### Ожидаемые логи:
```
🔄 Перезагружаем заказы из БД после удаления...
✅ Фильтруем отмененный заказ ID=XXX (status="cancelled")
🔄 Обновлено заказов из БД после отмены: N
```

### Не должно быть:
```
❌ Ошибка перезагрузки заказов после удаления
⚠️ Фильтруем отмененный заказ ... (в повторных запросах)
```

## 🐛 Если проблема сохраняется

Если заказ все еще остается в календаре после отмены:

1. **Проверьте консоль браузера** - есть ли ошибки?
2. **Проверьте терминал сервера** - завершился ли запрос успешно?
3. **Проверьте сеть** (вкладка Network в DevTools):
   - DELETE /api/orders/[id] - должен вернуть 200
   - GET /api/orders?userId=X - должен вернуть заказы БЕЗ отмененного

4. **Очистите кэш**:
   - localStorage: откройте консоль и выполните `localStorage.clear()`
   - Перезагрузите страницу

## 📊 Технические детали

### Затронутые файлы
- `app/page.tsx` - функция `handleCancelOrder` (строки ~1400-1513)

### Ключевые изменения
1. Добавлен `return` после асинхронной перезагрузки для авторизованных пользователей
2. Улучшено логгирование фильтрации отмененных заказов
3. Добавлено закрытие модального окна внутри блока перезагрузки
4. Диалог успеха показывается ПОСЛЕ обновления состояния

### Фильтрация отмененных заказов работает в 4 местах:
1. ✅ API уровень (`lib/nocodb.ts`) - отмененные заказы не возвращаются из БД
2. ✅ Клиент при загрузке (`app/page.tsx`) - дополнительная фильтрация
3. ✅ История заказов (`components/order-history.tsx`) - показываются только активные
4. ✅ После отмены (`app/page.tsx`) - перезагрузка с фильтрацией

## ✅ Успешное тестирование

Тест считается успешным, если:
- ✅ Заказ исчезает из календаря СРАЗУ после отмены
- ✅ После перезагрузки страницы заказ не появляется снова
- ✅ Баллы обновляются правильно (возвращаются для оплаченных, списываются для неоплаченных)
- ✅ В истории заказов не показываются отмененные заказы
- ✅ Не возникает ошибок в консоли

## 📞 Обратная связь

Если проблема не решена или возникают новые ошибки, пожалуйста, сообщите:
1. Скриншот консоли браузера (F12 → Console)
2. Скриншот вкладки Network (запросы DELETE и GET)
3. Логи из терминала сервера



