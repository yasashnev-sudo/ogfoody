# Функция "Повторить заказ" в OrderModal - ЭТАЛОН

**Дата:** 2026-01-16  
**Статус:** ✅ Реализовано и протестировано  
**Версия:** ЭТАЛОН (финальная рабочая версия)

## 📋 Описание

Функция "Повторить заказ" добавлена в модальное окно `OrderModal`, которое открывается при клике на заказ в календаре. Пользователь может повторить существующий заказ на новую дату прямо из модалки просмотра заказа.

## 🎯 Основные возможности

1. **Кнопка "Повторить"** в верхней части модалки (рядом с заголовком)
2. **Меню выбора даты** с доступными датами для доставки
3. **Автоматическая валидация** товаров по актуальному меню
4. **Предупреждение о недоступных товарах** внутри модалки
5. **Плавное переключение** между модалками (старая закрывается, новая открывается)

## 🔧 Технические детали

### Компоненты

- **`OrderModal`** (`components/order-modal.tsx`) - модальное окно заказа
- **`handleRepeatOrder`** (`app/page.tsx`) - функция повтора заказа

### Props для OrderModal

```typescript
interface OrderModalProps {
  // ... другие props
  onRepeatOrder?: (order: Order, targetDate: Date) => Promise<void>
  availableDates?: Date[]
  unavailableItems?: string[] // Для показа предупреждения о недоступных товарах
}
```

### State в app/page.tsx

```typescript
const [draftOrder, setDraftOrder] = useState<Order | null>(null)
const [draftOrderUnavailableItems, setDraftOrderUnavailableItems] = useState<string[]>([])
```

### Алгоритм работы

1. Пользователь открывает заказ в календаре → открывается `OrderModal`
2. Пользователь нажимает кнопку "Повторить" → открывается меню выбора даты
3. Пользователь выбирает дату → вызывается `onRepeatOrder(existingOrder, date)`
4. `handleRepeatOrder`:
   - Загружает актуальное меню
   - Валидирует товары из старого заказа
   - Создает новый заказ с актуальными ценами
   - Очищает поля оплаты, промокода, статуса
   - Сохраняет недоступные товары в `draftOrderUnavailableItems`
   - Устанавливает `draftOrder` и `selectedDate` через `flushSync`
5. Модалка автоматически переключается на новую дату
6. Показывается предупреждение о недоступных товарах (если есть)

## ✅ Ключевые особенности реализации

### 1. Позиционирование кнопки

- Кнопка "Повторить" находится в `DialogHeader`, рядом с заголовком
- Используется `shrink-0` для предотвращения сжатия
- Кнопка не пересекается с крестиком закрытия

### 2. Меню выбора даты

- Позиционирование: `bottom-full left-0 right-0 mb-1` (над кнопкой)
- Даты отображаются в **несколько рядов** (grid layout) для удобства
- Показываются только свободные даты (без существующих заказов)
- Визуальное выделение: сегодня (желтый), завтра (фиолетовый), эта неделя (синий)

### 3. Предупреждение о недоступных товарах

- Показывается **внутри модалки** после открытия (не до открытия!)
- Передается через prop `unavailableItems`
- Очищается при сохранении/закрытии заказа

### 4. Управление состоянием

- `onClose()` **НЕ вызывается** в обработчике повтора заказа
- `handleRepeatOrder` сам управляет открытием/закрытием через `setSelectedDate`
- Меню выбора даты закрывается сразу при клике на дату

### 5. Очистка полей при повторе

```typescript
const newOrder: Order = {
  ...cleanValidatedOrder,
  id: undefined,
  orderNumber: undefined,
  startDate: targetDate,
  paid: false,
  paidAt: undefined,
  paymentMethod: undefined,
  paymentStatus: undefined,
  paymentId: undefined,
  orderStatus: 'pending',
  promoCode: undefined,
  promoDiscount: undefined,
  // ... другие поля
}
```

## 🎨 Верстка

### Кнопка "Повторить"

```tsx
<Button
  variant="outline"
  size="sm"
  className="bg-[#9D00FF]/10 hover:bg-[#9D00FF]/20 text-[#9D00FF] border-2 border-[#9D00FF] hover:border-[#9D00FF] font-medium shrink-0"
>
  <RotateCcw className="w-3 h-3 mr-1.5" />
  Повторить
</Button>
```

### Меню выбора даты

- Grid layout: `grid grid-cols-3 gap-2` (3 колонки)
- Каждая дата: `w-[70px] h-[80px]`
- Адаптивная верстка для мобильных устройств

### Предупреждение о недоступных товарах

```tsx
{unavailableItems.length > 0 && (
  <div className="mb-4 p-3 bg-orange-50 border border-orange-200 rounded-lg">
    <div className="flex items-start gap-2">
      <AlertTriangle className="w-4 h-4 text-orange-600" />
      <div>
        <p className="text-sm font-medium text-orange-700">Внимание!</p>
        <p className="text-xs text-orange-600 mt-1">
          Следующие позиции больше не в меню и будут пропущены: {unavailableItems.join(', ')}
        </p>
      </div>
    </div>
  </div>
)}
```

## 📝 Важные моменты

1. **НЕ вызывать `onClose()`** в обработчике повтора заказа - это закрывает модалку до открытия новой
2. **Использовать `flushSync`** для синхронного обновления `draftOrder` и `selectedDate`
3. **Показывать предупреждение внутри модалки**, а не до открытия
4. **Очищать `unavailableItems`** при сохранении/закрытии заказа
5. **Меню выбора даты закрывается сразу** при клике на дату

## 🐛 Известные проблемы (решенные)

1. ✅ Модалка закрывалась после выбора даты - исправлено (убран `onClose()`)
2. ✅ Предупреждение закрывало модалку - исправлено (показывается внутри модалки)
3. ✅ Кнопка пересекалась с крестиком - исправлено (верстка)
4. ✅ Даты не помещались в модалку - исправлено (grid layout в несколько рядов)

## 📊 Тестирование

### Сценарии

1. ✅ Повтор заказа с доступными товарами
2. ✅ Повтор заказа с недоступными товарами
3. ✅ Повтор заказа на дату с существующим заказом (показывается предупреждение)
4. ✅ Повтор оплаченного заказа
5. ✅ Повтор заказа с промокодом (промокод очищается)

### Результаты

- ✅ Все сценарии работают корректно
- ✅ UX улучшен (плавное переключение модалок)
- ✅ Предупреждения показываются в нужный момент
- ✅ Верстка адаптивная и удобная

## 🔄 История изменений

- **2026-01-16**: Первая реализация кнопки повтора в OrderModal
- **2026-01-16**: Исправлена проблема с закрытием модалки
- **2026-01-16**: Добавлено предупреждение о недоступных товарах
- **2026-01-16**: Исправлена верстка (grid layout, позиционирование кнопки)

---

**Статус:** ✅ ЭТАЛОН - финальная рабочая версия, готова к использованию
