# 🧪 Инструкция: Архитектурный аудит E2E

## ✅ ЧТО СДЕЛАНО:

Создан полный E2E тест проверки архитектуры в файле:
```
tests/architecture_audit.spec.ts
```

Тест проверяет соответствие системы документу `DATA_ARCHITECTURE_RULES.md`:
1. **Гостевой режим** - localStorage, отсутствие API запросов
2. **Маппинг данных** - user_id, Delivery Fee в Title Case
3. **Система баллов** - транзакции, начисление/списание
4. **API NocoDB** - прямые запросы через прокси
5. **Title Case** - корректный маппинг в/из БД

## 🚀 КАК ЗАПУСТИТЬ ПОЛНУЮ ПРОВЕРКУ:

### Шаг 1: Создать тестовые данные

**Вариант А: Через UI (рекомендуется)**
1. Откройте http://localhost:3000
2. Нажмите "Войти"
3. Введите телефон: `79999999999`
4. Заполните профиль:
   - Имя: Тестовый
   - Район: Центральный район
   - Улица: Тестовая
   - Дом: 99
5. Создайте заказ:
   - Выберите дату
   - Добавьте человека
   - Выберите блюда
   - Нажмите "Заказать"
6. Оплатите картой (для мгновенного начисления баллов)

**Вариант Б: Автоматизированно (если есть данные)**
```bash
npx playwright test tests/create-test-data.spec.ts
```

### Шаг 2: Запустить архитектурный аудит

```bash
npx playwright test tests/architecture_audit.spec.ts --workers=1
```

### Шаг 3: Посмотреть отчет

```bash
npx playwright show-report
```

## 📊 ЧТО ПРОВЕРЯЕТ ТЕСТ:

### Сценарий 1: Гостевой режим
- ✅ Кнопка "Войти" видна
- ✅ localStorage.guest_orders используется
- ✅ POST /api/orders НЕ вызывается

### Сценарий 2: Маппинг
- ✅ GET /api/orders возвращает orders
- ✅ Поля в camelCase (deliveryFee, orderNumber)
- ✅ userId связан с заказом

### Сценарий 3: Баллы
- ✅ Транзакции имеют Transaction Type, Status
- ✅ Связь транзакций с заказами
- ✅ Расчет баланса корректен

### Сценарий 4: API
- ✅ /api/orders?userId=N работает
- ✅ Структура ответа корректна
- ✅ Маппинг из БД работает

### Сценарий 5: Title Case
- ✅ GET возвращает данные в правильном формате
- ✅ Поля замаплены из "Title Case" в camelCase

## 🐛 ЕСЛИ НАЙДЕНЫ БАГИ:

Тест укажет на конкретную проблему:
- Отсутствие полей
- Неправильный маппинг
- Неверные типы данных
- Проблемы со связями

Исправьте проблемы в файлах:
- `lib/nocodb.ts` - маппинг
- `app/api/orders/route.ts` - API
- `app/page.tsx` - React state

Повторяйте цикл "тест → исправление → тест" до зеленых результатов.

## 📈 ТЕКУЩИЙ СТАТУС:

```
✅ 5/5 тестов прошли
⚠️ БД пустая - тесты прошли базовую проверку
📝 Добавьте данные для полной проверки
```

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ:

```bash
# Запустить только один сценарий
npx playwright test tests/architecture_audit.spec.ts -g "Гостевой режим"

# С UI браузера (медленно)
npx playwright test tests/architecture_audit.spec.ts --headed

# С debug
npx playwright test tests/architecture_audit.spec.ts --debug

# Очистить БД
curl "http://localhost:3000/?clear=1"
```

## 🎯 СЛЕДУЮЩИЕ ШАГИ:

1. Создайте тестовые данные (см. Шаг 1)
2. Запустите полный аудит
3. Исправьте найденные проблемы
4. Повторяйте до 100% зеленых тестов
5. Запускайте тест после каждого изменения архитектуры

---

**Дата создания:** 10.01.2026  
**Автор:** Автономный QA-инженер  
**Статус:** ✅ Готов к использованию



