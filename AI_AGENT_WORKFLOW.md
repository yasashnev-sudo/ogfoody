# 🤖 Инструкция для AI Агента: Работа с проектом ogfoody.ru

## 📋 Общая информация о проекте

### Структура проекта:
- **Фронтенд/Бэкенд**: Next.js (React + API Routes)
- **База данных**: NocoDB (PostgreSQL в Docker)
- **Хостинг**: VPS сервер (TimewebCloud)
- **Домены**: 
  - `https://ogfoody.ru` - основной сайт
  - `https://noco.povarnakolesah.ru` - панель NocoDB
- **Сервер управления**: Nginx + PM2

### SSH доступ к серверу:
```bash
ssh root@5.129.194.168
Пароль: pULRoAvF@P-@4Y
```

---

## 🔄 Workflow: Как вносить изменения

### Шаг 1: Изменения в локальном коде

1. **Редактируй файлы локально** в проекте
2. **Тестируй локально**:
   ```bash
   npm run dev
   # Проверь на http://localhost:3000
   ```

### Шаг 2: Коммит изменений

```bash
git add .
git commit -m "Описание изменений (например: fix: исправлена отправка заказов)"
git push origin main
```

### Шаг 3: Деплой на сервер

**ВАЖНО**: Нужно зайти на сервер и обновить код там!

```bash
# 1. Подключись к серверу
ssh root@5.129.194.168

# 2. Перейди в директорию проекта
cd /var/www/ogfoody

# 3. Обнови код
git pull origin main

# 4. Установи зависимости (если добавлялись новые)
npm install

# 5. Пересобери проект
npm run build

# 6. Перезапусти приложение
pm2 restart ogfoody

# 7. Проверь логи
pm2 logs ogfoody --lines 50
```

### Альтернатива: Скрипт для быстрого деплоя

Создай на сервере файл `/root/deploy-ogfoody.sh`:
```bash
#!/bin/bash
cd /var/www/ogfoody
echo "🔄 Получаем обновления..."
git pull origin main
echo "📦 Устанавливаем зависимости..."
npm install
echo "🔨 Собираем проект..."
npm run build
echo "🔄 Перезапускаем..."
pm2 restart ogfoody
echo "✅ Готово! Логи:"
pm2 logs ogfoody --lines 30 --nostream
```

Потом просто запускай: `bash /root/deploy-ogfoody.sh`

---

## 🐛 Debugging: Как искать проблемы

### 1. Проверка логов приложения

```bash
# Подключись к серверу
ssh root@5.129.194.168

# Смотри логи приложения (последние 100 строк)
pm2 logs ogfoody --lines 100

# Логи в реальном времени
pm2 logs ogfoody

# Только ошибки
pm2 logs ogfoody --err

# Логи за последние N строк
tail -100 /var/www/ogfoody/logs/err.log
tail -100 /var/www/ogfoody/logs/out.log
```

### 2. Проверка статуса сервиса

```bash
# Статус PM2
pm2 status

# Статус приложения
pm2 info ogfoody

# Использование ресурсов
pm2 monit
```

### 3. Проверка Nginx логов

```bash
# Логи доступа
tail -100 /var/log/nginx/access.log

# Логи ошибок
tail -100 /var/log/nginx/error.log

# Поиск ошибок
grep -i error /var/log/nginx/error.log | tail -50
```

### 4. Проверка API эндпоинтов

```bash
# Проверь health endpoint
curl https://ogfoody.ru/api/health

# Проверь меню
curl https://ogfoody.ru/api/menu | jq '.' | head -100

# Проверь конкретный заказ (если есть)
curl https://ogfoody.ru/api/orders
```

---

## 📱 Пример: Проблема с заказами с мобильного

### Шаг 1: Проверь логи на момент проблемы

```bash
ssh root@5.129.194.168

# Смотри логи когда пользователь пытался сделать заказ
pm2 logs ogfoody --lines 200 | grep -i "order\|заказ\|error"

# Или посмотри все последние логи
pm2 logs ogfoody --lines 200
```

### Шаг 2: Найди API роут для заказов

В проекте это скорее всего:
- `app/api/orders/route.ts` или
- `app/api/order/create/route.ts` или
- `pages/api/orders.ts`

**Найди файл с обработкой заказов:**
```bash
# В локальном проекте
find . -name "*order*.ts" -o -name "*order*.tsx" | grep -E "api|pages"
```

### Шаг 3: Проверь что именно не работает

Типичные проблемы:
1. **CORS ошибки** (мобильный браузер блокирует запросы)
2. **Ошибки валидации** (данные формы не проходят проверку)
3. **Ошибки NocoDB** (нет подключения к БД)
4. **Таймауты** (долгий запрос)

### Шаг 4: Добавь логирование

В API роуте добавь детальное логирование:

```typescript
export async function POST(request: Request) {
  console.log('📱 [ORDER API] Получен запрос на создание заказа')
  
  try {
    const body = await request.json()
    console.log('📱 [ORDER API] Данные заказа:', JSON.stringify(body))
    
    // ... твой код обработки заказа
    
    console.log('✅ [ORDER API] Заказ успешно создан')
    return NextResponse.json({ success: true })
    
  } catch (error) {
    console.error('❌ [ORDER API] Ошибка:', error)
    console.error('❌ [ORDER API] Stack:', error.stack)
    return NextResponse.json(
      { error: 'Failed to create order', details: error.message }, 
      { status: 500 }
    )
  }
}
```

### Шаг 5: Задеплой изменения и проверь

```bash
# Закоммить изменения
git add .
git commit -m "feat: добавлено детальное логирование заказов"
git push

# На сервере
ssh root@5.129.194.168
cd /var/www/ogfoody
git pull
npm run build
pm2 restart ogfoody

# Смотри логи в реальном времени
pm2 logs ogfoody
```

### Шаг 6: Воспроизведи проблему

Попробуй снова сделать заказ с мобильного и **сразу смотри логи** - ты увидишь точную ошибку!

---

## 🔍 Частые проблемы и решения

### Проблема 1: "Не работает на мобильном, на десктопе работает"

**Причина**: Часто CORS или mobile viewport

**Решение**:
1. Проверь Console в Chrome DevTools (на десктопе включи mobile режим)
2. Проверь CORS заголовки в API роутах
3. Проверь что форма корректно валидируется на мобильном

### Проблема 2: "Изменения не применяются"

**Причина**: Забыл пересобрать или не обновил код на сервере

**Решение**:
```bash
ssh root@5.129.194.168
cd /var/www/ogfoody
git pull
npm run build  # ОБЯЗАТЕЛЬНО!
pm2 restart ogfoody
```

### Проблема 3: "Ошибка подключения к NocoDB"

**Причина**: Неправильные переменные окружения или NocoDB недоступен

**Решение**:
```bash
# Проверь переменные
cat /var/www/ogfoody/.env.production | grep NOCODB

# Проверь что NocoDB работает
curl http://172.20.0.2:8080

# Проверь Docker контейнеры
docker ps | grep nocodb
```

### Проблема 4: "Сайт не открывается / 502 Bad Gateway"

**Причина**: PM2 процесс упал или не запущен

**Решение**:
```bash
ssh root@5.129.194.168
pm2 status
pm2 restart ogfoody

# Если не помогает - смотри логи
pm2 logs ogfoody --err
```

---

## 📊 Мониторинг

### Проверь здоровье системы

```bash
# Статус всех сервисов
systemctl status nginx
pm2 status
docker ps

# Использование диска
df -h

# Использование памяти
free -h

# Нагрузка CPU
top -n 1
```

### Проверь что всё работает

```bash
# Основной сайт
curl -I https://ogfoody.ru

# API здоровья
curl https://ogfoody.ru/api/health

# API меню
curl https://ogfoody.ru/api/menu | jq '.meals | keys'

# NocoDB
curl -I https://noco.povarnakolesah.ru
```

---

## 🗄️ Работа с базой данных NocoDB

### Изменения данных (блюда, цены, зоны доставки)

1. Открой https://noco.povarnakolesah.ru
2. Войди в базу **FooD**
3. Отредактируй таблицу (Meals, Extras, и т.д.)
4. Изменения **сразу** доступны на сайте (API обновляется автоматически)

### Если нужно изменить структуру таблиц (добавить колонки)

1. Изменения в NocoDB
2. Обнови типы/интерфейсы в коде (`lib/nocodb.ts`)
3. Закоммить, задеплоить

### Backup базы данных

```bash
ssh root@5.129.194.168

# Backup PostgreSQL из Docker
docker exec nocodb-db-1 pg_dump -U postgres nocodb > /root/nocodb_backup_$(date +%Y%m%d).sql

# Скачать backup на локальную машину
# (запусти локально)
scp root@5.129.194.168:/root/nocodb_backup_*.sql ./backups/
```

---

## ⚡ Quick Commands (шпаргалка)

```bash
# ==== НА СЕРВЕРЕ ====

# Быстрый деплой
cd /var/www/ogfoody && git pull && npm install && npm run build && pm2 restart ogfoody

# Посмотреть логи
pm2 logs ogfoody --lines 100

# Перезапустить
pm2 restart ogfoody

# Статус
pm2 status

# Nginx перезагрузка
systemctl reload nginx

# Проверить сертификаты
certbot certificates

# Продлить SSL (если нужно)
certbot renew

# ==== ЛОКАЛЬНО ====

# Запустить dev сервер
npm run dev

# Собрать production версию
npm run build

# Запустить production локально
npm start

# Проверить линтер
npm run lint
```

---

## 🎯 Workflow для конкретной задачи: "Заказы не отправляются с мобильного"

1. **Воспроизведи проблему локально**:
   - Включи Chrome DevTools
   - Переключись в Mobile режим (Ctrl+Shift+M)
   - Открой Console и Network вкладки
   - Попробуй сделать заказ
   - Смотри что показывает Console (красные ошибки)
   - Смотри Network - есть ли запрос? Какой статус? Что в ответе?

2. **Если локально работает, проверь на проде**:
   ```bash
   # Добавь детальное логирование в API роут заказов
   # Закоммить и задеплоить
   git add app/api/orders/route.ts
   git commit -m "debug: добавлено логирование заказов"
   git push
   
   # На сервере
   ssh root@5.129.194.168
   cd /var/www/ogfoody
   git pull && npm run build && pm2 restart ogfoody
   
   # Смотри логи в реальном времени
   pm2 logs ogfoody
   
   # Попробуй заказ с мобильного СЕЙЧАС (пока смотришь логи)
   ```

3. **Найди ошибку в логах**:
   - Ищи красные ошибки ❌
   - Ищи stack trace
   - Ищи "undefined", "null", "validation error"

4. **Исправь проблему**:
   - Отредактируй код
   - Добавь проверки на null/undefined
   - Добавь try-catch где нужно
   - Улучши валидацию

5. **Задеплой фикс**:
   ```bash
   git add .
   git commit -m "fix: исправлена отправка заказов с мобильного"
   git push
   
   # На сервере
   ssh root@5.129.194.168
   cd /var/www/ogfoody && git pull && npm run build && pm2 restart ogfoody
   ```

6. **Проверь что работает**:
   - Попробуй снова с мобильного
   - Проверь логи - нет ошибок
   - Проверь в NocoDB - заказ появился в таблице Orders

---

## 📞 Если что-то сломалось полностью

### Откат к предыдущей версии

```bash
ssh root@5.129.194.168
cd /var/www/ogfoody

# Посмотри последние коммиты
git log --oneline -10

# Откатись к предыдущему коммиту
git checkout <commit-hash>

# Пересобери
npm run build
pm2 restart ogfoody

# Когда исправишь проблему, вернись обратно
git checkout main
```

### Полный перезапуск

```bash
ssh root@5.129.194.168

# Перезапуск приложения
pm2 restart ogfoody

# Перезапуск Nginx
systemctl restart nginx

# Перезапуск NocoDB (если нужно)
cd /root/nocodb
docker-compose restart
```

---

## ✅ Чеклист перед каждым деплоем

- [ ] Протестировал локально (`npm run dev`)
- [ ] Проверил что нет ошибок линтера (`npm run lint`)
- [ ] Закоммитил изменения с понятным сообщением
- [ ] Запушил в Git
- [ ] Зашёл на сервер
- [ ] Сделал `git pull`
- [ ] Сделал `npm run build`
- [ ] Перезапустил PM2 (`pm2 restart ogfoody`)
- [ ] Проверил логи (`pm2 logs ogfoody`)
- [ ] Протестировал на проде что всё работает

---

## 🎓 Полезные команды для дебага

```bash
# Поиск по логам
pm2 logs ogfoody | grep "error"
pm2 logs ogfoody | grep "order"

# Проверка переменных окружения на сервере
pm2 env ogfoody

# Информация о процессе
pm2 show ogfoody

# Очистить логи
pm2 flush ogfoody

# Посмотреть все процессы
pm2 list

# Метрики в реальном времени
pm2 monit
```

---

**Помни**: Всегда сначала проверяй логи, потом пиши код! 🎯

