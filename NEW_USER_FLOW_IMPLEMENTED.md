# ✅ РЕАЛИЗОВАН: Полный flow для нового клиента

**Дата**: 2026-01-09  
**Статус**: ✅ ГОТОВО К ТЕСТИРОВАНИЮ

---

## 🎯 Новый flow для неавторизованного пользователя

### Пошаговый сценарий:

1. **Первый заход на сайт**
   - ✅ Сразу показывается модалка выбора района
   - ✅ Пользователь выбирает район → сохраняется в `guest_district`
   - ✅ Показывается календарь

2. **Создание заказа**
   - ✅ Пользователь выбирает блюда, добавки
   - ✅ Видит **корректную стоимость доставки** для своего района
   - ✅ Итоговая сумма уже включает доставку

3. **Нажатие "Оформить заказ"**
   - ✅ Данные заказа (order + total) сохраняются в `pendingCheckout`
   - ✅ Устанавливается флаг `shouldAutoCheckout = true`
   - ✅ Открывается **AuthModal**

4. **Авторизация**
   - ✅ Ввод телефона → Код подтверждения
   - ✅ После успешного кода:
     - Район из `guest_district` автоматически переносится в профиль
     - Сохраняется в базу данных NocoDB

5. **Проверка профиля**
   - ✅ Если профиль **НЕ заполнен** (нет имени или адреса):
     - Открывается **ProfileModal**
     - Район уже заполнен автоматически
     - Пользователь вводит: Имя, Улица, Дом, (опционально: квартира, подъезд и т.д.)
   
   - ✅ Если профиль **заполнен**:
     - Сразу автоматическое оформление заказа

6. **Сохранение профиля**
   - ✅ После нажатия "Сохранить":
     - Профиль сохраняется в localStorage
     - Профиль обновляется в NocoDB
     - **Автоматически вызывается** `handleAutoCheckout()`

7. **Автоматическое оформление заказа**
   - ✅ Загружаются зоны доставки
   - ✅ Рассчитывается точная стоимость доставки для района
   - ✅ Обновляется заказ с данными:
     - `deliveryFee` - стоимость доставки
     - `deliveryDistrict` - район
     - `deliveryAddress` - полный адрес
     - `subtotal` - сумма без доставки
     - `total` - итоговая сумма с доставкой
   - ✅ Заказ сохраняется через `handleSaveOrder()`
   - ✅ Открывается **PaymentModal**

8. **Оплата**
   - ✅ Пользователь видит итоговую сумму с доставкой
   - ✅ Может использовать баллы лояльности (если есть)
   - ✅ Выбирает способ оплаты
   - ✅ Заказ отправляется в базу данных

---

## 🔄 Сценарий для существующего пользователя

1. **Авторизация**
   - ✅ Модалка района **НЕ показывается**
   - ✅ Сразу показывается календарь

2. **Создание заказа**
   - ✅ Видит корректную стоимость доставки
   - ✅ Итоговая сумма уже включает доставку

3. **Нажатие "Оформить заказ"**
   - ✅ Если профиль заполнен → сразу показывается **PaymentModal**
   - ✅ Если профиля нет адреса → сначала **ProfileModal**, потом оплата

---

## 📊 Изменения в коде

### 1. `components/order-modal.tsx`

#### Добавлено:
- ✅ Prop `userProfile` в `OrderModalProps`
- ✅ Изменена сигнатура `onRequestAuth: (order: Order, total: number) => void`
- ✅ Состояние `guestDistrict` для хранения района гостя
- ✅ useEffect для загрузки `guest_district` из localStorage
- ✅ Кнопка "Заказать" для неавторизованных теперь создает объект заказа и передает его в `onRequestAuth`

#### Логика кнопки "Заказать" для гостя:
```typescript
onClick={() => {
  const order: Order = {
    startDate: dateKey,
    persons,
    deliveryTime,
    extras,
    paid: false,
    paymentStatus: "pending",
    paymentMethod,
    promoCode: appliedPromo?.code,
    promoDiscount: appliedPromo?.discount,
    loyaltyPointsUsed: pointsDiscount > 0 ? pointsDiscount : undefined,
    deliveryFee,
    deliveryDistrict: guestDistrict || undefined,
    subtotal: totalBeforeDiscount,
    total: finalTotal,
  }
  onRequestAuth?.(order, finalTotal)
}}
```

### 2. `app/page.tsx`

#### Добавлено:
- ✅ Состояние `shouldAutoCheckout` - флаг автоматического оформления
- ✅ Функция `handleAutoCheckout()` - автоматическое оформление заказа
- ✅ Обновлен `handleProfileSave()` - вызывает автооформление после сохранения
- ✅ Обновлен `handleLogin()` - проверяет заполненность профиля
- ✅ Обновлен `handleDistrictSelected()` - проверяет нужно ли показывать ProfileModal
- ✅ Обновлен `onRequestAuth` в OrderModal - принимает order и total

#### Функция `handleAutoCheckout()`:
```typescript
const handleAutoCheckout = async () => {
  // 1. Получает район из профиля
  // 2. Загружает зоны доставки
  // 3. Рассчитывает стоимость доставки
  // 4. Обновляет заказ с данными доставки и адреса
  // 5. Сохраняет заказ через handleSaveOrder
  // 6. Открывает PaymentModal
  // 7. Очищает pendingCheckout и shouldAutoCheckout
}
```

---

## 🧪 Тестирование

### Тест 1: Новый пользователь (полный flow)

```bash
1. Откройте http://localhost:3000 в режиме Инкогнито
2. ✅ Должна показаться модалка выбора района
3. Выберите "Адмиралтейский район" → Нажмите "Продолжить"
4. ✅ Должен показаться календарь

5. Выберите дату → Добавьте блюда
6. ✅ Внизу должна показываться стоимость доставки (например, 250₽)
7. ✅ Итоговая сумма = Блюда + Доставка

8. Нажмите "Заказать · XXXX ₽"
9. ✅ Должна открыться модалка авторизации
10. Введите новый номер: +7 (999) 555-11-22
11. Введите код из консоли
12. ✅ После кода должна открыться модалка профиля
13. ✅ Поле "Район" уже заполнено "Адмиралтейский район"

14. Заполните:
    - Имя: "Тестовый Пользователь"
    - Улица: "Невский проспект"
    - Дом: "1"
    - Квартира: "10" (опционально)

15. Нажмите "Сохранить"
16. ✅ Должно показаться уведомление "Заказ оформлен!"
17. ✅ Должна открыться модалка оплаты
18. ✅ Сумма в модалке = Блюда + Доставка

19. Выберите способ оплаты → Оплатите
20. ✅ Заказ должен сохраниться в базу данных
21. ✅ Проверьте в NocoDB:
    - Таблица Orders: новый заказ с deliveryFee, deliveryDistrict, deliveryAddress
    - Таблица Users: пользователь с заполненным профилем
```

### Тест 2: Существующий пользователь с заполненным профилем

```bash
1. Очистите localStorage
2. Откройте сайт
3. Нажмите "Войти"
4. Введите номер существующего пользователя
5. Подтвердите код
6. ✅ Модалка района НЕ должна показаться
7. ✅ Модалка профиля НЕ должна показаться
8. ✅ Сразу должен показаться календарь

9. Создайте заказ → Нажмите "Оформить"
10. ✅ Сразу должна показаться модалка оплаты
```

### Тест 3: Гость создает несколько заказов

```bash
1. Откройте сайт как гость (инкогнито)
2. Выберите район
3. Создайте заказ на завтра
4. Нажмите "Заказать" → Авторизуйтесь → Заполните профиль
5. ✅ Первый заказ должен оформиться

6. НЕ выходя из аккаунта:
7. Создайте второй заказ на послезавтра
8. Нажмите "Оформить"
9. ✅ Должна сразу показаться модалка оплаты (профиль уже заполнен)
10. ✅ НЕ должна показываться модалка профиля
```

---

## 🔍 Что проверить в базе данных

### Таблица Users:
```
✅ Phone: 79995551122
✅ Name: "Тестовый Пользователь"
✅ District: "Адмиралтейский район"
✅ Street: "Невский проспект"
✅ Building: "1"
✅ Apartment: "10"
```

### Таблица Orders:
```
✅ User ID: [ID пользователя]
✅ Subtotal: [сумма блюд]
✅ Delivery Fee: 250
✅ Total: [subtotal + delivery fee]
✅ Delivery District: "Адмиралтейский район"
✅ Delivery Address: "Невский проспект, 1, кв. 10"
✅ Payment Status: "paid" или "pending"
```

---

## ✅ Преимущества нового flow

1. **Полностью автоматический процесс**
   - Гость видит цену с доставкой ДО авторизации
   - После авторизации заказ оформляется автоматически
   - Не нужно повторно заполнять заказ

2. **Минимум шагов для пользователя**
   - Выбор района → 1 раз
   - Авторизация → 1 раз
   - Заполнение профиля → 1 раз
   - Все остальное автоматически

3. **Корректный расчет доставки**
   - Стоимость доставки рассчитывается из базы данных
   - Учитывается район пользователя
   - Показывается ДО оформления заказа

4. **Не ломает существующий функционал**
   - Существующие пользователи работают как раньше
   - Проверяется флаг `shouldAutoCheckout`
   - Проверяется заполненность профиля

---

## 🚀 Готово к тестированию!

Сервер запущен на **http://localhost:3000**

Протестируйте все сценарии и дайте обратную связь! 🎉

---

## 📝 Дополнительные улучшения (опционально, на будущее)

1. **Валидация адреса**: Проверка, что улица действительно находится в выбранном районе
2. **Подсказки адресов**: Автодополнение улиц через API (DaData, Google Maps)
3. **Сохранение нескольких адресов**: Дом, Работа, Дача и т.д.
4. **Расчет времени доставки**: В зависимости от района
5. **Уведомления**: SMS/Email после оформления заказа



