# 🚀 Деплой исправлений инкремента промокода

**Дата:** 2026-01-15  
**Статус:** Готово к деплою

---

## 📋 Что исправлено

1. ✅ Улучшена проверка условия оплаты (поддержка разных типов данных)
2. ✅ Исправлена функция `fetchPromoCode()` (убрано условие активности, экранирование)
3. ✅ Исправлена функция `incrementPromoCodeUsage()` (правильный формат обновления)
4. ✅ Добавлено детальное логирование во всех критических местах
5. ✅ Переделан тест для локального запуска

---

## 🚀 Деплой на продакшн

### Способ 1: Через SSH (рекомендуется)

```bash
# 1. Закоммитить изменения локально
cd "/Users/sergejasasnev/Downloads/my-project (1)"
git add .
git commit -m "🔧 Fix promo code increment logic and add debug logging"
git push origin main

# 2. Подключиться к серверу
ssh root@5.129.194.168

# 3. Обновить код на сервере
cd /root/my-project
git pull origin main

# 4. Установить зависимости и собрать
npm install
npm run build

# 5. Перезапустить приложение
pm2 restart all

# 6. Проверить логи
pm2 logs --lines 50
```

### Способ 2: Автоматический (через GitHub Actions)

Если настроен автоматический деплой через GitHub Actions:
1. Просто запушить изменения в `main` ветку
2. GitHub Actions автоматически задеплоит

---

## 🧪 Тестирование после деплоя

### Локальное тестирование (перед деплоем)

```bash
# 1. Запустить локальный сервер
npm run dev

# 2. В другом терминале запустить тест
npm run test:fixes:local
```

### Тестирование на продакшн

```bash
# После деплоя запустить тест на продакшн
npm run test:fixes:prod
```

---

## 📊 Проверка логов

После деплоя проверьте логи на сервере:

```bash
ssh root@5.129.194.168
pm2 logs --lines 100 | grep -E "(Проверка инкремента|fetchPromoCode|incrementPromoCodeUsage)"
```

Ищите записи:
- `🔍 [POST] Проверка инкремента промокода:` - проверка условия
- `Calling fetchPromoCode` - поиск промокода
- `fetchPromoCode result` - результат поиска
- `Calling incrementPromoCodeUsage` - вызов инкремента
- `clientFetch update completed` - завершение обновления

---

## ✅ Checklist деплоя

- [ ] Изменения закоммичены
- [ ] Изменения запушены в `main`
- [ ] Подключение к серверу работает
- [ ] `git pull` выполнен успешно
- [ ] `npm install` выполнен успешно
- [ ] `npm run build` выполнен успешно
- [ ] `pm2 restart all` выполнен успешно
- [ ] Логи проверены (нет ошибок)
- [ ] Тест на продакшн запущен
- [ ] Инкремент работает (проверено вручную)

---

## 🔍 Диагностика проблем

### Проблема: Build failed

```bash
# Очистить и пересобрать
cd /root/my-project
rm -rf .next node_modules
npm install
npm run build
```

### Проблема: PM2 не перезапускается

```bash
# Проверить статус
pm2 status

# Перезапустить вручную
pm2 restart all

# Или остановить и запустить заново
pm2 stop all
pm2 start ecosystem.config.js
```

### Проблема: Логи не видны

```bash
# Проверить логи PM2
pm2 logs --lines 100

# Проверить логи Next.js
pm2 logs ogfoody --lines 100
```

---

**Готово к деплою!** 🚀
