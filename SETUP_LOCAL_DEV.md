# 🚀 Быстрая настройка для локальной разработки

## ✅ Что уже сделано:

1. ✅ `.env.local` создан и настроен для локальной разработки
2. ✅ Переменные окружения скачаны с сервера
3. ✅ `.gitignore` настроен (секреты не попадут в Git)

---

## 🔄 Как работать дальше:

### 1. Запусти локальный dev сервер

```bash
npm run dev
```

Откроется на http://localhost:3000

### 2. Вноси изменения в код

- Редактируй компоненты в `app/`
- Редактируй API в `app/api/`
- Изменения появятся сразу (hot reload)

### 3. Коммить изменения

```bash
git add .
git commit -m "feat: описание изменений"
git push origin main
```

**⚠️ Важно:** `.env.local` **НЕ** попадёт в Git (он в `.gitignore`)

### 4. Деплой на сервер

```bash
# Вариант А: Подключись к серверу через SSH
ssh root@5.129.194.168

cd /var/www/ogfoody
git pull origin main
npm install  # только если добавлял новые зависимости
npm run build
pm2 restart ogfoody

# Проверь логи
pm2 logs ogfoody --lines 50
```

```bash
# Вариант Б: Используй скрипт быстрого деплоя (если создал на сервере)
ssh root@5.129.194.168 "bash /root/deploy-ogfoody.sh"
```

---

## 🔐 Переменные окружения

### Локально (`.env.local`) - уже настроено ✅

```bash
NOCODB_URL=https://noco.povarnakolesah.ru  # Публичный URL
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### На сервере (`.env.production`) - уже настроено ✅

```bash
NOCODB_URL=http://172.20.0.2:8080  # Внутренний Docker IP
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://ogfoody.ru
```

---

## 📝 Если нужно обновить переменные

### Скачать свежий .env с сервера:

```bash
./download-env.expect
```

Или вручную:
```bash
scp root@5.129.194.168:/var/www/ogfoody/.env.production ./.env.local
# Потом отредактируй NOCODB_URL и NODE_ENV
```

---

## 🐛 Типичные проблемы

### "Cannot find module or its type declarations"

```bash
npm install
```

### "Port 3000 already in use"

```bash
# Останови предыдущий процесс или используй другой порт
npm run dev -- -p 3001
```

### "NocoDB connection error"

Проверь что:
1. `NOCODB_URL=https://noco.povarnakolesah.ru` (публичный URL)
2. `NOCODB_TOKEN` правильный
3. NocoDB доступен: `curl https://noco.povarnakolesah.ru`

### Изменения не применяются на проде

```bash
# Не забудь пересобрать на сервере!
ssh root@5.129.194.168
cd /var/www/ogfoody
npm run build  # ОБЯЗАТЕЛЬНО!
pm2 restart ogfoody
```

---

## 📚 Дополнительная документация

- `AI_AGENT_WORKFLOW.md` - полный workflow для AI агента
- `NOCODB_ИНСТРУКЦИЯ.md` - работа с базой данных
- `ENV_TEMPLATE.md` - шаблон переменных окружения
- `DEPLOYMENT_GUIDE_OGFOODY.md` - полный гайд по деплою

---

## ✅ Чеклист перед каждым коммитом

- [ ] Протестировал локально (`npm run dev`)
- [ ] Нет ошибок в консоли браузера
- [ ] API работает корректно
- [ ] Проверил что `.env.local` не в списке изменений (`git status`)
- [ ] Написал понятное commit сообщение
- [ ] После деплоя - проверил на проде

---

**Готово! Теперь можешь спокойно разрабатывать! 🎉**

```bash
npm run dev
```

