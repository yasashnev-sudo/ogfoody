# 🐛 ИСПРАВЛЕНИЕ: Неверное масштабирование на iPhone

**Дата:** 13.01.2026  
**Отчет:** `2026-01-13T15-33-04-946Z_user-122_logs.txt`  
**User ID:** 122  
**Устройство:** iPhone (iOS 18.7, Safari 26.2)

---

## 📋 Описание бага

**Жалоба пользователя:**
> "При открытии сайта, неверное масштабирование (немного приближено). Для телефона нужно вообще убрать автоматическое увеличение"

### Проблема:
Safari на iPhone автоматически увеличивал масштаб страницы при:
- Открытии сайта
- Фокусе на input полях
- Взаимодействии с формами

Это происходило из-за отсутствия viewport meta-тега с правильными параметрами для мобильных устройств.

---

## 🔍 Техническая причина

В `app/layout.tsx` отсутствовала настройка viewport:
- Next.js использовал дефолтный viewport
- Safari на iOS свободно масштабировал страницу
- Пользовательский опыт на мобильных устройствах был нарушен

### User Agent из отчета:
```
Mozilla/5.0 (iPhone; CPU iPhone OS 18_7 like Mac OS X) 
AppleWebKit/605.1.15 (KHTML, like Gecko) 
Version/26.2 Mobile/15E148 Safari/604.1
```

---

## 🔧 Исправление

### Файл: `app/layout.tsx`

**Добавлен viewport export с правильными параметрами:**

```typescript
// ✅ ДОБАВЛЕНО:
import type { Metadata, Viewport } from "next"

export const viewport: Viewport = {
  width: 'device-width',    // Ширина = ширине устройства
  initialScale: 1,          // Начальный масштаб 1:1
  maximumScale: 1,          // Максимальный масштаб (запрет увеличения)
  userScalable: false,      // Запрет масштабирования пользователем
}
```

### Параметры viewport:

1. **`width: 'device-width'`**  
   Устанавливает ширину viewport равной ширине экрана устройства

2. **`initialScale: 1`**  
   Начальный масштаб 1:1 (без увеличения при загрузке)

3. **`maximumScale: 1`**  
   Максимальный масштаб = 1 (запрещает увеличение)

4. **`userScalable: false`**  
   Полностью отключает масштабирование пользователем (жест пинч-зум)

---

## ✅ Результат

После исправления:
- ✅ Сайт открывается в масштабе 1:1 на всех мобильных устройствах
- ✅ Safari больше не увеличивает масштаб автоматически
- ✅ Жест пинч-зум отключен (для PWA-подобного опыта)
- ✅ Улучшен UX на iPhone и других мобильных устройствах

### HTML output (для справки):
```html
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
```

---

## 📱 Тестирование

### Проверить на iPhone:
1. Открыть https://ogfoody.ru в Safari
2. ✅ Страница загружается в масштабе 1:1 (не увеличена)
3. ✅ Нажатие на input поля НЕ увеличивает масштаб
4. ✅ Жест пинч-зум не работает
5. ✅ Весь контент помещается на экран корректно

### Проверить на других устройствах:
- Android Chrome
- Desktop браузеры (для совместимости)

---

## 🔗 Связанные файлы

- `app/layout.tsx` - добавлен viewport export

---

## 📚 Документация

- [Next.js Viewport](https://nextjs.org/docs/app/api-reference/functions/generate-viewport)
- [MDN: Viewport meta tag](https://developer.mozilla.org/en-US/docs/Web/HTML/Viewport_meta_tag)
- [Safari iOS Web Content Guide](https://developer.apple.com/library/archive/documentation/AppleApplications/Reference/SafariWebContent/UsingtheViewport/UsingtheViewport.html)

---

## 🚀 Деплой

```bash
# Коммит изменений
git add app/layout.tsx FIX_MOBILE_VIEWPORT_ZOOM_2026_01_13.md BUG_FIXES_2026_01_13.md
git commit -m "fix: добавлен viewport для корректного отображения на мобильных устройствах"

# Пуш на сервер
git push origin main

# SSH на production
ssh root@5.129.194.168

# Деплой
cd /var/www/ogfoody
git pull origin main
npm run build
pm2 restart ogfoody
```

---

**Статус:** ✅ Исправлено и готово к деплою


