# 🐛 ПРОБЛЕМЫ С БАЛЛАМИ ЛОЯЛЬНОСТИ

## ✅ Что ИСПРАВЛЕНО:

### 1. ❌ Баллы не начислялись при создании заказа
**Причина**: `calculatedTotal = 0` потому что `meal.price` не читался в `saveMeal()`

**Исправление**: 
```typescript
// БЫЛО:
const price = meal.prices 
  ? getMealPriceForPortion(meal)
  : await getMealPriceFromDB(cleanMealId, meal.portion || "single")

// СТАЛО:
const price = meal.prices 
  ? getMealPriceForPortion(meal)
  : (meal.price || await getMealPriceFromDB(cleanMealId, meal.portion || "single"))
```

**Файл**: `app/api/orders/route.ts:535-543`

**Результат**: Теперь API возвращает `loyaltyPointsEarned: 70` (было `0`) ✅

---

### 2. ❌ Тип `Meal` не поддерживал одиночную цену
**Причина**: `Meal.price` отсутствовал в TypeScript интерфейсе

**Исправление**:
```typescript
export interface Meal {
  id: number
  name: string
  price?: number // Добавлено!
  prices?: {    // Сделано optional
    single: number
    medium?: number
    large?: number
  }
  // ...
}
```

**Файл**: `lib/types.ts:31-51`

**Результат**: TypeScript не ругается на `meal.price` ✅

---

### 3. ❌ Интеграционные тесты создавали заказы с неправильной структурой
**Причина**: `persons` отправлялись с `{name: "..."}` вместо `{id: 1}`

**Исправление**:
```typescript
// БЫЛО:
persons: [{ name: 'Тест', day1: {...} }]

// СТАЛО:
persons: [{ id: 1, day1: {...} }]
```

**Файл**: `__tests__/integration/loyalty-points.test.ts` (5 мест)

**Результат**: Заказы создаются с правильной структурой ✅

---

## ⚠️ Что ОСТАЛОСЬ ИСПРАВИТЬ:

### 1. ❌ КЭШ в `fetchUserById` возвращает устаревшие данные

**Проблема**:
- После очистки баллов в NocoDB баланс = 0
- Но GET `/api/orders?userId=5` возвращает баллы = 347 (старые!)
- Кэш `revalidate: 60` секунд слишком долгий

**Где**:
- `lib/nocodb.ts` - функция `serverFetch` (строка 106)
- Используется в `fetchUserById` → `fetchOrdersWithDetails` → GET `/api/orders`

**Решение**:
```typescript
// ВАРИАНТ 1: Уменьшить revalidate
next: { revalidate: 5 }, // Было 60

// ВАРИАНТ 2: Использовать noCache для userProfile
const user = await fetchUserById(userId, true) // noCache=true
```

**Файлы для изменения**:
- `lib/nocodb.ts:892` - `fetchUserById` должен принимать `noCache`
- `app/api/orders/route.ts:38` - передать `noCache=true`

---

### 2. ❌ При удалении заказа баллы НЕ возвращаются

**Проблема**:
- Заказ удаляется (статус = `cancelled`)
- Но `loyalty_points_earned = 0` в заказе (не были начислены!)
- DELETE проверяет `pointsEarned > 0` → не возвращает баллы

**Логи**:
```
loyalty_points_earned: 0  ❌
'Loyalty Points Earned': 0  ❌
```

**Причина**: Баллы не начислились при создании заказа (см. проблему #1)

**Решение**: После исправления проблемы #1, баллы будут начисляться → при удалении будут возвращаться

---

### 3. ❌ Для наличных баллы должны начисляться ПОСЛЕ доставки

**Текущая логика** (ПРАВИЛЬНАЯ!):
```typescript
if (order.paymentMethod === 'card' || order.paymentMethod === 'sbp') {
  // Онлайн - начисляем сразу
  await awardLoyaltyPoints(...)
} else if (order.paymentMethod === 'cash') {
  // Наличные - pending транзакция
  await createPendingLoyaltyPoints(...)
}
```

**Проблема**: Pending транзакции не обрабатываются автоматически на следующий день

**Решение**: Нужен cron job или webhook который:
1. Находит заказы со статусом "delivered" + дата доставки <= вчера
2. Обрабатывает pending транзакции для этих заказов
3. Меняет статус транзакции на "completed"

**Файл для создания**: `app/api/cron/process-pending-points/route.ts`

---

## 🧪 ИНТЕГРАЦИОННЫЕ ТЕСТЫ

**Файл**: `__tests__/integration/loyalty-points.test.ts`

**Статус**:
- ✅ Создан
- ✅ 5 тестов написано
- ❌ 4 теста падают из-за проблемы с кэшем
- ✅ 1 тест проходит (для неоплаченных заказов)

**Тесты покрывают**:
1. Начальный баланс
2. Начисление при оплате картой
3. Возврат баллов при удалении заказа
4. Pending для наличных
5. Полный цикл (создание → использование → возврат)

---

## 🔧 БЫСТРОЕ ИСПРАВЛЕНИЕ (TODO):

### Шаг 1: Отключить кэш для userProfile

**Файл**: `lib/nocodb.ts`

<function_calls>
<invoke name="todo_write">
<parameter name="merge">false


