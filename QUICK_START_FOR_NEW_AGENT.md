# ⚡ Быстрый старт для нового AI агента

## 🎯 Твоя миссия: Найти и исправить баги

---

## 📍 ШАГ 1: ПОДКЛЮЧИСЬ К PRODUCTION И ПРОВЕРЬ БАГИ (2 минуты)

```bash
# Создай скрипт для проверки
cat > /tmp/check.sh << 'EOF'
#!/bin/bash
expect << 'EXPECTEOF'
spawn ssh root@5.129.194.168
expect "password:"
send "pULRoAvF@P-@4Y\r"
expect "# "
send "cat /var/www/ogfoody/debug_reports/index.json\r"
expect "# "
send "exit\r"
expect eof
EXPECTEOF
EOF

bash /tmp/check.sh
```

**Что увидишь:**
```json
{
  "reports": [
    {
      "id": "2026-01-13T14-25-34-072Z",
      "userComment": "Очень долго грузится логотип",
      "files": { "log": "..." }
    }
  ]
}
```

---

## 📂 ШАГ 2: ПРОЧИТАЙ ДОКУМЕНТАЦИЮ (5 минут)

### Обязательно прочитай:
```bash
# Последние исправления
cat BUG_FIXES_2026_01_13.md

# Как подключаться
cat SERVER_ACCESS_INFO.md

# Полный workflow
cat AI_AGENT_BUG_FIX_WORKFLOW.md
```

### Важная информация:
- **Сервер:** root@5.129.194.168 (пароль: pULRoAvF@P-@4Y)
- **Проект:** /var/www/ogfoody
- **Сайт:** https://ogfoody.ru
- **Баг-репорты:** /var/www/ogfoody/debug_reports/

---

## 🔧 ШАГ 3: ИСПРАВЬ БАГ (15-60 минут)

### A. Найди проблему:
```bash
# Семантический поиск
codebase_search: "как работает [функция из бага]"

# Точный поиск
grep -r "название компонента" .
```

### B. Исправь локально:
```bash
# Редактируй файлы
# Тестируй если нужно: npm run dev
```

### C. Задокументируй:
Создай `FIX_[НАЗВАНИЕ]_2026_01_13.md`:
```markdown
# Исправление: [Название]

## 🐛 Проблема:
> "Цитата из userComment"

## ✅ Решение:
[Что изменил]

## 📁 Файлы:
- file.tsx
```

---

## 🚀 ШАГ 4: ДЕПЛОЙ (5 минут)

```bash
# 1. Коммит
git add -A
git commit -m "fix: [краткое описание]"
git push origin main

# 2. Создай скрипт деплоя
cat > deploy.sh << 'EOF'
#!/bin/bash
expect << 'EXPECTEOF'
spawn ssh root@5.129.194.168
expect "password:"
send "pULRoAvF@P-@4Y\r"
expect "# "
send "cd /var/www/ogfoody && git fetch origin && git reset --hard origin/main && npm run build && pm2 restart ogfoody\r"
expect "# "
send "exit\r"
expect eof
EXPECTEOF
EOF

# 3. Деплой
bash deploy.sh

# 4. Удали временные файлы
rm deploy.sh /tmp/check.sh
```

---

## ✅ ШАГ 5: ПРОВЕРЬ (2 минуты)

```bash
# Проверь сайт работает
curl -I https://ogfoody.ru

# Проверь логи
ssh root@5.129.194.168 "pm2 logs ogfoody --lines 20 --nostream"
```

Если видишь ошибки - читай `AI_AGENT_BUG_FIX_WORKFLOW.md` раздел "Если что-то сломалось"

---

## 📋 ЧЕКЛИСТ

- [ ] Подключился к production
- [ ] Прочитал index.json с багами
- [ ] Прочитал последние FIX_*.md
- [ ] Нашёл и исправил баг
- [ ] Создал FIX_*.md документацию
- [ ] Сделал коммит с описанием
- [ ] Задеплоил на production
- [ ] Проверил что всё работает
- [ ] Удалил временные скрипты

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Сайт не открывается:
```bash
ssh root@5.129.194.168
pm2 logs ogfoody --lines 100
# Читай ошибки
```

### Ошибка при деплое:
```bash
ssh root@5.129.194.168
cd /var/www/ogfoody
git status  # Посмотри что не так
git reset --hard origin/main  # Сброс на рабочую версию
npm run build
pm2 restart ogfoody
```

### Нужен откат:
```bash
ssh root@5.129.194.168
cd /var/www/ogfoody
git log --oneline -10  # Найди последний рабочий коммит
git reset --hard [хеш-коммита]
npm run build
pm2 restart ogfoody
```

---

## 🎓 ПРИМЕРЫ

### Пример 1: Быстрый фикс
```
Баг: "Кнопка не работает"
Время: 10 минут
1. grep "название кнопки" → нашёл компонент
2. Увидел onClick={undefined} → исправил
3. Коммит, деплой, готово
```

### Пример 2: Сложный баг
```
Баг: "Заказ не создаётся с дополнениями"
Время: 45 минут
1. codebase_search: "как создаётся заказ с extras"
2. Нашёл: extra.id undefined
3. Исправил в extras-selector.tsx
4. Добавил проверки в API
5. Документация, деплой, готово
```

---

## 📚 ДОПОЛНИТЕЛЬНОЕ ЧТЕНИЕ

Подробная информация:
- `AI_AGENT_BUG_FIX_WORKFLOW.md` - полный workflow
- `SERVER_ACCESS_INFO.md` - доступы
- `НАЧАЛО_РАБОТЫ.md` - архитектура проекта

---

**Время на весь процесс: 30-90 минут**  
**Старт → Финиш = Счастливые пользователи! 🎉**


