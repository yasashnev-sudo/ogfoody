# 🔧 Диагностика проблемы с виджетом YooKassa

**Дата:** 2026-01-18  
**Проблема:** `invalid_combination_of_payment_methods` даже после удаления `customization`

---

## 🔍 Проблема

Виджет YooKassa падает с ошибкой `invalid_combination_of_payment_methods`, даже после того как мы:
1. Убрали `payment_methods` из `customization`
2. Полностью убрали объект `customization`

**Логи:**
```
✅ YooKassa widget initialized with token: ct-30ff63cb-000f-500...
❌ YooKassa widget error: {
  "error": "invalid_combination_of_payment_methods"
}
```

---

## 🔍 Возможные причины

### 1. Код не обновился на сервере
- Старая версия с `customization` все еще работает
- Нужно проверить, что деплой прошел успешно

### 2. Проблема в настройках аккаунта YooKassa
- В аккаунте YooKassa могут быть ограничения на методы оплаты
- Некоторые методы могут быть несовместимы друг с другом
- Нужно проверить настройки в личном кабинете YooKassa

### 3. Проблема в самом платеже
- При создании платежа с типом `embedded` YooKassa автоматически определяет доступные методы
- Эти методы могут конфликтовать с настройками виджета
- Возможно, нужно указать методы оплаты при создании платежа

### 4. Проблема с ключами
- Неправильные ключи (тестовые вместо продакшн)
- Ключи не настроены в переменных окружения
- Нужно проверить `.env` файл на сервере

---

## ✅ Временное решение

**Временно отключен виджет, используется только redirect:**
- `shouldUseRedirect = true` для всех платформ
- Это позволит проверить, работает ли оплата вообще
- После проверки можно вернуться к виджету

**Файл:** `components/payment-modal.tsx`
```typescript
// ✅ ВРЕМЕННО: Отключаем виджет из-за ошибки invalid_combination_of_payment_methods
const shouldUseRedirect = true // Временно: redirect везде
```

---

## 🔍 Что проверить

### 1. Проверка кода на сервере
```bash
ssh root@ogfoody.ru "cd /var/www/ogfoody && git log --oneline -3"
ssh root@ogfoody.ru "cd /var/www/ogfoody && grep -r 'customization' components/payment-modal.tsx"
```

### 2. Проверка переменных окружения
```bash
ssh root@ogfoody.ru "cd /var/www/ogfoody && grep YOOKASSA .env"
```

### 3. Проверка логов создания платежа
```bash
ssh root@ogfoody.ru "cd /var/www/ogfoody && pm2 logs ogfoody --lines 100 | grep 'Creating YooKassa payment'"
```

### 4. Проверка настроек аккаунта YooKassa
- Откройте https://yookassa.ru/my
- Проверьте настройки методов оплаты
- Убедитесь, что все нужные методы включены
- Проверьте, нет ли ограничений на комбинации методов

---

## 📝 Следующие шаги

1. **Проверить, работает ли оплата через redirect**
   - Если работает → проблема в виджете
   - Если не работает → проблема в настройках YooKassa или ключах

2. **Проверить настройки аккаунта YooKassa**
   - Включены ли все нужные методы оплаты
   - Нет ли ограничений на комбинации методов

3. **Проверить документацию YooKassa**
   - Какие методы оплаты совместимы с виджетом
   - Есть ли ограничения на использование виджета

4. **Связаться с поддержкой YooKassa**
   - Если проблема не решается, возможно, это баг или ограничение API

---

**Статус:** 🔄 Временное решение применено (redirect везде)
