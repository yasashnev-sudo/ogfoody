# 📋 Подробная документация интеграции YooKassa

## 📦 Обзор

Интеграция YooKassa реализована для обработки онлайн-платежей в приложении. Система поддерживает:
- Создание платежей через API YooKassa
- Обработку webhook-уведомлений о статусах платежей
- Проверку статуса платежей
- Обработку возвратов (refunds)
- Интеграцию с системой баллов лояльности

---

## 🏗️ Архитектура

### Компоненты интеграции

1. **Клиент YooKassa** (`lib/yookassa/client.ts`)
2. **IP-валидатор** (`lib/yookassa/ip-validator.ts`)
3. **API endpoints**:
   - `/api/payments/yookassa/create` - создание платежа
   - `/api/payments/yookassa/webhook` - обработка webhook-уведомлений
   - `/api/payments/yookassa/webhook/check` - проверка конфигурации
   - `/api/payments/yookassa/webhook/test` - тестовый endpoint
   - `/api/payments/yookassa/status/[id]` - проверка статуса платежа
   - `/api/payments/yookassa/refund` - создание возврата средств
4. **UI компоненты**:
   - `components/payment-modal.tsx` - модальное окно оплаты
   - `app/payment/success/page.tsx` - страница успешной оплаты

---

## 🔧 Конфигурация

### Переменные окружения

```env
# Shop ID из личного кабинета YooKassa
YOOKASSA_SHOP_ID=1251656

# Секретный ключ (test_ для тестового режима)
YOOKASSA_SECRET_KEY=test_sDZCHKIUGwEiXpsq0REALRWgsdPY9wCGBdYCRvCH4QE

# Режим тестирования (true/false)
YOOKASSA_TEST_MODE=true

# URL приложения (для webhook и redirect)
NEXT_PUBLIC_APP_URL=https://ogfoody.ru
```

### Определение тестового режима

Тестовый режим определяется автоматически:
- Если `YOOKASSA_TEST_MODE === 'true'`
- ИЛИ если `YOOKASSA_SECRET_KEY` начинается с `test_`

---

## 📚 Детальное описание компонентов

### 1. Клиент YooKassa (`lib/yookassa/client.ts`)

**Назначение**: Инициализация SDK клиента YooKassa

**Используемая библиотека**: `@yookassa/sdk` версии `^0.0.3`

**Код**:
```typescript
import { YookassaSdk } from '@yookassa/sdk'

const isTestMode = 
  process.env.YOOKASSA_TEST_MODE === 'true' || 
  process.env.YOOKASSA_SECRET_KEY?.startsWith('test_')

export const yookassaClient = new YookassaSdk({
  shopId: process.env.YOOKASSA_SHOP_ID!,
  secretKey: process.env.YOOKASSA_SECRET_KEY!,
})

export { isTestMode }
```

**Особенности**:
- Проверяет наличие обязательных переменных окружения
- Логирует режим работы (тестовый/продакшн)
- Экспортирует клиент и флаг тестового режима

---

### 2. IP-валидатор (`lib/yookassa/ip-validator.ts`)

**Назначение**: Проверка IP-адресов webhook-запросов для безопасности

**Разрешенные IP-диапазоны YooKassa**:

IPv4:
- `185.71.76.0/27` (185.71.76.0 - 185.71.76.31)
- `185.71.77.0/27` (185.71.77.0 - 185.71.77.31)
- `77.75.153.0/25` (77.75.153.0 - 77.75.153.127)
- `77.75.156.11` (одиночный IP)
- `77.75.156.35` (одиночный IP)
- `77.75.154.128/25` (77.75.154.128 - 77.75.154.255)

IPv6:
- `2a02:5180::/32` (начинается с `2a02:5180:`)

**Функции**:
- `isValidYookassaIp(ip)` - проверяет, является ли IP разрешенным
- `getClientIp(request)` - извлекает реальный IP из заголовков запроса

**Особенности**:
- В режиме разработки разрешает localhost
- Проверяет заголовки: `x-forwarded-for`, `x-real-ip`, `cf-connecting-ip`
- В тестовом режиме проверка менее строгая (логируется предупреждение, но не блокируется)

---

### 3. Создание платежа (`/api/payments/yookassa/create`)

**Метод**: `POST`

**Назначение**: Создание платежа в YooKassa и получение URL для редиректа

**Входные данные**:
```json
{
  "orderId": 123,
  "amount": 1500.00,
  "description": "Заказ #123",
  "returnUrl": "https://ogfoody.ru/payment/success?orderId=123" // опционально
}
```

**Процесс**:
1. Валидация входных данных (orderId, amount обязательны)
2. Генерация ключа идемпотентности: `order_${orderId}_${Date.now()}`
3. Создание платежа через SDK:
   - Сумма в рублях (RUB)
   - Тип подтверждения: `redirect` (редирект на страницу оплаты)
   - URL возврата: `/payment/success?orderId=${orderId}`
   - Metadata: `{ orderId: String(orderId) }` - **критично для связи платежа с заказом**
   - `capture: true` - автоматическое подтверждение платежа
4. Сохранение `payment_id` в заказ со статусом `pending`
5. Возврат `confirmation_url` для редиректа

**Ответ**:
```json
{
  "paymentId": "2c7c2c8a-0001-5000-8000-1e8b8b8b8b8b",
  "confirmationUrl": "https://yoomoney.ru/checkout/payments/v2/contract?orderId=...",
  "status": "pending"
}
```

**Логирование**:
- Успешное создание платежа с paymentId и orderId
- В тестовом режиме: напоминание о настройке webhook

**Обработка ошибок**:
- Возвращает 400 при отсутствии обязательных полей
- Возвращает 500 при ошибке создания платежа
- Логирует ошибки, но не прерывает процесс при ошибке обновления заказа

---

### 4. Webhook обработчик (`/api/payments/yookassa/webhook`)

**Методы**: 
- `GET` - диагностический endpoint (проверка конфигурации)
- `POST` - обработка webhook-уведомлений

**Назначение**: Обработка уведомлений от YooKassa о статусах платежей

**⚠️ КРИТИЧНО**: YooKassa требует HTTP 200 в ответе, даже при ошибках. Система всегда возвращает 200, но логирует ошибки.

#### GET /api/payments/yookassa/webhook

Возвращает информацию о конфигурации:
```json
{
  "status": "webhook_endpoint_active",
  "message": "This is the YooKassa webhook endpoint",
  "configuration": {
    "shopId": "1251656",
    "testMode": true,
    "webhookUrl": "https://ogfoody.ru/api/payments/yookassa/webhook"
  },
  "instructions": {
    "setup": "Configure this URL in YooKassa dashboard",
    "events": ["payment.succeeded", "payment.canceled", "payment.waiting_for_capture"]
  }
}
```

#### POST /api/payments/yookassa/webhook

**Процесс обработки**:

1. **Валидация IP**:
   - В продакшене: строгая проверка IP (блокировка при несоответствии)
   - В тестовом режиме: предупреждение, но не блокировка

2. **Извлечение данных**:
   - Тип события: `event.event` или `event.type`
   - Объект платежа: `event.object`
   - Payment ID: `object.id`

3. **Получение orderId**:
   - Для обычных событий: `object.metadata.orderId`
   - Для `refund.succeeded`: дополнительный запрос к API YooKassa для получения payment_id, затем извлечение orderId из metadata платежа

4. **Обработка событий**:

##### `payment.succeeded` (успешный платеж)

**Действия**:
1. Обновление заказа:
   - `paid: true`
   - `payment_status: 'paid'`
   - `paid_at: текущая дата`
   - `payment_id: object.id`
   - `payment_method: object.payment_method?.type || 'online'`

2. Начисление баллов лояльности:
   - Проверка, что баллы еще не начислены (`loyalty_points_earned === 0`)
   - Вызов `awardLoyaltyPoints()` с расчетом баллов на основе суммы заказа
   - Учитываются использованные баллы (`loyalty_points_used`)

**Логирование**:
```
✅ Payment succeeded for order 123
💎 Awarding loyalty points for order 123
```

##### `payment.canceled` (отмененный платеж)

**Действия**:
- Обновление заказа:
  - `payment_status: 'canceled'`
  - `payment_id: object.id`

**Логирование**:
```
❌ Payment canceled for order 123
```

##### `payment.waiting_for_capture` (ожидает подтверждения)

**Действия**:
- Обновление заказа:
  - `payment_status: 'waiting_for_capture'`
  - `payment_id: object.id`

**Логирование**:
```
⏳ Payment waiting for capture for order 123
```

##### `refund.succeeded` (возврат платежа)

**Действия**:
1. Возврат баллов лояльности:
   - Возврат использованных баллов (транзакция типа `refunded`)
   - Списывание начисленных баллов (транзакция типа `cancelled`)
   - Откат `total_spent` пользователя
   - Вызов `refundLoyaltyPoints()`

2. Обновление заказа:
   - `payment_status: 'refunded'`
   - `paid: false`

**Логирование**:
```
💰 Refund succeeded for order 123
💎 Refunding loyalty points for order 123
```

**Обработка ошибок**:
- Всегда возвращает HTTP 200
- Логирует все ошибки
- При ошибке начисления баллов не прерывает обновление заказа
- При отсутствии orderId в metadata логирует полную структуру события

**Логирование webhook**:
```
📥 YooKassa webhook received: {
  type: 'notification',
  event: 'payment.succeeded',
  paymentId: '...',
  ip: '...',
  hasObject: true,
  objectKeys: [...],
  fullEvent: '...' // первые 1000 символов
}
```

---

### 5. Проверка статуса платежа (`/api/payments/yookassa/status/[id]`)

**Метод**: `GET`

**Назначение**: Проверка статуса платежа по `payment_id` через API YooKassa

**Использование**: 
- На странице успешной оплаты (`/payment/success`)
- Для проверки статуса, если webhook еще не пришел

**Процесс**:
1. Получение `payment_id` из параметров URL
2. Запрос к API YooKassa: `yookassaClient.payments.paymentsPaymentIdGet(paymentId)`
3. Если статус `succeeded`:
   - Получение `orderId` из `payment.metadata.orderId`
   - Проверка, что заказ еще не обновлен
   - Обновление заказа (аналогично webhook `payment.succeeded`)

**Ответ**:
```json
{
  "paymentId": "2c7c2c8a-0001-5000-8000-1e8b8b8b8b8b",
  "status": "succeeded",
  "paid": true,
  "amount": {
    "value": "1500.00",
    "currency": "RUB"
  },
  "metadata": {
    "orderId": "123"
  }
}
```

**Обработка ошибок**:
- Возвращает 400 при отсутствии payment_id
- Возвращает 500 при ошибке запроса к YooKassa
- Логирует все ошибки

---

### 6. Возврат средств (`/api/payments/yookassa/refund`)

**Метод**: `POST`

**Назначение**: Создание возврата средств через YooKassa API при отмене оплаченного заказа

**Входные данные**:
```json
{
  "orderId": 123,
  "paymentId": "2c7c2c8a-0001-5000-8000-1e8b8b8b8b8b",
  "amount": 1500.00,
  "reason": "Отмена заказа #123"
}
```

**Процесс**:
1. Валидация входных данных (orderId, paymentId обязательны)
2. Получение заказа из БД для проверки
3. Проверка статуса платежа в YooKassa:
   - Если статус `waiting_for_capture` → отмена платежа (cancel)
   - Если статус `succeeded` → создание возврата (refund)
   - Если другой статус → ошибка
4. Валидация суммы возврата (не должна превышать сумму платежа)
5. Создание возврата через SDK:
   - Ключ идемпотентности: `refund_order_${orderId}_${Date.now()}`
   - Сумма в рублях (RUB)
   - Описание возврата
6. Обновление статуса заказа:
   - `refunded` - если возврат завершен (`succeeded`)
   - `refund_pending` - если возврат в обработке

**Ответ при успехе**:
```json
{
  "success": true,
  "refundId": "2c7c2c8a-0001-5000-8000-refund123",
  "paymentId": "2c7c2c8a-0001-5000-8000-1e8b8b8b8b8b",
  "orderId": 123,
  "amount": 1500.00,
  "status": "succeeded",
  "message": "Refund completed successfully"
}
```

**Ответ при отмене платежа (waiting_for_capture)**:
```json
{
  "success": true,
  "action": "cancelled",
  "paymentId": "2c7c2c8a-0001-5000-8000-1e8b8b8b8b8b",
  "status": "canceled",
  "message": "Payment was cancelled (not captured yet)"
}
```

**Обработка ошибок**:
- Возвращает 400 при отсутствии обязательных полей
- Возвращает 400 если платеж не в статусе `succeeded` или `waiting_for_capture`
- Возвращает 400 если сумма возврата превышает сумму платежа
- Возвращает 404 если заказ не найден
- Возвращает 500 при ошибке запроса к YooKassa
- Логирует все ошибки с деталями

**Логирование**:
```
💳 Заказ 123 был оплачен онлайн - создаем возврат средств через YooKassa
🔍 Payment status check: { paymentId: '...', status: 'succeeded', orderId: 123 }
✅ YooKassa refund created: { refundId: '...', paymentId: '...', orderId: 123, amount: 1500, status: 'succeeded' }
```

**Интеграция с отменой заказа**:
- Автоматически вызывается при отмене оплаченного заказа через `DELETE /api/orders/[id]`
- Работает только для заказов с `payment_method === 'online'` и наличием `payment_id`
- Не прерывает процесс отмены заказа при ошибке (логируется предупреждение)
- Возврат баллов лояльности выполняется независимо от результата возврата средств

**Важные замечания**:
- Возврат средств может занять 3-5 рабочих дней
- Комиссия YooKassa за платеж не возвращается
- При ошибке возврата администратор может создать возврат вручную через личный кабинет YooKassa
- Идемпотентность гарантируется ключом идемпотентности

---

### 7. Диагностические endpoints

#### `/api/payments/yookassa/webhook/check`

**Метод**: `GET`

**Назначение**: Проверка конфигурации webhook

**Ответ**:
```json
{
  "status": "ok",
  "webhook": {
    "url": "https://ogfoody.ru/api/payments/yookassa/webhook",
    "testUrl": "https://ogfoody.ru/api/payments/yookassa/webhook/test",
    "isConfigured": "Check in YooKassa dashboard"
  },
  "configuration": {
    "shopId": "1251...",
    "testMode": true,
    "secretKeyPrefix": "test_sDZCH..."
  },
  "instructions": {
    "step1": "Go to https://yookassa.ru/my",
    "step2": "Select test shop (ID: 1251656)",
    "step3": "Go to Integration → HTTP Notifications",
    "step4": "Set URL: https://ogfoody.ru/api/payments/yookassa/webhook",
    "step5": "Subscribe to: payment.succeeded, payment.canceled",
    "step6": "Save and test"
  }
}
```

#### `/api/payments/yookassa/webhook/test`

**Методы**: `GET`, `POST`

**Назначение**: Тестовый endpoint для проверки доступности webhook URL

**GET**: Возвращает информацию о endpoint
**POST**: Принимает тестовые данные и возвращает их обратно

---

## 🎨 UI компоненты

### PaymentModal (`components/payment-modal.tsx`)

**Назначение**: Модальное окно для выбора способа оплаты

**Функциональность**:
1. Выбор способа оплаты:
   - **Онлайн** (карта, СБП, электронные кошельки)
   - **Наличные** (опционально, по умолчанию разрешено)

2. Использование баллов лояльности:
   - Чекбокс для включения
   - Ползунок для выбора количества (максимум 50% от суммы)
   - Отображение итоговой суммы с учетом баллов

3. Создание платежа:
   - При выборе "Онлайн":
     - Сохранение использованных баллов в localStorage
     - Запрос к `/api/payments/yookassa/create`
     - Редирект на `confirmation_url`
   - При выборе "Наличные":
     - Вызов `onPaymentComplete` с методом `cash`

**Особенности**:
- Улучшенный UX для мобильных устройств (увеличенная область клика для ползунка)
- Визуальная индикация выбранного способа оплаты
- Информация о начислении баллов при наличной оплате

---

### PaymentSuccessPage (`app/payment/success/page.tsx`)

**Назначение**: Страница после редиректа с YooKassa

**Процесс проверки оплаты**:

1. **Первичная проверка** (каждые 2 секунды, до 15 раз = 30 секунд):
   - Запрос к `/api/orders/${orderId}`
   - Проверка `order.paid` или `order.paymentStatus === 'paid'`
   - Если оплачен → показ успеха

2. **Проверка через YooKassa API** (после 3 проверок = 6 секунд):
   - Если есть `payment_id`, запрос к `/api/payments/yookassa/status/${paymentId}`
   - Если статус `succeeded` → обновление заказа и показ успеха

3. **Таймаут** (30 секунд):
   - Если есть `payment_id` → показ успеха (платеж создан, webhook придет позже)
   - Если нет `payment_id` → показ ошибки

**Состояния**:
- `loading` - проверка оплаты (с индикатором загрузки)
- `success` - оплата успешна (с информацией о начисленных баллах)
- `error` - ошибка проверки (с кнопкой повторной проверки)

**Особенности**:
- Автоматическая проверка статуса
- Fallback на проверку через API YooKassa
- Информация о начисленных баллах
- Кнопки навигации (главная, просмотр заказа)

---

## 🔄 Поток оплаты

### Полный цикл оплаты

1. **Пользователь выбирает оплату**:
   - Открывается `PaymentModal`
   - Выбирает способ оплаты (онлайн/наличные)
   - Опционально использует баллы лояльности

2. **Создание платежа** (только для онлайн):
   - Запрос к `/api/payments/yookassa/create`
   - Получение `confirmation_url`
   - Редирект на страницу оплаты YooKassa

3. **Оплата на стороне YooKassa**:
   - Пользователь вводит данные карты
   - YooKassa обрабатывает платеж
   - Редирект на `/payment/success?orderId=${orderId}`

4. **Проверка статуса**:
   - Страница `/payment/success` проверяет статус заказа
   - Если webhook еще не пришел, проверка через API YooKassa

5. **Webhook-уведомление** (асинхронно):
   - YooKassa отправляет webhook на `/api/payments/yookassa/webhook`
   - Обновление статуса заказа
   - Начисление баллов лояльности

6. **Завершение**:
   - Пользователь видит подтверждение оплаты
   - Заказ обновлен в системе
   - Баллы начислены (если применимо)

### Поток отмены заказа с возвратом средств

1. **Пользователь отменяет заказ**:
   - Вызов `DELETE /api/orders/[id]` из интерфейса
   - Проверка, что заказ был оплачен онлайн

2. **Создание возврата средств** (если заказ оплачен онлайн):
   - Проверка наличия `payment_id` в заказе
   - Вызов `/api/payments/yookassa/refund`
   - Проверка статуса платежа в YooKassa:
     - Если `waiting_for_capture` → отмена платежа (деньги не списывались)
     - Если `succeeded` → создание возврата (refund)

3. **Обработка возврата**:
   - YooKassa обрабатывает возврат средств
   - Обновление статуса заказа на `refunded` или `refund_pending`
   - Webhook `refund.succeeded` придет асинхронно (если настроен)

4. **Возврат баллов лояльности**:
   - Вызов `refundLoyaltyPoints()` независимо от результата возврата средств
   - Возврат использованных баллов
   - Списывание начисленных баллов

5. **Завершение**:
   - Заказ помечен как `cancelled`
   - Статус оплаты: `refunded` или `refund_pending`
   - Баллы возвращены
   - Деньги возвращаются на карту клиента (3-5 рабочих дней)

---

## 🔐 Безопасность

### IP-валидация

- **Продакшн**: Строгая проверка IP-адресов YooKassa
- **Тестовый режим**: Предупреждение, но не блокировка (для работы через прокси/CDN)

### Идемпотентность

- Ключ идемпотентности: `order_${orderId}_${Date.now()}`
- Предотвращает дублирование платежей при повторных запросах

### Metadata

- `orderId` сохраняется в `metadata` платежа
- Используется для связи платежа с заказом в webhook

### HTTP 200 в webhook

- **Критично**: YooKassa требует HTTP 200, иначе повторяет отправку
- Всегда возвращаем 200, даже при ошибках
- Ошибки логируются для отладки

---

## 📊 Интеграция с системой лояльности

### Начисление баллов

**Триггер**: Событие `payment.succeeded` в webhook

**Логика**:
- Проверка, что баллы еще не начислены (`loyalty_points_earned === 0`)
- Расчет баллов на основе суммы заказа
- Учет использованных баллов (`loyalty_points_used`)
- Вызов `awardLoyaltyPoints()`

**Транзакции**:
- Тип: `earned`
- Статус: `completed`
- Связь с заказом через `order_id`

### Возврат баллов

**Триггер**: Событие `refund.succeeded` в webhook

**Логика**:
- Возврат использованных баллов (транзакция `refunded`)
- Списывание начисленных баллов (транзакция `cancelled`)
- Откат `total_spent` пользователя
- Вызов `refundLoyaltyPoints()`

**Транзакции**:
- `refunded`: +использованные баллы
- `cancelled`: -начисленные баллы

---

## 🧪 Тестирование

### Тестовые карты

Документация: `YOOKASSA_TEST_CARDS.md`

**Успешные платежи**:
- `5555555555554444` (Mastercard, без 3-D Secure) - **рекомендуется для автоплатежей**
- `4793128161644804` (Visa, с 3-D Secure)
- `2200000000000004` (Mir, с 3-D Secure)

**Неуспешные платежи**:
- Различные сценарии ошибок (недостаточно средств, истек срок карты, и т.д.)

### Проверка webhook

1. **Настройка в личном кабинете**:
   - URL: `https://ogfoody.ru/api/payments/yookassa/webhook`
   - События: `payment.succeeded`, `payment.canceled`, `payment.waiting_for_capture`, `refund.succeeded`

2. **Проверка логов**:
   ```bash
   pm2 logs ogfoody --lines 100 | grep -i webhook
   ```

3. **Диагностика**:
   - `GET /api/payments/yookassa/webhook/check` - проверка конфигурации
   - `GET /api/payments/yookassa/webhook` - информация о endpoint

### Тестирование возврата средств

1. **Создание тестового платежа**:
   - Создайте заказ и оплатите его тестовой картой
   - Дождитесь успешной оплаты (статус `succeeded`)

2. **Отмена заказа**:
   - Отмените заказ через интерфейс сайта
   - Проверьте логи на наличие сообщений о возврате средств

3. **Проверка возврата**:
   - В логах должно быть: `💳 Заказ X был оплачен онлайн - создаем возврат средств через YooKassa`
   - Затем: `✅ YooKassa refund created` или `✅ Payment cancelled`
   - Проверьте статус заказа в БД: должен быть `refunded` или `refund_pending`

4. **Проверка в личном кабинете YooKassa**:
   - Откройте https://yookassa.ru/my/payments
   - Найдите платеж по `payment_id`
   - Проверьте наличие возврата (refund) для этого платежа

5. **Тестирование отмены платежа (waiting_for_capture)**:
   - Создайте платеж, но не завершайте оплату
   - Отмените заказ до завершения оплаты
   - Платеж должен быть отменен (cancel), а не возвращен (refund)

---

## 🐛 Обработка ошибок

### Типичные проблемы

1. **Webhook не приходит**:
   - Проверить URL в личном кабинете YooKassa
   - Проверить SSL-сертификат
   - Проверить логи сервера
   - Проверить IP-валидацию (в тестовом режиме менее строгая)

2. **Заказ не обновляется**:
   - Проверить наличие `orderId` в `metadata` платежа
   - Проверить логи webhook на наличие ошибок
   - Проверить, что заказ существует в БД

3. **Баллы не начисляются**:
   - Проверить логи webhook на наличие ошибок
   - Проверить, что `loyalty_points_earned === 0` (не начислены ранее)
   - Проверить наличие `user_id` в заказе

4. **IP блокируется**:
   - В тестовом режиме это нормально (логируется предупреждение)
   - В продакшене проверить настройки прокси/CDN
   - Убедиться, что реальный IP передается в заголовках

---

## 📝 Важные замечания

1. **Metadata обязательна**: Без `orderId` в metadata webhook не сможет связать платеж с заказом

2. **HTTP 200 критичен**: YooKassa повторяет отправку webhook, если не получает 200

3. **Идемпотентность**: Ключ идемпотентности предотвращает дублирование платежей и возвратов

4. **Тестовый режим**: В тестовом режиме IP-валидация менее строгая для работы через прокси/CDN

5. **Автоматическое подтверждение**: `capture: true` означает, что платеж подтверждается автоматически

6. **Проверка статуса**: Страница успешной оплаты проверяет статус через API, если webhook еще не пришел

7. **Баллы лояльности**: Начисляются только при успешной оплате онлайн, проверяется отсутствие предыдущего начисления

8. **Возврат средств**: 
   - Автоматически создается при отмене оплаченного заказа (только для онлайн-оплаты)
   - Возврат может занять 3-5 рабочих дней
   - Комиссия YooKassa не возвращается
   - При ошибке возврата администратор может создать возврат вручную через личный кабинет

9. **Отмена платежа**: Если платеж в статусе `waiting_for_capture`, он отменяется (деньги не списывались), возврат не требуется

---

## 🔗 Полезные ссылки

- [Документация YooKassa: HTTP-уведомления](https://yookassa.ru/developers/using-api/webhooks)
- [Документация YooKassa: Тестовый режим](https://yookassa.ru/developers/payment-acceptance/getting-started/testing)
- [Личный кабинет YooKassa](https://yookassa.ru/my)
- [История платежей](https://yookassa.ru/my/payments)
- [Документация проекта: YOOKASSA_WEBHOOK_SETUP.md](./YOOKASSA_WEBHOOK_SETUP.md)
- [Тестовые карты: YOOKASSA_TEST_CARDS.md](./YOOKASSA_TEST_CARDS.md)

---

## 📋 Чеклист настройки

- [ ] Переменные окружения настроены (`YOOKASSA_SHOP_ID`, `YOOKASSA_SECRET_KEY`, `YOOKASSA_TEST_MODE`)
- [ ] Webhook URL настроен в личном кабинете YooKassa
- [ ] Подписаны на события: `payment.succeeded`, `payment.canceled`, `payment.waiting_for_capture`, `refund.succeeded`
- [ ] Проверена доступность endpoint: `curl https://ogfoody.ru/api/payments/yookassa/webhook/check`
- [ ] Проведено тестовое платеж с тестовой картой
- [ ] Проверены логи webhook: `pm2 logs ogfoody --lines 100 | grep webhook`
- [ ] Проверено начисление баллов лояльности
- [ ] Проверена обработка возвратов (refunds)

---

*Документация обновлена: 2026-01-17*
