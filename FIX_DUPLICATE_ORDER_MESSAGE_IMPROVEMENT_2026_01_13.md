# Улучшение: Сообщение об ошибке дубликата заказа с номером заказа - 2026-01-13

## 🐛 Проблема (из баг-репорта):

**Отчет:** `2026-01-13T22-25-21-322Z_user-121_logs.txt`  
**User:** 121 (Mac, Chrome 143.0.0.0)  
**Дата заказа:** 2026-01-23

**Ошибка:**
```
❌ Ошибка при создании заказа: {
  "error": "Order already exists for this date",
  "details": "На эту дату (2026-01-23) у вас уже есть заказ. Отмените существующий заказ или выберите другую дату.",
  "existingOrderId": 560,
  "existingOrderNumber": "ORD-20260113-IFP935",
  "date": "2026-01-23"
}
```

**Проблема:**
- API возвращает `existingOrderId` и `existingOrderNumber`, но они не используются в сообщении
- Пользователь не видит номер существующего заказа
- Существующий заказ не загружается в календарь, поэтому пользователь не может его найти

## 🔍 Причина:

1. **Сообщение об ошибке** не показывало номер существующего заказа
2. **Заказы не загружались** после ошибки, поэтому существующий заказ не отображался в календаре
3. **errorData не сохранялась** в error для доступа в catch блоках

## ✅ Решение:

### 1. Улучшено сообщение об ошибке (строка 2608-2640):

```typescript:2608:2640:app/page.tsx
// ✅ ИСПРАВЛЕНО 2026-01-13: Формируем детальное сообщение с номером заказа
let description = errorData.details || `На эту дату (${typeof updatedOrder.startDate === 'string' ? new Date(updatedOrder.startDate).toLocaleDateString('ru-RU') : updatedOrder.startDate.toLocaleDateString('ru-RU')}) у вас уже есть заказ.`

if (errorData.existingOrderNumber) {
  description += ` Номер заказа: ${errorData.existingOrderNumber}.`
}

description += " Отмените существующий заказ или выберите другую дату."

setWarningDialog({
  open: true,
  title: "Заказ уже существует",
  description,
  variant: "warning",
})

// ✅ ИСПРАВЛЕНО 2026-01-13: Если есть existingOrderId, загружаем заказы чтобы показать существующий
if (errorData.existingOrderId) {
  console.log(`🔄 Загружаем заказы для отображения существующего заказа #${errorData.existingOrderId}`)
  try {
    const ordersResponse = await fetch(`/api/orders?userId=${userProfile.id}`)
    if (ordersResponse.ok) {
      const ordersData = await ordersResponse.json()
      if (ordersData.orders) {
        setOrders(ordersData.orders)
        console.log(`✅ Заказы загружены, найден заказ #${errorData.existingOrderId}`)
      }
    }
  } catch (error) {
    console.error("❌ Ошибка при загрузке заказов:", error)
  }
}
```

### 2. Сохранение errorData в error (строка 2665-2670):

```typescript:2665:2670:app/page.tsx
// ✅ ИСПРАВЛЕНО 2026-01-13: Сохраняем errorData в error для доступа в catch
const errorWithData = new Error(errorData.error || "Failed to create order")
;(errorWithData as any).data = errorData
throw errorWithData
```

### 3. Обновлена обработка в catch блоках (строка 2681-2720, 2717-2755):

Добавлена проверка `errorData.existingOrderNumber` и загрузка заказов для отображения существующего заказа.

## 🎯 Результат:

**До исправления:**
- ❌ Сообщение не показывало номер существующего заказа
- ❌ Существующий заказ не загружался в календарь
- ❌ Пользователь не мог найти существующий заказ

**После исправления:**
- ✅ Сообщение показывает номер существующего заказа: "Номер заказа: ORD-20260113-IFP935"
- ✅ Существующий заказ автоматически загружается и отображается в календаре
- ✅ Пользователь может найти и открыть существующий заказ
- ✅ Улучшен UX - больше информации для пользователя

## 📁 Измененные файлы:

1. `app/page.tsx` - улучшена обработка ошибки "Order already exists" в трех местах:
   - Обработка ошибки API (строка 2608-2640)
   - Catch блок создания заказа (строка 2681-2720)
   - Общий catch блок (строка 2717-2755)

## 🔗 Связанные проблемы:

Это **дополнение** к исправлению бага #19 "Обработка ошибки 'Order already exists' в handleAutoCheckout":
- Баг #19: Добавлена обработка ошибки дубликата
- Баг #20: Улучшено сообщение с номером заказа и загрузка заказов

## 🧪 Тестирование:

**До исправления:**
- ❌ Сообщение: "На эту дату у вас уже есть заказ. Отмените существующий заказ или выберите другую дату."
- ❌ Существующий заказ не отображался в календаре

**После исправления:**
- ✅ Сообщение: "На эту дату (23.01.2026) у вас уже есть заказ. Номер заказа: ORD-20260113-IFP935. Отмените существующий заказ или выберите другую дату."
- ✅ Существующий заказ отображается в календаре
- ✅ Пользователь может найти и открыть существующий заказ

## 🚀 Деплой:

```bash
# Время: 2026-01-13T22:30:00Z
# Статус: Готов к деплою
```

---

**Итог:** Улучшено сообщение об ошибке дубликата заказа. Теперь пользователь видит номер существующего заказа, и заказ автоматически загружается в календарь. 🎉
