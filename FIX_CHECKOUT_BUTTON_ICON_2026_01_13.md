# Исправление: Иконка на кнопке "Оформить заказ" - 2026-01-13

## 🐛 Проблема (из баг-репорта):

> "Здесь наверное не нужно делать прилипшую кнопку «заказать» и на ней не должно быть дискеты"

**User:** 122  
**Device:** iPhone iOS 18.7, Safari Mobile  
**Time:** 2026-01-13T14:40:07  
**Report ID:** 2026-01-13T14-40-07-254Z

## 🔍 Причина:

В компоненте `components/profile-modal.tsx` на кнопке "Оформить заказ" использовалась иконка **Save** (дискета), что семантически неверно для оформления заказа. Иконка дискеты ассоциируется с сохранением данных, а не с оформлением/подтверждением заказа.

### Было (строка 540):
```typescript
<Save className="w-5 h-5 inline mr-2" strokeWidth={3} />
{isCheckoutFlow ? "Оформить заказ" : "Сохранить"}
```

## ✅ Решение:

Заменил иконку на **CheckCircle2** для режима оформления заказа (`isCheckoutFlow = true`), оставив **Save** только для обычного режима сохранения профиля.

### Стало:
```typescript
{isCheckoutFlow ? (
  <>
    <CheckCircle2 className="w-5 h-5 inline mr-2" strokeWidth={3} />
    Оформить заказ
  </>
) : (
  <>
    <Save className="w-5 h-5 inline mr-2" strokeWidth={3} />
    Сохранить
  </>
)}
```

## 📁 Измененные файлы:

1. `components/profile-modal.tsx`:
   - Добавлен импорт `CheckCircle2` из lucide-react
   - Условный рендеринг иконки в зависимости от `isCheckoutFlow`

## 📊 Результат:

- **До:** Кнопка "Оформить заказ" имела иконку дискеты 💾 (Save)
- **После:** Кнопка "Оформить заказ" имеет иконку галочки ✅ (CheckCircle2)
- **Профиль:** Кнопка "Сохранить" по-прежнему имеет иконку дискеты 💾 (Save)

## 🎯 UX улучшение:

- ✅ Более понятная семантика для пользователей
- ✅ Иконка CheckCircle2 четко показывает завершение/подтверждение действия
- ✅ Иконка Save остается для режима редактирования профиля

## 🚀 Деплой:

```bash
git add components/profile-modal.tsx FIX_CHECKOUT_BUTTON_ICON_2026_01_13.md
git commit -m "fix: заменил иконку дискеты на CheckCircle2 для кнопки оформления заказа"
git push origin main
```

## 📝 Примечание о "прилипшей кнопке":

Кнопка находится внизу модального окна благодаря структуре `flex flex-col` с `flex-1 overflow-y-auto` на контенте. Это стандартное UX решение для модальных окон - кнопка всегда видна внизу, независимо от прокрутки контента. Это правильное поведение и не требует изменений.

---

**Время исправления:** ~10 минут  
**Статус:** ✅ Готово к деплою


