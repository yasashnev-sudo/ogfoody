# 🚀 Инструкция по деплою на сервер

## ✅ Что уже сделано

Код загружен в GitHub (коммит: `bf01e2ef`)

## ⚠️ ВАЖНО

**Изменения НЕ появятся автоматически на домене после пуша в GitHub!**

Нужно зайти на сервер и обновить код вручную.

---

## 📋 Быстрый деплой (скопируй и вставь)

```bash
# 1. Подключись к серверу
ssh root@5.129.194.168

# 2. Перейди в директорию проекта
cd /var/www/ogfoody

# 3. Обнови код из GitHub
git pull origin main

# 4. Установи зависимости (если были новые)
npm install

# 5. Пересобери проект
npm run build

# 6. Перезапусти приложение
pm2 restart ogfoody

# 7. Проверь логи (опционально)
pm2 logs ogfoody --lines 50
```

**Пароль сервера**: `pULRoAvF@P-@4Y`

---

## 🔍 Пошаговая инструкция

### Шаг 1: Подключение к серверу

```bash
ssh root@5.129.194.168
```

Введи пароль: `pULRoAvF@P-@4Y`

### Шаг 2: Переход в директорию проекта

```bash
cd /var/www/ogfoody
```

### Шаг 3: Проверка текущего статуса

```bash
# Проверь, что ты в правильной директории
pwd
# Должно быть: /var/www/ogfoody

# Проверь текущую ветку
git branch
# Должно быть: * main

# Проверь статус
git status
```

### Шаг 4: Обновление кода

```bash
# Получи последние изменения из GitHub
git pull origin main
```

Ты должен увидеть список обновлённых файлов:
- `app/api/orders/[id]/route.ts`
- `components/order-history.tsx`
- `components/order-modal.tsx`
- И другие...

### Шаг 5: Установка зависимостей

```bash
npm install
```

(Этот шаг можно пропустить, если не добавлялись новые пакеты)

### Шаг 6: Сборка проекта

```bash
npm run build
```

Это займёт 1-2 минуты. Дождись завершения.

### Шаг 7: Перезапуск приложения

```bash
pm2 restart ogfoody
```

### Шаг 8: Проверка

```bash
# Проверь, что приложение работает
pm2 status

# Посмотри логи (последние 50 строк)
pm2 logs ogfoody --lines 50

# Если нужно смотреть логи в реальном времени
pm2 logs ogfoody
# (Выход: Ctrl+C)
```

### Шаг 9: Проверка на сайте

1. Открой браузер
2. Перейди на `https://ogfoody.ru`
3. Создай заказ на сегодня
4. Проверь, что вместо кнопки "Отменить" есть кнопка "Поддержка"

---

## 🔧 Если что-то пошло не так

### Проблема: `git pull` не работает

```bash
# Проверь, есть ли незакоммиченные изменения
git status

# Если есть изменения, сохрани их
git stash

# Попробуй снова
git pull origin main

# Верни изменения (если нужно)
git stash pop
```

### Проблема: `npm run build` падает с ошибкой

```bash
# Удали node_modules и установи заново
rm -rf node_modules
npm install
npm run build
```

### Проблема: Сайт не работает после деплоя

```bash
# Проверь логи
pm2 logs ogfoody --lines 100

# Перезапусти с полной перезагрузкой
pm2 delete ogfoody
pm2 start npm --name ogfoody -- start

# Или используй ecosystem файл
pm2 start ecosystem.config.js
```

### Проблема: Старая версия всё ещё показывается

```bash
# Очисти кэш Next.js
rm -rf .next
npm run build
pm2 restart ogfoody

# В браузере:
# - Открой DevTools (F12)
# - Правый клик на кнопку обновления → "Очистить кэш и обновить"
```

---

## 📱 Автоматический деплой (опционально)

Можешь создать скрипт для быстрого деплоя:

```bash
# На сервере создай файл
nano /root/deploy-ogfoody.sh
```

Вставь:

```bash
#!/bin/bash
echo "🚀 Деплой ogfoody.ru..."

cd /var/www/ogfoody

echo "📥 Получение изменений из GitHub..."
git pull origin main

echo "📦 Установка зависимостей..."
npm install

echo "🔨 Сборка проекта..."
npm run build

echo "🔄 Перезапуск приложения..."
pm2 restart ogfoody

echo "✅ Деплой завершён!"
echo "📊 Статус приложения:"
pm2 status ogfoody

echo "📝 Последние логи:"
pm2 logs ogfoody --lines 20 --nostream
```

Сделай скрипт исполняемым:

```bash
chmod +x /root/deploy-ogfoody.sh
```

Теперь для деплоя достаточно запустить:

```bash
/root/deploy-ogfoody.sh
```

---

## 🔗 Полезные команды

```bash
# Проверить статус PM2
pm2 status

# Посмотреть логи в реальном времени
pm2 logs ogfoody

# Остановить приложение
pm2 stop ogfoody

# Запустить приложение
pm2 start ogfoody

# Перезагрузить конфигурацию PM2
pm2 reload ecosystem.config.js

# Проверить использование ресурсов
pm2 monit
```

---

## 📞 Контакты

- **Домен**: https://ogfoody.ru
- **NocoDB**: https://noco.povarnakolesah.ru
- **Сервер**: 5.129.194.168

---

## ✅ Чек-лист деплоя

- [ ] Подключился к серверу
- [ ] Перешёл в `/var/www/ogfoody`
- [ ] Выполнил `git pull origin main`
- [ ] Выполнил `npm install` (если нужно)
- [ ] Выполнил `npm run build`
- [ ] Выполнил `pm2 restart ogfoody`
- [ ] Проверил логи `pm2 logs ogfoody --lines 50`
- [ ] Проверил сайт в браузере
- [ ] Проверил работу кнопки "Поддержка"

---

**Время деплоя**: ~3-5 минут  
**Последнее обновление**: 13 января 2026

