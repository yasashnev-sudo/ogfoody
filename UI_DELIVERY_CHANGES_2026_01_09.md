# UI: ОТОБРАЖЕНИЕ ДОСТАВКИ И МИНИМАЛЬНОЙ СУММЫ
**Дата**: 2026-01-09  
**Компонент**: `components/order-modal.tsx`

---

## ✅ РЕАЛИЗОВАННЫЕ ИЗМЕНЕНИЯ

### 1. Расчет стоимости доставки в UI
- Доставка **бесплатная** при сумме заказа ≥ 2300₽
- Доставка **250₽** при сумме от 1000₽ до 2299₽
- Доставка **не применяется** если сумма < 1000₽ (минимум не достигнут)

### 2. Красивый блок с разбивкой суммы
**Показывается перед кнопкой оформления, если сумма ≥ 1000₽:**
- Сумма заказа
- Скидка по промокоду (если есть)
- Скидка баллами лояльности (если есть)
- Стоимость доставки (или "Бесплатно")
- **Итого** (жирным шрифтом)
- Подсказка о бесплатной доставке (если применима)

### 3. Предупреждение о минимальной сумме
**Яркий оранжевый блок с иконкой колокольчика:**
- Заголовок: "Минимальная сумма заказа: 1000 ₽"
- Описание: "Добавьте еще блюд на X ₽, чтобы оформить заказ"
- Анимация появления (slide-in + fade-in)

### 4. Блокировка кнопок при недостаточной сумме
**Кнопки становятся неактивными и меняют текст:**
- Основная кнопка: "Минимум 1000 ₽" (вместо "Заказать · X ₽")
- Плавающая кнопка: показывает "МИН. СУММА 1000 ₽" с иконкой колокольчика
- Стили: opacity-50, cursor-not-allowed

---

## 🎨 ВИЗУАЛЬНЫЙ ДИЗАЙН

### Блок разбивки суммы (≥1000₽):
```
┌─────────────────────────────────────┐
│ 🎯 РАЗБИВКА СУММЫ                   │
│                                     │
│ Сумма заказа:           1,500 ₽    │
│ Промокод WELCOME10:       -150 ₽   │ (фиолетовый)
│ Баллы лояльности:          -50 ₽   │ (фиолетовый)
│ 🚚 Доставка:              +250 ₽    │ (оранжевый)
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│ ИТОГО:                  1,550 ₽    │ (жирный)
│                                     │
│ 💡 Бесплатная доставка от 2,300₽   │
│    (еще 800₽)                       │
└─────────────────────────────────────┘
```

### Предупреждение (<1000₽):
```
┌─────────────────────────────────────┐
│ 🔔  Минимальная сумма заказа:       │
│     1000 ₽                          │
│                                     │
│     Добавьте еще блюд на 300 ₽,    │
│     чтобы оформить заказ            │
└─────────────────────────────────────┘
(оранжевый фон, border-2)
```

---

## 📝 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Константы:
```typescript
const MIN_ORDER_AMOUNT = 1000           // Минимальная сумма заказа
const FREE_DELIVERY_THRESHOLD = 2300    // Порог бесплатной доставки
const deliveryFee = 250                 // Стоимость доставки (по умолчанию)
```

### Логика расчета доставки:
```typescript
const deliveryFee = totalBeforeDiscount >= FREE_DELIVERY_THRESHOLD ? 0 : 
                    totalBeforeDiscount < MIN_ORDER_AMOUNT ? 0 : 
                    250
```

### Расчет итоговой суммы:
```typescript
const subtotalAfterDiscounts = totalBeforeDiscount - pointsDiscount - promoDiscount
const finalTotal = subtotalAfterDiscounts + deliveryFee
```

### Проверки:
```typescript
const isBelowMinimum = totalBeforeDiscount < MIN_ORDER_AMOUNT
const shortfall = MIN_ORDER_AMOUNT - totalBeforeDiscount
```

---

## 🧪 СЦЕНАРИИ ДЛЯ ТЕСТИРОВАНИЯ

### Тест 1: Сумма < 1000₽
**Действия**: Добавить блюда на 700₽
**Ожидается**:
- ✅ Оранжевое предупреждение: "Добавьте еще блюд на 300₽"
- ✅ Кнопки заблокированы, текст: "Минимум 1000 ₽"
- ✅ Плавающая кнопка показывает "МИН. СУММА 1000 ₽"
- ✅ Блок разбивки суммы НЕ показывается

### Тест 2: Сумма 1000₽ - 2299₽ (платная доставка)
**Действия**: Добавить блюда на 1500₽
**Ожидается**:
- ✅ Предупреждение скрыто
- ✅ Блок разбивки показывается:
  - Сумма заказа: 1,500 ₽
  - 🚚 Доставка: +250 ₽
  - **ИТОГО: 1,750 ₽**
- ✅ Подсказка: "💡 Бесплатная доставка от 2,300₽ (еще 800₽)"
- ✅ Кнопка активна: "Заказать · 1,750 ₽"

### Тест 3: Сумма ≥ 2300₽ (бесплатная доставка)
**Действия**: Добавить блюда на 2500₽
**Ожидается**:
- ✅ Блок разбивки показывается:
  - Сумма заказа: 2,500 ₽
  - 🚚 Доставка: **Бесплатно** (зеленый цвет)
  - **ИТОГО: 2,500 ₽**
- ✅ Подсказка о бесплатной доставке НЕ показывается
- ✅ Кнопка активна: "Заказать · 2,500 ₽"

### Тест 4: Использование промокода
**Действия**: Добавить блюда на 1500₽, применить промокод WELCOME10 (-150₽)
**Ожидается**:
- ✅ Блок разбивки:
  - Сумма заказа: 1,500 ₽
  - Промокод WELCOME10: −150 ₽ (фиолетовый)
  - 🚚 Доставка: +250 ₽
  - **ИТОГО: 1,600 ₽**

### Тест 5: Использование баллов лояльности
**Действия**: Добавить блюда на 1500₽, использовать 100 баллов
**Ожидается**:
- ✅ Блок разбивки:
  - Сумма заказа: 1,500 ₽
  - Баллы лояльности: −100 ₽ (фиолетовый)
  - 🚚 Доставка: +250 ₽
  - **ИТОГО: 1,650 ₽**

### Тест 6: Комбинация промокод + баллы
**Действия**: Добавить блюда на 2000₽, применить WELCOME10 (-200₽) и использовать 200 баллов
**Ожидается**:
- ✅ Блок разбивки:
  - Сумма заказа: 2,000 ₽
  - Промокод WELCOME10: −200 ₽
  - Баллы лояльности: −200 ₽
  - 🚚 Доставка: +250 ₽ (т.к. после скидок сумма < 2300₽)
  - **ИТОГО: 1,850 ₽**

---

## 🔍 ДЕТАЛИ РЕАЛИЗАЦИИ

### Цветовая схема:
- **Серый фон** блока разбивки: `bg-gray-50` / `dark:bg-gray-900/50`
- **Скидки** (промокод, баллы): фиолетовый `text-[#9D00FF]`
- **Доставка** (платная): оранжевый `text-orange-600`
- **Бесплатная доставка**: зеленый `text-green-600`
- **Предупреждение**: оранжевый фон `bg-orange-50`, border `border-orange-300`

### Адаптивность:
- Все блоки адаптируются под темную тему (dark mode)
- Используются Tailwind классы с `dark:` префиксом
- Анимации: `animate-in slide-in-from-top-2 fade-in`

### Доступность:
- Иконки с семантическим значением (Bell, Truck)
- Четкий контраст цветов
- Понятные сообщения об ошибках
- Disabled состояния с `cursor-not-allowed`

---

## 📱 МОБИЛЬНАЯ ВЕРСИЯ

Все изменения полностью адаптивны:
- Блоки масштабируются под ширину экрана
- Текст остается читаемым на малых экранах
- Кнопки сохраняют удобный размер для нажатия (h-14, h-16)

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. Стоимость доставки "захардкожена"
В данный момент стоимость доставки = 250₽ (константа).

**Для интеграации с реальными данными:**
- Получать `deliveryFee` из `userProfile.district`
- Делать запрос к API `/api/delivery-zones` или использовать данные из контекста
- Учитывать разные районы с разной стоимостью

### 2. Стоимость рассчитывается на клиенте
Расчет `finalTotal` происходит в компоненте OrderModal.

**Для консистентности:**
- Сервер также должен рассчитывать итоговую сумму
- При отправке заказа сравнивать client-side и server-side суммы
- Отклонять заказ если суммы не совпадают

### 3. Баллы начисляются с subtotal (без доставки)
API (`POST /api/orders`) уже учитывает это, UI только для отображения.

---

## ✅ ГОТОВНОСТЬ

**Статус**: ✅ **ГОТОВО К ТЕСТИРОВАНИЮ**

**Что работает**:
- ✅ Расчет доставки в UI
- ✅ Блок разбивки суммы
- ✅ Предупреждение о минимальной сумме
- ✅ Блокировка кнопок
- ✅ Адаптивный дизайн
- ✅ Темная тема

**Следующий шаг**: Тестирование через интерфейс сайта

---

## 📄 СВЯЗАННЫЕ ФАЙЛЫ

- `DELIVERY_AND_VALIDATION_2026_01_09.md` - Backend реализация
- `components/order-modal.tsx` - Измененный компонент

---

**Готово для пользовательского тестирования!** 🚀



