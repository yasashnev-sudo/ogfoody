# 🛠️ Настройка админ-панели

## 📋 Обзор

Админ-панель доступна по адресу `/admin` и включает:
- ✅ Вход по паролю
- ✅ Дашборд со статистикой
- ✅ Управление заказами
- ✅ Управление пользователями
- ✅ Управление блюдами
- ✅ Чат с клиентами
- ✅ Управление пуш-уведомлениями
- ✅ Управление промокодами
- ✅ Статистика и аналитика

## 🔐 Настройка входа

### Пароль администратора

По умолчанию пароль: `admin123`

Для изменения пароля установите переменную окружения:
```bash
NEXT_PUBLIC_ADMIN_PASSWORD=ваш_новый_пароль
```

**Важно:** В production используйте сложный пароль!

## 📊 Создание таблиц в NocoDB

Для полной функциональности админ-панели нужно создать две таблицы в NocoDB:

### 1. Таблица Messages (Чат с клиентами)

1. Откройте NocoDB: https://noco.povarnakolesah.ru
2. Выберите базу данных "FooD"
3. Создайте новую таблицу "Messages"
4. Добавьте следующие колонки:

| Название колонки | Тип | Обязательное | Описание |
|-----------------|-----|--------------|----------|
| User ID | Link to Users | Нет | Связь с пользователем |
| User Phone | Text | Нет | Телефон пользователя |
| User Name | Text | Нет | Имя пользователя |
| Message | Long Text | Да | Текст сообщения |
| Is Admin | Checkbox | Да | Сообщение от администратора |
| Is Read | Checkbox | Да | Прочитано ли сообщение |
| Created At | DateTime | Да | Дата создания |

5. Скопируйте Table ID из URL (например: `md_xxxxx`)
6. Добавьте в `.env`:
```bash
NOCODB_TABLE_MESSAGES=md_xxxxx
```

### 2. Таблица Push_Notifications (Пуш-уведомления)

1. Создайте новую таблицу "Push_Notifications"
2. Добавьте следующие колонки:

| Название колонки | Тип | Обязательное | Описание |
|-----------------|-----|--------------|----------|
| Title | Text | Да | Заголовок уведомления |
| Message | Long Text | Да | Текст уведомления |
| Target Type | Single Select | Да | Тип получателя (all, user_id, user_phone) |
| Target Value | Text | Нет | Значение (ID или телефон) |
| Status | Single Select | Да | Статус (pending, sent, scheduled, failed) |
| Scheduled At | DateTime | Нет | Запланированное время отправки |
| Sent At | DateTime | Нет | Время отправки |
| Created At | DateTime | Да | Дата создания |

3. Скопируйте Table ID
4. Добавьте в `.env`:
```bash
NOCODB_TABLE_PUSH_NOTIFICATIONS=md_xxxxx
```

## 🚀 Использование

### Вход в админ-панель

1. Откройте `/admin/login`
2. Введите пароль (по умолчанию: `admin123`)
3. После входа вы попадете на дашборд

### Чат с клиентами

1. Перейдите в раздел "Чат с клиентами"
2. Выберите пользователя из списка
3. Отправляйте и получайте сообщения в реальном времени

**Примечание:** Для работы чата клиенты должны отправлять сообщения через API `/api/admin/messages`

### Пуш-уведомления

1. Перейдите в раздел "Пуш-уведомления"
2. Нажмите "Создать уведомление"
3. Заполните форму:
   - Заголовок
   - Сообщение
   - Получатели (все или конкретный пользователь)
   - Запланированное время (опционально)
4. Нажмите "Отправить"

**Примечание:** Для реальной отправки уведомлений нужно интегрировать сервис пуш-уведомлений (Firebase Cloud Messaging, OneSignal и т.д.)

## 🔧 Интеграция с клиентской частью

### Добавление чата для клиентов

Чтобы клиенты могли отправлять сообщения, добавьте компонент чата на главную страницу:

```tsx
// В компоненте клиентской части
const sendMessage = async (message: string) => {
  await fetch("/api/admin/messages", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      user_id: userId,
      user_phone: userPhone,
      user_name: userName,
      message,
      is_admin: false,
    }),
  })
}
```

## 📝 Примечания

- Сессия администратора действительна 8 часов
- Все данные загружаются из NocoDB в реальном времени
- Статистика обновляется автоматически
- Чат обновляется каждые 5 секунд
- Уведомления обновляются каждые 10 секунд

## 🎨 Стиль

Админ-панель использует тот же brutal design стиль, что и основной сайт:
- Черные границы (`border-2 border-black`)
- Тени (`shadow-brutal`)
- Яркие цвета (#9D00FF, #FFEA00)
