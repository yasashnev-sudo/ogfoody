# 🐛 ИСПРАВЛЕНИЕ: startDate сохраняется как ISO timestamp вместо YYYY-MM-DD

**Дата:** 2026-01-11  
**Файлы:** `app/page.tsx`

---

## 🐛 ПРОБЛЕМА

В логах `startDate` отображается как ISO timestamp вместо `YYYY-MM-DD`:

```javascript
// ❌ ПРОБЛЕМА в логе:
"startDate": "2026-01-10T21:00:00.000Z"  // ISO timestamp

// ✅ А в API правильно:
"start_date": "2026-01-11"  // YYYY-MM-DD
```

### Симптомы:

**User #108 (лог 19:01:59):**
- API возвращает `start_date: "2026-01-11"` ✅
- Frontend логирует `startDate: "2026-01-10T21:00:00.000Z"` ❌
- Заказ не отображается в календаре корректно

---

## 🔍 ПРИЧИНА

### Проблема: Преобразование строки в Date объект

В трех местах `app/page.tsx` при маппинге заказов из API мы преобразовывали `startDate` в `Date` объект:

```typescript
// ❌ БАГ: Преобразование в Date объект (строки 526, 2266, 3110)
startDate: toDate(db.startDate ?? db.start_date ?? db["Start Date"]),
```

### Последовательность событий:

1. API возвращает `start_date: "2026-01-11"` (строка)
2. Frontend вызывает `toDate("2026-01-11")`
3. `toDate` преобразует строку в `Date` объект
4. `Date` объект сохраняется в React state
5. При логировании `JSON.stringify(Date)` → ISO timestamp: `"2026-01-10T21:00:00.000Z"`
6. Календарь пытается отобразить заказ, но дата некорректна из-за timezone

### Почему это проблема?

1. **Timezone конверсия:** `"2026-01-11"` → `Date` → `"2026-01-10T21:00:00.000Z"` (UTC-3)
2. **Логирование:** `Date` объект при сериализации превращается в ISO timestamp
3. **Календарь:** Получает ISO timestamp вместо простой даты `YYYY-MM-DD`

---

## ✅ РЕШЕНИЕ

### Изменение: Оставляем startDate как строку

Вместо преобразования в `Date` объект, оставляем `startDate` как строку `YYYY-MM-DD`:

```typescript
// ✅ ИСПРАВЛЕНО (строки 526, 2266, 3110):
// Оставляем startDate как строку YYYY-MM-DD
// чтобы избежать преобразования в ISO timestamp при логировании
startDate: db.startDate ?? db.start_date ?? db["Start Date"],
```

### Почему это работает?

Функция `toDate` (строка 46) поддерживает ОБА формата:

```typescript
const toDate = (value: Date | string): Date => {
  if (value instanceof Date) return value
  if (typeof value === "string") {
    if (value.includes("T")) return new Date(value)  // ← ISO
    if (value.includes("-")) return parseDateKey(value)  // ← YYYY-MM-DD
  }
  return new Date(value)
}
```

Это означает, что вся система может работать с `startDate` как со СТРОКОЙ, и преобразовывать в `Date` ТОЛЬКО когда нужно!

### Дополнительное исправление в handlePaymentComplete (строка 1924):

```typescript
// ✅ БЫЛО:
startDate: toDate(updatedOrderFromAPI.start_date || ...),

// ✅ СТАЛО:
// Используем start_date из API напрямую (уже в правильном формате)
startDate: updatedOrderFromAPI.start_date || updatedOrderFromAPI["Start Date"] || o.startDate,
```

---

## 🎯 РЕЗУЛЬТАТ

Теперь:
- ✅ `startDate` хранится как строка `"YYYY-MM-DD"`
- ✅ Логи показывают правильный формат даты
- ✅ Календарь корректно отображает заказы
- ✅ Нет timezone конверсий при логировании
- ✅ API и Frontend синхронизированы

### До исправления:

```javascript
// Frontend лог:
"startDate": "2026-01-10T21:00:00.000Z"  // ❌ ISO, неправильный день

// API ответ:
"start_date": "2026-01-11"  // ✅ Правильно
```

### После исправления:

```javascript
// Frontend лог:
"startDate": "2026-01-11"  // ✅ Правильно

// API ответ:
"start_date": "2026-01-11"  // ✅ Правильно
```

---

## 📋 ТЕСТИРОВАНИЕ

### Сценарий 1: Создание заказа

1. Создать заказ на `2026-01-15`
2. Открыть консоль браузера
3. Проверить логи после создания
4. ✅ `startDate` должен быть `"2026-01-15"`, НЕ ISO timestamp

### Сценарий 2: Загрузка заказов

1. Обновить страницу
2. Дождаться загрузки заказов из API
3. Проверить логи `📅 Calendar: получено заказов`
4. ✅ `startDate` должен быть `"YYYY-MM-DD"`, НЕ ISO timestamp

### Сценарий 3: Календарь

1. Создать заказ на любую дату
2. Проверить отображение в календаре
3. ✅ Заказ должен отображаться на ПРАВИЛЬНОЙ дате
4. ✅ Не должно быть смещения на ±1 день

### Сценарий 4: Debug логи

1. Создать заказ
2. Отправить debug лог
3. Проверить `debug_reports/`
4. ✅ В логе `startDate` должен быть `"YYYY-MM-DD"`

---

## 📊 ИЗМЕНЕННЫЕ СТРОКИ

| Файл | Строка | Изменение |
|------|--------|-----------|
| `app/page.tsx` | 526 | Убрано `toDate()` при маппинге заказов (первая загрузка) |
| `app/page.tsx` | 1924 | Убрано `toDate()` в `handlePaymentComplete` |
| `app/page.tsx` | 2266 | Убрано `toDate()` при маппинге заказов (после входа) |
| `app/page.tsx` | 3110 | Убрано `toDate()` при маппинге заказов (после оплаты) |

---

## 🔄 СВЯЗАННЫЕ ИСПРАВЛЕНИЯ

- ✅ `FIX_CALENDAR_STARTDATE_FORMAT_2026_01_11.md` - Нормализация формата даты в API
- ✅ `FIX_CRITICAL_ORDER_UPDATE_BUG_2026_01_11.md` - Обновление заказов по ID

---

## ✅ СТАТУС

**ИСПРАВЛЕНО** - `startDate` теперь всегда хранится как строка `YYYY-MM-DD`, предотвращая timezone конверсии и проблемы с календарем.


