# Система "Черный ящик" для отладки критических ошибок

## 📋 Описание

Система автоматического логирования и захвата состояния приложения при возникновении критических ошибок, особенно в процессе автооформления заказов.

## 🏗️ Архитектура

### 1. API Endpoint (`app/api/debug/report/route.ts`)
- **Метод**: POST
- **Endpoint**: `/api/debug/report`
- **Формат данных**:
  ```json
  {
    "logs": ["строка1", "строка2", ...],
    "screenshot": "data:image/png;base64,...",
    "meta": {
      "userId": "123",
      "userEmail": "user@example.com",
      "url": "https://...",
      "userAgent": "...",
      "timestamp": "2026-01-11T...",
      "errorMessage": "...",
      "errorData": {...}
    }
  }
  ```

- **Результат**: Создаёт файлы в папке `debug_reports/`:
  - `{timestamp}_user-{userId}_logs.txt` - текстовый файл с логами и метаданными
  - `{timestamp}_user-{userId}_screenshot.png` - скриншот экрана на момент ошибки

### 2. Debug Recorder Hook (`hooks/useDebugRecorder.tsx`)

#### Основные функции:

**`useDebugRecorder(userId?, userEmail?)`** - основной хук

Возвращает объект с методами:

- **`captureError(options)`** - захватить ошибку и отправить отчет
  ```typescript
  await debugRecorder.captureError({
    errorMessage: 'Описание ошибки',
    data: { /* любые данные */ },
    includeScreenshot: true // по умолчанию
  })
  ```

- **`sendManualReport()`** - отправить отчет вручную
  ```typescript
  await debugRecorder.sendManualReport()
  ```

- **`log(message, data?)`** - логировать информацию
  ```typescript
  debugRecorder.log('User clicked button', { buttonId: 'submit' })
  ```

- **`error(message, data?)`** - логировать ошибку
  ```typescript
  debugRecorder.error('Failed to fetch data', { error })
  ```

- **`warn(message, data?)`** - логировать предупреждение

- **`info(message, data?)`** - логировать информацию

- **`getLogs()`** - получить все текущие логи

- **`clearLogs()`** - очистить все логи

- **`captureScreenshot()`** - создать скриншот (используется внутри captureError)

#### Особенности:

1. **Перехват консоли**: Автоматически перехватывает `console.log`, `console.error`, `console.warn`, `console.info`
2. **Хранение логов**: Последние 100 логов (настраивается через `maxLogs`)
3. **Защита от дублирования**: Флаг `isCapturing` предотвращает одновременную отправку нескольких отчетов

### 3. UI Компоненты

#### `DebugFloatingButton` (`components/debug/DebugFloatingButton.tsx`)
- Плавающая кнопка 🐞 в правом нижнем углу экрана
- Показывает количество ошибок красным бейджем
- При клике открывает консоль отладки

#### `DebugConsole` (`components/debug/DebugConsole.tsx`)
- Полноэкранная модалка с логами
- Фильтрация по типу: All, Log, Error, Warn, Info
- Цветовое кодирование логов
- Кнопка "Send Debug Report" для ручной отправки отчета
- Кнопка "Clear Logs" для очистки логов

## 🔧 Интеграция в `handleAutoCheckout`

### Критические точки логирования:

1. **Начало автооформления**:
   ```typescript
   debugRecorder.log("🚀 START handleAutoCheckout", {
     userId, userEmail, pendingCheckoutData
   })
   ```

2. **Ошибка отсутствия района**:
   ```typescript
   await debugRecorder.captureError({
     errorMessage: "Район не указан при автооформлении",
     data: { userId, userEmail }
   })
   ```

3. **Расчет доставки**:
   ```typescript
   debugRecorder.log(`💰 Delivery fee calculated: ${deliveryFee}₽`, {
     district, subtotal
   })
   ```

4. **Пустой массив persons** (критическая ошибка):
   ```typescript
   await debugRecorder.captureError({
     errorMessage: "CRITICAL: persons пуст при автооформлении",
     data: { updatedOrder, pendingCheckout, lastOrder }
   })
   ```

5. **Логирование payload перед отправкой**:
   ```typescript
   const createOrderPayload = { order: updatedOrder, userId }
   debugRecorder.log("📤 POST /api/orders PAYLOAD:", createOrderPayload)
   ```

6. **Ошибки API**:
   ```typescript
   await debugRecorder.captureError({
     errorMessage: `API Error: POST /api/orders (${response.status})`,
     data: { status, errorData, payload }
   })
   ```

7. **Успешное создание заказа**:
   ```typescript
   debugRecorder.log("✅ Order created successfully", {
     orderId, orderNumber
   })
   ```

8. **Необработанные ошибки**:
   ```typescript
   await debugRecorder.captureError({
     errorMessage: `Unhandled error in handleAutoCheckout: ${error.message}`,
     data: { error: error.toString(), stack: error.stack, pendingCheckout }
   })
   ```

## 📂 Структура отчета

### Logs файл (`*_logs.txt`):
```
================================================================================
DEBUG REPORT
================================================================================
Timestamp: 2026-01-11T15:30:00.000Z
User ID: 123
User Email: user@example.com
URL: https://yourapp.com/
User Agent: Mozilla/5.0...
Error Message: CRITICAL: persons пуст при автооформлении

Additional Metadata:
{
  "userId": "123",
  "errorData": {...}
}

================================================================================
LOGS
================================================================================

[2026-01-11T15:29:58.000Z] [LOG] 🚀 START handleAutoCheckout
Data: {"userId":"123",...}

[2026-01-11T15:29:59.000Z] [ERROR] CAPTURE ERROR: CRITICAL: persons пуст
Data: {"updatedOrder":{...}}

================================================================================
END OF REPORT
================================================================================
```

### Screenshot файл (`*_screenshot.png`):
- PNG изображение текущего состояния экрана
- Размер зависит от разрешения экрана пользователя

## 🚀 Использование

### В компоненте (уже интегрировано в `app/page.tsx`):

```typescript
import { useDebugRecorder } from '@/hooks/useDebugRecorder'
import { DebugFloatingButton } from '@/components/debug/DebugFloatingButton'

export default function Home() {
  const debugRecorder = useDebugRecorder(userId, userEmail)
  
  // ... ваш код ...
  
  return (
    <div>
      {/* ваш контент */}
      <DebugFloatingButton userId={userId} userEmail={userEmail} />
    </div>
  )
}
```

### Ручная отправка отчета:
1. Кликните на кнопку 🐞 в правом нижнем углу
2. Просмотрите логи в консоли
3. Нажмите "Send Debug Report"
4. Отчет будет сохранен в `debug_reports/`

### Автоматический захват ошибок:
Все критические ошибки в `handleAutoCheckout` автоматически захватываются и отправляются.

## 📊 Анализ отчетов

### Где искать отчеты:
```bash
cd debug_reports/
ls -la
```

### Просмотр последнего отчета:
```bash
cat $(ls -t debug_reports/*_logs.txt | head -1)
```

### Открыть последний скриншот:
```bash
open $(ls -t debug_reports/*_screenshot.png | head -1)
```

## 🔍 Отладка "гостевого" бага

### Сценарий:
1. Пользователь (гость) создает заказ
2. Происходит авторизация
3. Запускается автооформление
4. Если что-то идет не так → автоматически создается отчет

### Что проверять в отчете:
- ✅ Есть ли `pendingCheckout` в логах?
- ✅ Сохранен ли `persons` массив?
- ✅ Правильно ли передан `paymentMethod`?
- ✅ Какой `payload` был отправлен на сервер?
- ✅ Что вернул сервер (errorData)?
- ✅ Что видел пользователь на экране (screenshot)?

## ⚙️ Настройка

### Изменить количество хранимых логов:
В `hooks/useDebugRecorder.tsx`:
```typescript
const maxLogs = 100; // измените на нужное значение
```

### Отключить перехват консоли:
Закомментируйте `useEffect` с перехватом консоли в хуке.

### Изменить папку для отчетов:
В `app/api/debug/report/route.ts`:
```typescript
const debugDir = join(process.cwd(), 'your_folder_name');
```

## 🛡️ Безопасность

⚠️ **ВАЖНО**: Отчеты могут содержать чувствительные данные!

- Папка `debug_reports/` добавлена в `.gitignore`
- НЕ размещайте отчеты в публичных репозиториях
- Периодически очищайте старые отчеты
- Эндпоинт `/api/debug/report` должен быть защищен в продакшене (добавьте авторизацию при необходимости)

## 📦 Зависимости

- `html2canvas` - для создания скриншотов (установлен: ✅)

## ✅ Статус внедрения

- ✅ API endpoint создан
- ✅ useDebugRecorder хук создан
- ✅ UI компоненты созданы
- ✅ Интеграция в handleAutoCheckout завершена
- ✅ Debug кнопка добавлена в app/page.tsx
- ✅ html2canvas установлен
- ✅ .gitignore обновлен

## 🎯 Цель

Получить полную картину того, что происходит при автооформлении заказа, чтобы:
1. Воспроизвести баг "гостя"
2. Увидеть точный JSON, отправленный на сервер
3. Увидеть состояние UI на момент ошибки
4. Проанализировать последовательность действий

---

**Дата создания**: 11.01.2026  
**Автор**: AI Assistant  
**Версия**: 1.0


