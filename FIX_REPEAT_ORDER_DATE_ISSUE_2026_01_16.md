# Исправление: Повтор заказа открывает существующий заказ на другую дату

**Дата:** 2026-01-16  
**Статус:** ✅ Исправлено

## 🐛 Проблема (из баг-репортов):

### Отчет 1:
- **User ID:** 170
- **Комментарий:** "Повтор заказа на 18 января не сработал, а открыл существующий заказ на 19 января"
- **Время:** 2026-01-16T15:27:26.121Z
- **Устройство:** iPhone (Safari)

### Отчет 2:
- **User ID:** 170
- **Комментарий:** "Тоже самое и на 21 число"
- **Время:** 2026-01-16T15:29:38.334Z
- **Устройство:** iPhone (Safari)

## 🔍 Причина:

Проблема была в несоответствии нормализации дат между функцией `handleRepeatOrder` и логикой определения `existingOrder`:

1. **В `handleRepeatOrder`** использовалась нормализация даты через `setHours(0, 0, 0, 0)` и `.getTime()`
2. **В логике `existingOrder`** использовалась функция `getDateTimestamp`, которая **не нормализовала** время к 00:00:00
3. Это приводило к тому, что при сравнении дат система могла найти существующий заказ на другую дату из-за различий в времени

### Технические детали:

- Функция `getDateTimestamp` использовала `toDate(value).getTime()` без нормализации времени
- Это могло привести к проблемам при сравнении дат, особенно при работе с разными временными зонами
- В `handleRepeatOrder` даты нормализовались вручную через `setHours(0, 0, 0, 0)`, но в `existingOrder` использовалась функция без нормализации

## ✅ Решение:

### 1. Исправлена функция `getDateTimestamp`:
```typescript
// ДО:
const getDateTimestamp = (value: Date | string): number => {
  return toDate(value).getTime()
}

// ПОСЛЕ:
const getDateTimestamp = (value: Date | string): number => {
  const date = toDate(value)
  date.setHours(0, 0, 0, 0)  // ✅ Нормализуем к 00:00:00
  return date.getTime()
}
```

### 2. Улучшена логика определения `existingOrder`:
- Используется `getDateTimestamp` для консистентности с `handleRepeatOrder`
- Добавлено дополнительное логирование для отладки
- Улучшена проверка черновика перед поиском существующего заказа

### 3. Добавлено логирование в `handleRepeatOrder`:
- Логирование перед открытием OrderModal
- Логирование timestamp'ов для отладки

## 📊 Результат:

- ✅ Все сравнения дат теперь используют единую нормализацию к 00:00:00
- ✅ Черновик заказа имеет приоритет над существующим заказом
- ✅ При повторе заказа на выбранную дату создается новый черновик, а не открывается существующий заказ
- ✅ Улучшено логирование для отладки проблем с датами

## 🚀 Деплой:

**Время:** 2026-01-16  
**Статус:** ✅ Готово к деплою

## 📝 Файлы:

- `app/page.tsx`:
  - Функция `getDateTimestamp` (строки 58-62)
  - Логика определения `existingOrder` (строки 3318-3369)
  - Функция `handleRepeatOrder` (строки 1696-1833)

## ⚠️ Важно:

- **НЕ изменялась** логика работы с баллами лояльности
- Все изменения касаются только нормализации дат и логики повтора заказа
- Функция `getDateTimestamp` используется во многих местах кода, поэтому исправление влияет на все сравнения дат

---

**Проверено:** ✅  
**Тестирование:** Требуется проверка на production после деплоя
