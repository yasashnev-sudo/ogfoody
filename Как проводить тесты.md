# Как проводить тесты системы лояльности

## 📋 Содержание

1. [Подготовка к тестированию](#подготовка-к-тестированию)
2. [Очистка базы данных](#очистка-базы-данных)
3. [Деплой изменений](#деплой-изменений)
4. [Запуск тестов](#запуск-тестов)
5. [Описание тестов](#описание-тестов)
6. [Ожидаемые результаты](#ожидаемые-результаты)
7. [Устранение проблем](#устранение-проблем)

---

## 🚀 Подготовка к тестированию

### Требования

- Node.js установлен
- Переменные окружения настроены (`.env.local` или `.env`)
- Доступ к продакшн-серверу через SSH (для деплоя)
- Скрипт `deploy-increment-fix.expect` настроен

### Переменные окружения

Убедитесь, что в `.env` или `.env.local` установлены:

```bash
NOCODB_URL=https://noco.povarnakolesah.ru
NOCODB_TOKEN=ваш_токен
NOCODB_TABLE_USERS=mg9dm2m41bjv8ar
NOCODB_TABLE_ORDERS=m96i4ai2yelbboh
NOCODB_TABLE_LOYALTY_POINTS_TRANSACTIONS=mn244txmccpwmhx
NOCODB_TABLE_PROMO_CODES=m8k9x2m3n4p5q6r
API_BASE=https://ogfoody.ru  # для продакшена
```

---

## 🗑️ Очистка базы данных

**ВАЖНО:** Очистка удаляет ВСЕ данные из указанных таблиц. Используйте только для тестирования!

### Вариант 1: Очистка заказов и транзакций (пользователи остаются)

```bash
npx tsx scripts/cleanup-all-test-data.ts
```

### Вариант 2: Полная очистка (включая пользователей)

```bash
npx tsx scripts/cleanup-all-test-data.ts --delete-users
```

### Вариант 3: Полная очистка (включая промокоды)

```bash
npx tsx scripts/cleanup-all-test-data.ts --delete-users --delete-promos
```

### Что удаляется

- **Loyalty_Points_Transactions** — все транзакции лояльности
- **Orders** — все заказы
- **Users** — все пользователи (только с флагом `--delete-users`)
- **Promo_Codes** — все промокоды (только с флагом `--delete-promos`)

### Пример вывода

```
🚀 НАЧАЛО ПОЛНОЙ ОЧИСТКИ ТЕСТОВЫХ ДАННЫХ
============================================================
🌐 NocoDB: https://noco.povarnakolesah.ru
📊 Режим: development

🗑️ Очистка таблицы Loyalty_Points_Transactions...
   Найдено записей: 517
   Удалено: 100/517
   Удалено: 200/517
   ...
   ✅ Удалено записей: 517

🗑️ Очистка таблицы Orders...
   Найдено записей: 277
   ✅ Удалено записей: 277

🗑️ Очистка таблицы Users...
   Найдено записей: 4
   ✅ Удалено записей: 4

============================================================
✅ ОЧИСТКА ЗАВЕРШЕНА
```

---

## 📦 Деплой изменений

### Шаг 1: Коммит и пуш изменений

```bash
git add -A
git commit -m "Описание изменений"
git push
```

### Шаг 2: Деплой на продакшн

```bash
expect deploy-increment-fix.expect
```

Скрипт автоматически:
1. Подключается к серверу по SSH
2. Выполняет `git pull`
3. Запускает `npm install`
4. Запускает `npm run build`
5. Перезапускает PM2 (`pm2 restart all`)

### Шаг 3: Проверка деплоя

Проверьте логи PM2 на сервере:

```bash
ssh user@server
pm2 logs ogfoody --lines 50
```

Или проверьте доступность API:

```bash
curl https://ogfoody.ru/api/health
```

---

## 🧪 Запуск тестов

### Локальное тестирование

```bash
API_BASE=http://localhost:3000 npx tsx scripts/test-loyalty-clean.ts
```

### Тестирование на продакшене

```bash
API_BASE=https://ogfoody.ru npx tsx scripts/test-loyalty-clean.ts
```

### Что делают тесты

1. **Автоматически создают пользователей** через API с именем, телефоном, улицей и номером дома
2. **Создают заказы** через API
3. **Проверяют начисление и использование баллов**
4. **Очищают данные** после каждого теста (удаляют заказы)

---

## 📝 Описание тестов

### Тест 1: Базовое начисление баллов

**Цель:** Проверить корректность начисления баллов за оплаченный заказ.

**Шаги:**
1. Создается новый пользователь через API:
   - Имя: "Тестовый Пользователь 1"
   - Телефон: уникальный (+7999...)
   - Адрес: "Тестовая улица, 1"
   - Район: "Центральный"
2. Проверяется начальный баланс (должен быть 0)
3. Создается заказ:
   - Сумма: 2000₽
   - Способ оплаты: карта
   - Статус: оплачен (`paid: true`, `paymentStatus: 'paid'`)
   - Использовано баллов: 0
4. Ожидается 5 секунд для обработки
5. Проверяется финальный баланс

**Ожидаемый результат:**
- Начислено: **60 баллов** (3% от 2000₽ для нового пользователя)
- Баланс: 0 → 60

**Формула расчета:**
```
earnedPoints = floor(orderTotal * 0.03)
earnedPoints = floor(2000 * 0.03) = 60
```

---

### Тест 2: Начисление баллов с промокодом

**Цель:** Проверить, что баллы начисляются на сумму заказа С УЧЕТОМ промокода.

**Шаги:**
1. Создается новый пользователь через API
2. Создается промокод:
   - Тип: фиксированная скидка
   - Значение: 200₽
   - Тип использования: безлимитный
3. Создается заказ:
   - Сумма без промокода: 2000₽
   - Промокод: -200₽
   - Итоговая сумма: 1800₽
   - Способ оплаты: карта
   - Статус: оплачен
4. Ожидается 5 секунд для обработки
5. Проверяется финальный баланс
6. Удаляется промокод

**Ожидаемый результат:**
- Начислено: **54 балла** (3% от 1800₽)
- Баланс: 0 → 54

**Формула расчета:**
```
orderTotal = 2000 - 200 = 1800
earnedPoints = floor(1800 * 0.03) = 54
```

**Критично:** Баллы начисляются на `orderTotal` (сумма ПОСЛЕ применения промокода), а не на `subtotal`.

---

### Тест 3: Использование баллов

**Цель:** Проверить корректность списания баллов при использовании их для оплаты заказа.

**Шаги:**
1. Создается новый пользователь через API
2. **Первый заказ** (начисление баллов):
   - Сумма: 2000₽
   - Способ оплаты: карта
   - Статус: оплачен
   - Использовано баллов: 0
   - Ожидаемый результат: начислено 60 баллов
3. Проверяется баланс после первого заказа (должен быть 60)
4. **Второй заказ** (использование баллов):
   - Сумма: 2000₽
   - Использовано баллов: 50
   - Итоговая сумма: 2000 - 50 = 1950₽
   - Способ оплаты: карта
   - Статус: оплачен
5. Ожидается 5 секунд для обработки
6. Проверяется финальный баланс

**Ожидаемый результат:**
- Баланс после первого заказа: 60
- Баланс после второго заказа: **70**
  - Начислено за второй заказ: 60 (3% от 2000)
  - Списано: 50
  - Итого: 60 + 60 - 50 = 70
- Фактически списано: **50 баллов**

**Формула расчета:**
```
balanceAfterFirstOrder = 60
earnedFromSecondOrder = floor(2000 * 0.03) = 60
pointsUsed = 50
finalBalance = 60 + 60 - 50 = 70
```

**Критично:** При использовании баллов они списываются, но за заказ все равно начисляются новые баллы на полную сумму заказа (до применения скидки по баллам).

---

## ✅ Ожидаемые результаты

### Успешный прогон всех тестов

```
🚀 ЗАПУСК ЧИСТЫХ ТЕСТОВ СИСТЕМЫ ЛОЯЛЬНОСТИ
============================================================
🌐 API: https://ogfoody.ru
📊 Режим: development

🧪 Тест 1: Базовое начисление баллов
   Создаем пользователя через API: Тестовый Пользователь 1 (+7999...)
   ✅ Пользователь создан: ID=129
   Начальный баланс: 0, total_spent: 0
   Финальный баланс: 60, начислено: 60
   Ожидалось: 60

🧪 Тест 2: Начисление баллов с промокодом
   Создаем пользователя через API: Тестовый Пользователь 2 (+7999...)
   ✅ Пользователь создан: ID=130
   Финальный баланс: 54, начислено: 54
   Ожидалось: 54 (3% от 1800)

🧪 Тест 3: Использование баллов
   Создаем пользователя через API: Тестовый Пользователь 3 (+7999...)
   ✅ Пользователь создан: ID=131
   Баланс после начисления: 60
   Баланс после использования: 70
   Ожидаемый баланс: 70 (60 + 60 - 50)
   Фактически списано баллов: 50

============================================================
📊 РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ

✅ 1. Тест 1: Базовое начисление баллов
   Баллы начислены правильно: 60

✅ 2. Тест 2: Начисление баллов с промокодом
   Баллы начислены правильно: 54

✅ 3. Тест 3: Использование баллов
   Баллы использованы правильно: списано 50, баланс 70

============================================================

📈 ИТОГО: 3 прошло, 0 провалено, 0 пропущено

✅ ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО!
```

---

## 🔧 Устранение проблем

### Проблема: Тест 1 проваливается (0 баллов вместо 60)

**Возможные причины:**
1. Заказ не помечен как оплаченный (`paid: false` или `paymentStatus !== 'paid'`)
2. Способ оплаты не "card" (для немедленного начисления)
3. `awardLoyaltyPoints` не вызывается
4. Транзакция создается со статусом "pending" вместо "completed"

**Решение:**
- Проверьте логи сервера: `pm2 logs ogfoody --lines 100`
- Ищите строки: `✅ Транзакция "earned" создана`, `💳 Пересчитанный баланс`
- Убедитесь, что `reloadedOrder.paid === true` и `reloadedOrder.payment_status === 'paid'`

---

### Проблема: Тест 2 проваливается (неправильное количество баллов)

**Возможные причины:**
1. Баллы начисляются на `subtotal` вместо `orderTotal`
2. Промокод не применяется корректно
3. `orderTotal` не учитывает промокод

**Решение:**
- Проверьте расчет `orderTotal` в логах
- Убедитесь, что `orderTotal = subtotal - promoDiscount`
- Проверьте, что `calculateEarnedPoints` получает правильный `orderTotal`

---

### Проблема: Тест 3 проваливается (баллы не списываются)

**Возможные причины:**
1. Транзакция "used" не создается
2. Транзакция создается с неправильным знаком (положительным вместо отрицательного)
3. Транзакция создается со статусом "pending" вместо "completed"
4. Баланс не пересчитывается из транзакций

**Решение:**
- Проверьте логи: должна быть строка `✅ СПИСАНИЕ: Создана транзакция на списание X баллов`
- Убедитесь, что транзакция имеет `points: -pointsUsed` (отрицательное значение)
- Проверьте, что `transaction_status === 'completed'`
- Проверьте, что `calculateUserBalance` учитывает транзакцию "used"

---

### Проблема: Отрицательный баланс

**Причина:**
- В старых версиях кода баланс мог быть отрицательным
- Теперь добавлена защита: `Math.max(0, balance)` в `calculateUserBalance`

**Решение:**
- Убедитесь, что в `lib/nocodb.ts` в функции `calculateUserBalance` есть:
  ```typescript
  const finalBalance = Math.max(0, balance)
  return finalBalance
  ```

---

### Проблема: Очистка данных не работает

**Причина:**
- Неправильный формат DELETE запроса к NocoDB API

**Решение:**
- Убедитесь, что в `cleanup-all-test-data.ts` используется формат:
  ```typescript
  body: JSON.stringify([{ Id: recordId }])
  ```
  (массив объектов с полем `Id`)

---

## 📚 Дополнительная информация

### Файлы тестов

- `scripts/test-loyalty-clean.ts` — основные тесты системы лояльности
- `scripts/cleanup-all-test-data.ts` — скрипт очистки данных
- `scripts/test-all-fixes.ts` — старые тесты (используются для других проверок)

### API endpoints

- `POST /api/db/Users/records` — создание пользователя
- `POST /api/orders` — создание заказа
- `DELETE /api/orders/[id]` — удаление заказа
- `POST /api/db/Promo_Codes/records` — создание промокода
- `DELETE /api/admin/promo/[id]` — удаление промокода

### Ключевые функции в коде

- `calculateUserBalance(userId, noCache)` — расчет баланса из транзакций
- `awardLoyaltyPoints(userId, orderTotal, pointsUsed, pointsEarned, orderId)` — начисление баллов
- `createPendingLoyaltyPoints(...)` — создание pending транзакции
- `refundLoyaltyPoints(...)` — возврат баллов при отмене заказа
- `calculateEarnedPoints(orderTotal, pointsUsed, totalSpent)` — расчет начисляемых баллов

### Уровни лояльности

- **Bronze** (0-19999₽): 3% кэшбек
- **Silver** (20000-49999₽): 5% кэшбек
- **Gold** (50000+₽): 7% кэшбек

### Правила начисления баллов

1. Баллы начисляются на **полную сумму заказа** (`orderTotal`)
2. Использованные баллы **не влияют** на расчет начисления
3. Промокоды **учитываются** в `orderTotal` (баллы начисляются на сумму после промокода)
4. Баллы начисляются **немедленно** при оплате картой (`paymentMethod === 'card'` и `paid === true`)
5. Баллы начисляются **отложенно** (pending) при оплате наличными

---

## ✅ Чеклист перед тестированием

- [ ] Переменные окружения настроены
- [ ] База данных очищена
- [ ] Изменения закоммичены и запушены
- [ ] Деплой на продакшн выполнен успешно
- [ ] API доступен (`curl https://ogfoody.ru/api/health`)
- [ ] Готовы к запуску тестов

---

## 📞 Контакты и поддержка

При возникновении проблем:
1. Проверьте логи сервера: `pm2 logs ogfoody`
2. Проверьте логи тестов (выводятся в консоль)
3. Убедитесь, что все зависимости установлены: `npm install`
4. Проверьте, что код задеплоен: `git log` на сервере

---

**Последнее обновление:** 2026-01-16  
**Версия документа:** 1.0
