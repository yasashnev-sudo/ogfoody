# Исправление: Два заказа на одну дату - 2026-01-13

## 🐛 Проблема (из баг-репорта):

**Отчет:** `2026-01-13T21-16-03-367Z_user-121_logs.txt`  
**User:** 121 (Mac, Chrome 143.0.0.0)

**Жалоба пользователя:**
> "два заказа на одну дату. Надо это исключить, на одну дату может быть только один заказ"

**Из логов:**
```javascript
📅 Список заказов в календаре: [
  {
    "id": 557,
    "orderNumber": "ORD-20260113-LFKSCJ",
    "startDate": "2026-01-15T21:00:00.000Z",  ← 15 января
    "orderStatus": "pending"
  },
  {
    "id": 558,
    "orderNumber": "ORD-20260113-4FXKYG",
    "startDate": "2026-01-15T21:00:00.000Z",  ← 15 января (ТА ЖЕ ДАТА!)
    "orderStatus": "pending"
  },
  ...
]
```

## 🔍 Причина:

Пользователь смог создать **два заказа на одну дату** (15 января 2026). Это нарушает бизнес-правило: **на одну дату может быть только один заказ**.

**Проблемы:**
1. ❌ **Нет валидации на бэкенде** - POST /api/orders не проверял существующий заказ на дату
2. ❌ **Нет валидации на фронтенде** - `handleSaveOrder` не проверял перед созданием
3. ❌ **Нет валидации при повторении** - `handleRepeatOrder` не проверял перед созданием черновика
4. ❌ **UI не блокирует** - можно выбрать дату с существующим заказом при повторении

## ✅ Решение:

### 1. Валидация на бэкенде (POST /api/orders)

```typescript:147:175:app/api/orders/route.ts
// ✅ ИСПРАВЛЕНО 2026-01-13: Проверяем, нет ли уже заказа на эту дату для этого пользователя
if (userId) {
  const orderStartDate = typeof order.startDate === "string" 
    ? order.startDate 
    : order.startDate.toISOString().split("T")[0]
  
  console.log(`🔍 Проверка существующего заказа на дату ${orderStartDate} для пользователя ${userId}...`)
  
  try {
    const existingOrders = await fetchOrdersByUser(userId)
    const existingOrderOnDate = existingOrders.find((o) => {
      const oDate = typeof o.start_date === 'string' 
        ? o.start_date 
        : (o["Start Date"] || o.start_date)?.split('T')[0]
      return oDate === orderStartDate && o.order_status !== 'cancelled'
    })
    
    if (existingOrderOnDate) {
      console.warn(`⚠️ ВАЛИДАЦИЯ: На дату ${orderStartDate} уже есть заказ`)
      return NextResponse.json({ 
        error: "Order already exists for this date",
        details: `На эту дату (${orderStartDate}) у вас уже есть заказ. Отмените существующий заказ или выберите другую дату.`,
        existingOrderId: existingOrderOnDate.Id,
        existingOrderNumber: existingOrderOnDate.order_number || existingOrderOnDate["Order Number"],
        date: orderStartDate
      }, { status: 400 })
    }
  } catch (error) {
    console.error(`❌ Ошибка при проверке существующего заказа:`, error)
  }
}
```

**Результат:**
- ✅ Бэкенд блокирует создание дубликата
- ✅ Возвращает понятную ошибку 400
- ✅ Исключает отмененные заказы из проверки

### 2. Валидация на фронтенде (handleSaveOrder)

```typescript:944:981:app/page.tsx
} else if (isAuthenticated && userProfile?.id) {
  // ✅ ИСПРАВЛЕНО 2026-01-13: Проверяем, нет ли уже заказа на эту дату
  const orderDate = typeof order.startDate === 'string' 
    ? order.startDate 
    : order.startDate.toISOString().split('T')[0]
  const existingOrderOnDate = orders.find((o) => {
    if (!o.id) return false // Черновики не учитываем
    const oDate = typeof o.startDate === 'string' 
      ? o.startDate 
      : o.startDate.toISOString().split('T')[0]
    return oDate === orderDate
  })
  
  if (existingOrderOnDate) {
    console.warn(`⚠️ На дату ${orderDate} уже есть заказ (ID: ${existingOrderOnDate.id})`)
    setWarningDialog({
      open: true,
      title: "Заказ уже существует",
      description: `На эту дату (${typeof order.startDate === 'string' ? new Date(order.startDate).toLocaleDateString('ru-RU') : order.startDate.toLocaleDateString('ru-RU')}) у вас уже есть заказ. Отмените существующий заказ или выберите другую дату.`,
      variant: "warning",
    })
    setShowOrderLoading(false)
    return
  }
  
  // ... создание заказа
  
  if (!response.ok) {
    const errorData = await response.json().catch(() => ({ error: "Unknown error" }))
    
    // ✅ ИСПРАВЛЕНО 2026-01-13: Показываем понятное сообщение, если заказ уже существует
    if (errorData.error === "Order already exists for this date" || errorData.details) {
      setWarningDialog({
        open: true,
        title: "Заказ уже существует",
        description: errorData.details || `На эту дату уже есть заказ. Отмените существующий заказ или выберите другую дату.`,
        variant: "warning",
      })
      setShowOrderLoading(false)
      return
    }
  }
}
```

**Результат:**
- ✅ Фронтенд проверяет перед отправкой запроса
- ✅ Показывает предупреждение, если заказ уже существует
- ✅ Обрабатывает ошибку от бэкенда

### 3. Валидация при повторении заказа (handleRepeatOrder)

```typescript:1603:1620:app/page.tsx
const handleRepeatOrder = async (order: Order, targetDate: Date) => {
  try {
    // ✅ ИСПРАВЛЕНО 2026-01-13: Проверяем, нет ли уже заказа на эту дату
    const targetDateKey = targetDate.toISOString().split('T')[0]
    const existingOrderOnDate = orders.find((o) => {
      if (!o.id) return false // Черновики не учитываем
      const oDate = typeof o.startDate === 'string' 
        ? o.startDate 
        : o.startDate.toISOString().split('T')[0]
      return oDate === targetDateKey
    })
    
    if (existingOrderOnDate) {
      console.warn(`⚠️ [Repeat Order] На дату ${targetDateKey} уже есть заказ`)
      setWarningDialog({
        open: true,
        title: "Заказ уже существует",
        description: `На эту дату (${targetDate.toLocaleDateString('ru-RU')}) у вас уже есть заказ. Отмените существующий заказ или выберите другую дату.`,
        variant: "warning",
      })
      return
    }
    
    // ... создание черновика
  }
}
```

**Результат:**
- ✅ Блокирует создание черновика на дату с существующим заказом
- ✅ Показывает предупреждение пользователю
- ✅ Не открывает OrderModal, если заказ уже существует

## 📊 Сценарии:

### Сценарий 1: Создание нового заказа на дату с существующим заказом
```
1. Пользователь выбирает дату в календаре
2. Открывается OrderModal
3. Пользователь заполняет заказ
4. Нажимает "Сохранить"
5. ✅ Фронтенд проверяет: заказ уже существует → предупреждение
6. ❌ Заказ НЕ создается
```

### Сценарий 2: Повторение заказа на дату с существующим заказом
```
1. Пользователь нажимает "Повторить заказ"
2. Выбирает дату из списка
3. ✅ handleRepeatOrder проверяет: заказ уже существует → предупреждение
4. ❌ Черновик НЕ создается, OrderModal НЕ открывается
```

### Сценарий 3: Обход фронтенда (прямой API запрос)
```
1. Прямой POST /api/orders с датой, на которую уже есть заказ
2. ✅ Бэкенд проверяет: заказ уже существует
3. ❌ Возвращает 400 с ошибкой
4. Заказ НЕ создается
```

## 🎯 Критичность:

- **Уровень:** HIGH (нарушение бизнес-правила)
- **Влияние:** Возможны дубликаты заказов на одну дату
- **Частота:** Редко, но критично

## 📁 Измененные файлы:

1. `app/api/orders/route.ts` - добавлена проверка на существующий заказ на дату (строки 147-175)
2. `app/page.tsx` - добавлена проверка в `handleSaveOrder` (строки 944-981)
3. `app/page.tsx` - добавлена проверка в `handleRepeatOrder` (строки 1603-1620)

## 🔗 Связанные проблемы:

Это **пятое исправление** в системе валидации заказов:
1. **Баг #1-13:** Различные UX и функциональные баги
2. **Баг #14:** Script error от Vercel Analytics
3. **Баг #15:** Два заказа на одну дату ← **ЭТОТ БАГ**

## 🧪 Тестирование:

**До исправления:**
- ❌ Можно создать два заказа на одну дату
- ❌ Нет проверки на бэкенде
- ❌ Нет проверки на фронтенде
- ❌ Можно повторить заказ на дату с существующим заказом

**После исправления:**
- ✅ Бэкенд блокирует дубликаты
- ✅ Фронтенд проверяет перед созданием
- ✅ Повторение заказа проверяет дату
- ✅ Понятные сообщения об ошибках

## 🚀 Деплой:

```bash
# Время: 2026-01-13T21:30:00Z
# Статус: Готов к деплою
```

---

**Итог:** Добавлена валидация на трех уровнях (бэкенд, фронтенд, повторение заказа), которая блокирует создание двух заказов на одну дату. Пользователи получают понятные предупреждения, если пытаются создать дубликат. 🎉
