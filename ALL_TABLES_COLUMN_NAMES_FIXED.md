# Исправление проблем с названиями колонок во всех таблицах

## ✅ Выполненные исправления

### Проблема
NocoDB API v2 возвращает данные с ключами как `title` колонок (с заглавными буквами и пробелами), а не `column_name` (snake_case). Это приводило к тому, что код не мог получить доступ к данным.

### Решение
Обновлены все интерфейсы TypeScript для поддержки обоих вариантов названий колонок:
- `column_name` (snake_case) - для совместимости
- `title` (с заглавными буквами и пробелами) - реальный формат из NocoDB API

## 📋 Обновленные интерфейсы

### 1. NocoDBMeal ✅
- Добавлена поддержка обоих вариантов для всех полей
- Пример: `name` и `Name`, `price_single` и `"Price (Single)"`

### 2. NocoDBExtra ✅
- Добавлена поддержка обоих вариантов для всех полей
- Пример: `name` и `Name`, `is_current_week` и `"Current Week"`

### 3. NocoDBDeliveryZone ✅
- Добавлена поддержка обоих вариантов для всех полей
- Пример: `city` и `City`, `is_available` и `Available`

### 4. NocoDBUser ✅
- Добавлена поддержка обоих вариантов для всех полей
- Пример: `phone` и `Phone`, `loyalty_points` и `"Loyalty Points"`

### 5. NocoDBOrder ✅
- Добавлена поддержка обоих вариантов для всех полей
- Пример: `order_number` и `"Order Number"`, `payment_status` и `"Payment Status"`

### 6. NocoDBOrderPerson ✅
- Добавлена поддержка обоих вариантов для всех полей
- Пример: `order_id` и `"Order ID"`, `person_number` и `"Person Number"`

### 7. NocoDBOrderMeal ✅
- Добавлена поддержка обоих вариантов для всех полей
- Пример: `order_person_id` и `"Order Person ID"`, `portion_size` и `"Portion Size"`

### 8. NocoDBOrderExtra ✅
- Добавлена поддержка обоих вариантов для всех полей
- Пример: `order_id` и `"Order ID"`, `extra_id` и `"Extra ID"`

### 9. NocoDBPromoCode ✅
- Добавлена поддержка обоих вариантов для всех полей
- Пример: `code` и `Code`, `discount_type` и `"Discount Type"`

### 10. NocoDBReview ✅
- Добавлена поддержка обоих вариантов для всех полей
- Пример: `order_id` и `"Order ID"`, `rating` и `Rating`

## 🔧 Обновленный код

### app/api/menu/route.ts
- ✅ Meals: уже поддерживает оба варианта (использует `(m as any)["Name"] ?? m.name`)
- ✅ Extras: обновлен для поддержки обоих вариантов
- ✅ Delivery_Zones: обновлен для поддержки обоих вариантов

### lib/nocodb.ts
- ✅ Все интерфейсы обновлены для поддержки обоих вариантов
- ✅ Функции `fetchUserByPhone` и `fetchUserById` уже используют правильные ключи

## 📝 Примечания

1. **Обратная совместимость**: Код продолжает работать со старыми данными (snake_case), если они есть
2. **Приоритет**: Сначала проверяется `title` (с заглавными), затем `column_name` (snake_case)
3. **Где используется**: В основном в `app/api/menu/route.ts` и функциях работы с данными в `lib/nocodb.ts`

## ✅ Результат

Теперь код корректно работает с данными из NocoDB API, независимо от того, какие ключи используются в ответе API.





