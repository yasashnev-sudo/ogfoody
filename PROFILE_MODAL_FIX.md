# ✅ ИСПРАВЛЕНО: ProfileModal теперь открывается автоматически

**Дата**: 2026-01-09  
**Статус**: ✅ ГОТОВО К ТЕСТИРОВАНИЮ

---

## Проблема

Пользователь создавал заказ как гость, авторизовывался, но **ProfileModal не открывался** автоматически для заполнения адреса. Заказ оформлялся без имени и адреса доставки.

---

## Причина

Проверка заполненности профиля (`isProfileComplete`) выполнялась в функции `handleLogin` **ДО** того, как `userProfile` обновлялся в состоянии. 

```typescript
// Старый код - НЕ РАБОТАЛ:
setUserProfile(profile)  // Асинхронное обновление состояния

const isProfileComplete = profile.name && profile.street && profile.building
// Проверка локальной переменной, а не состояния!
```

---

## Решение

### 1. Создан `useEffect` для проверки профиля

Проверка заполненности профиля теперь происходит в `useEffect`, который срабатывает **ПОСЛЕ** обновления `userProfile` в состоянии:

```typescript
useEffect(() => {
  if (isAuthenticated && userProfile && shouldAutoCheckout && pendingCheckout) {
    console.log("🔍 Проверяем заполненность профиля после авторизации")
    
    const isProfileComplete = userProfile.name && userProfile.street && userProfile.building
    
    if (!isProfileComplete) {
      console.log("⚠️ Профиль не заполнен, открываем ProfileModal")
      setShowProfile(true)
    } else {
      console.log("✅ Профиль заполнен, запускаем автооформление")
      handleAutoCheckout()
    }
  }
}, [isAuthenticated, userProfile, shouldAutoCheckout, pendingCheckout])
```

### 2. Обновлен ProfileModal

**Добавлен специальный режим для оформления заказа:**

- ✅ Prop `isCheckoutFlow` - флаг что это оформление заказа
- ✅ Кнопка закрытия (X) **скрывается** в режиме оформления
- ✅ Заголовок меняется на "Заполните данные для доставки"
- ✅ Добавлено информационное сообщение: "📦 Оформление заказа: Заполните имя и адрес..."
- ✅ Вкладка "Бонусы" **скрывается** в режиме оформления
- ✅ Кнопка "Сохранить" меняется на **"Оформить заказ"**
- ✅ Добавлена **валидация обязательных полей**: имя, улица, дом

**Валидация:**
```typescript
if (isCheckoutFlow) {
  if (!profile.name?.trim()) {
    alert("Пожалуйста, укажите ваше имя")
    return
  }
  if (!profile.street?.trim()) {
    alert("Пожалуйста, укажите улицу")
    return
  }
  if (!profile.building?.trim()) {
    alert("Пожалуйста, укажите номер дома")
    return
  }
}
```

### 3. Обновлена логика закрытия

Модалка профиля теперь закрывается:
- **После успешного автооформления** заказа
- **Сразу** при обычном сохранении (не в режиме оформления)

---

## Что изменилось

### Файл: `app/page.tsx`

1. **Добавлен useEffect** для проверки профиля после авторизации
2. **Убрана** старая проверка из `handleLogin` (теперь в useEffect)
3. **Убрана** старая проверка из `handleDistrictSelected` (теперь в useEffect)
4. **Обновлен** `handleProfileSave` - закрывает модалку после автооформления
5. **Обновлен** рендеринг ProfileModal - передается `isCheckoutFlow` и `userProfile`

### Файл: `components/profile-modal.tsx`

1. **Добавлен** prop `isCheckoutFlow`
2. **Изменен** заголовок в зависимости от режима
3. **Добавлено** информационное сообщение для оформления заказа
4. **Скрыта** кнопка закрытия (X) при оформлении
5. **Скрыты** вкладки (Профиль/Бонусы) при оформлении
6. **Изменена** кнопка "Сохранить" на "Оформить заказ"
7. **Добавлена** валидация обязательных полей

---

## Новый flow

### Для нового клиента (гостя):

```
1. Заходит на сайт → Модалка выбора района
2. Выбирает район → Сохраняется в localStorage (guest_district)
3. Создает заказ → Видит стоимость доставки
4. Нажимает "Заказать" → AuthModal

5. Вводит телефон → Код подтверждения
6. После успешного кода:
   - Создается профиль с районом из guest_district
   - setUserProfile(profile) ← Обновляется состояние
   
7. useEffect срабатывает:
   - Проверяет userProfile.name, userProfile.street, userProfile.building
   - Если НЕ заполнено → setShowProfile(true)
   
8. ✅ ProfileModal открывается:
   - Заголовок: "Заполните данные для доставки"
   - Сообщение: "📦 Оформление заказа..."
   - Район уже заполнен
   - Нельзя закрыть (нет кнопки X)
   - Только вкладка "Профиль"
   - Кнопка: "Оформить заказ"

9. Пользователь заполняет:
   - Имя: "Сергей"
   - Улица: "Невский проспект"
   - Дом: "1"
   - (Опционально: квартира, подъезд и т.д.)

10. Нажимает "Оформить заказ"
11. ✅ handleProfileSave вызывается
12. ✅ handleAutoCheckout выполняется
13. ✅ Заказ оформляется с полным адресом
14. ✅ PaymentModal открывается
15. ✅ ProfileModal закрывается
```

---

## 🧪 Тестирование

### Тест 1: Новый пользователь оформляет заказ

```bash
1. Откройте http://localhost:3000 в режиме Инкогнито
2. Выберите район "Адмиралтейский район"
3. Создайте заказ с блюдами
4. Нажмите "Заказать"
5. Введите новый номер: +7 (999) 777-88-99
6. Введите код из консоли

7. ✅ Автоматически должна открыться модалка профиля:
   - Заголовок: "Заполните данные для доставки"
   - Синий баннер: "📦 Оформление заказа..."
   - Нет кнопки X (нельзя закрыть)
   - Нет вкладки "Бонусы"
   - Район уже заполнен: "Адмиралтейский район"

8. Заполните:
   - Имя: "Тест Тестович"
   - Улица: "Невский проспект"
   - Дом: "10"

9. Нажмите "Оформить заказ"

10. ✅ Должно быть:
    - Уведомление: "Заказ оформлен!"
    - ProfileModal закрывается
    - PaymentModal открывается
    - Сумма включает доставку

11. ✅ В консоли должны быть логи:
    - "🔍 Проверяем заполненность профиля после авторизации"
    - "⚠️ Профиль не заполнен, открываем ProfileModal"
    - "🚀 После заполнения профиля оформляем заказ"
    - "🚀 Автооформление заказа после авторизации"
    - "💰 Стоимость доставки: 250₽ для района..."

12. ✅ Проверьте в NocoDB:
    - Users: новый пользователь с полным адресом
    - Orders: заказ с deliveryFee, deliveryDistrict, deliveryAddress
```

### Тест 2: Попытка сохранить без заполнения

```bash
1. Откройте ProfileModal в режиме оформления
2. Оставьте поле "Имя" пустым
3. Нажмите "Оформить заказ"
4. ✅ Должно быть alert: "Пожалуйста, укажите ваше имя"

5. Заполните имя, оставьте "Улицу" пустой
6. Нажмите "Оформить заказ"
7. ✅ Должно быть alert: "Пожалуйста, укажите улицу"

8. Заполните улицу, оставьте "Дом" пустым
9. Нажмите "Оформить заказ"
10. ✅ Должно быть alert: "Пожалуйста, укажите номер дома"
```

### Тест 3: Существующий пользователь

```bash
1. Войдите как существующий пользователь с заполненным профилем
2. Создайте заказ
3. Нажмите "Оформить"
4. ✅ ProfileModal НЕ должен открываться
5. ✅ Сразу должна открыться PaymentModal
```

---

## 🎯 Результат

✅ **ProfileModal теперь открывается автоматически** для новых пользователей  
✅ **Нельзя закрыть** модалку без заполнения обязательных полей  
✅ **Валидация** проверяет имя, улицу, дом  
✅ **Понятный интерфейс** с информацией о том, что это оформление заказа  
✅ **Автоматическое оформление** сразу после заполнения профиля  
✅ **Полный адрес** сохраняется в заказе  

---

## 📝 Дополнительные улучшения (на будущее)

1. **Подсказки адресов**: Интеграция с DaData для автодополнения
2. **Валидация района и улицы**: Проверка что улица действительно в выбранном районе
3. **Сохранение нескольких адресов**: Дом, Работа, Дача
4. **Телеграм-бот**: Уведомление о заказе с адресом доставки

---

**Протестируйте и убедитесь что всё работает!** 🚀



