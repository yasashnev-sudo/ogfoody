# ✅ Удалено поле `available` из таблицы Meals

## 📋 Что было сделано

### 1. Структура базы данных
- ❌ **Удалено** поле `available` из таблицы `Meals`
- ✅ **Оставлены** поля `is_current_week` и `is_next_week` для определения доступности

### 2. Логика доступности блюд

**Новая логика:**
- Доступность блюда определяется **только** через флаги недель:
  - Если `is_current_week = false` и `is_next_week = false` → блюдо **недоступно**
  - Если `is_current_week = true` → доступно в **текущей неделе**
  - Если `is_next_week = true` → доступно в **следующей неделе**
  - Если оба `true` → доступно в **обеих неделях**

**Старая логика (удалена):**
- Сначала проверялось поле `available`
- Затем проверялись флаги недель

### 3. Обновленные файлы

#### Структура таблиц
- ✅ `app/api/db/setup-tables/route.ts` - убрано поле `available` из определения таблицы Meals
- ✅ `app/api/db/check-structure/route.ts` - убрано поле `available` из схемы проверки

#### API и логика
- ✅ `app/api/menu/route.ts` - обновлена логика фильтрации:
  - Убрана проверка `available`
  - Добавлена проверка: если оба флага недель `false`, блюдо пропускается
  - Убрана переменная `skippedNotAvailable`

#### Интерфейсы и типы
- ✅ `lib/nocodb.ts` - убрано поле `available` из интерфейса `NocoDBMeal`
- ✅ `lib/types.ts` - убрано поле `available` из интерфейса `Meal`
- ✅ `lib/meals-data.ts` - обновлена функция `isMealAvailable()`:
  - Убрана проверка `meal.available`
  - Доступность определяется только через `weekType`

#### Компоненты
- ✅ `components/fresh-section.tsx` - убрана проверка `meal.available !== false`
- ✅ `components/meal-selector.tsx` - убраны проверки `available` для блюд и гарниров
- ✅ `components/order-modal.tsx` - убраны проверки `available` для блюд и гарниров

#### Документация
- ✅ `GOOGLE_SHEETS_FIELDS.md` - обновлена документация:
  - Убрано поле `available` из описания таблицы Meals
  - Добавлены пояснения о том, что доступность определяется через `is_current_week` и `is_next_week`
  - Обновлены примеры

## ⚠️ Важно

### Для Extras (топпингов)
- ✅ Поле `available` **остается** в таблице `Extras`
- Топпинги не привязаны к неделям, поэтому для них `available` используется для управления доступностью

### Для Delivery_Zones (районов)
- ✅ Поле `is_available` **остается** в таблице `Delivery_Zones`
- Районы не привязаны к неделям, поэтому для них `is_available` используется для управления доступностью

## 🔄 Что нужно сделать после изменений

### 1. Пересоздать таблицу Meals (если нужно)
Если таблица уже создана с полем `available`, можно:
- Удалить колонку `available` вручную через интерфейс NocoDB
- Или пересоздать таблицу через `/api/db/reset-tables` и `/api/db/setup-tables`

### 2. Обновить данные в Google Sheets
- Убрать колонку `available` из листа с блюдами
- Убедиться, что заполнены `is_current_week` и `is_next_week` для всех блюд

## ✅ Результат

Теперь доступность блюд определяется **только** через флаги недель:
- Проще управление - не нужно следить за двумя полями
- Логичнее - если блюдо не в меню недели, оно недоступно
- Меньше путаницы - один источник истины для доступности





