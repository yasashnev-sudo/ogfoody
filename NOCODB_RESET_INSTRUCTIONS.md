# Инструкция по пересозданию таблиц NocoDB

## ⚠️ ВНИМАНИЕ
Это удалит **ВСЕ данные** из всех таблиц в NocoDB! Используйте только для полной перестройки структуры базы данных.

## 📋 Что изменилось

### Таблица Orders
- ✅ **Добавлено**: `created_at` - дата и время создания заказа
- ✅ **Добавлено**: `updated_at` - дата и время обновления заказа
- ✅ **Добавлено**: `payment_status` - статус оплаты (pending, paid, refunded, failed)
- ✅ **Добавлено**: `payment_id` - ID платежа для онлайн оплат
- ✅ **Добавлено**: `order_status` - статус заказа (pending, confirmed, preparing, ready, cancelled)
- ❌ **Удалено**: `status` - старый общий статус
- ❌ **Удалено**: `delivered` - статус доставки (нет интеграции с логистикой)
- ❌ **Удалено**: `cancelled` - статус отмены (теперь часть order_status)

### Таблица Users
- ✅ **Добавлено**: `created_at` - дата регистрации пользователя
- ✅ **Добавлено**: `updated_at` - дата обновления профиля

### Таблицы Meals и Extras
- ✅ **Без изменений** - оставлены как есть

## 🚀 Порядок действий

### Шаг 1: Удалить все таблицы
```bash
curl -X POST http://localhost:3000/api/db/reset-tables
```

Или откройте в браузере:
```
http://localhost:3000/api/db/reset-tables
```

**Результат**: Все таблицы будут удалены из NocoDB.

### Шаг 2: Создать таблицы заново
```bash
curl -X POST http://localhost:3000/api/db/setup-tables
```

Или откройте в браузере:
```
http://localhost:3000/api/db/setup-tables
```

**Результат**: Все таблицы будут созданы с новой структурой. В ответе вы получите новые ID таблиц.

### Шаг 3: Обновить переменные окружения
Скопируйте новые ID таблиц из ответа и обновите файлы:
- `.env.local`
- `.idx/dev.nix` (если используется)

Пример:
```env
NOCODB_TABLE_ORDERS=новый_id_таблицы
NOCODB_TABLE_USERS=новый_id_таблицы
# и т.д.
```

### Шаг 4: Настроить связи между таблицами
Следуйте инструкции в файле `NOCODB_RELATIONSHIPS_SETUP_SIMPLE.md`

## ✅ Проверка

После выполнения всех шагов:
1. Проверьте структуру таблиц в NocoDB
2. Убедитесь, что в таблице Orders есть поля `created_at`, `updated_at`, `payment_status`, `order_status`
3. Убедитесь, что в таблице Users есть поля `created_at`, `updated_at`
4. Проверьте, что связи между таблицами настроены правильно

## 📝 Примечания

- Таблицы Meals и Extras остаются без изменений
- Все данные в таблицах будут удалены при выполнении reset-tables
- После пересоздания таблиц нужно будет заново настроить связи вручную через интерфейс NocoDB





