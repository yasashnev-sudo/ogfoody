# 🔔 Настройка Telegram уведомлений для Debug системы

## ✅ Данные уже готовы!

**Telegram Bot Token:**
```
8581334096:AAG2-h00EDRFj9kXiYCf5jpRw6WQjheTpUY
```

**Telegram Chat ID:**
```
163996864
```

**Бот:** [@myproject_debug_bot](https://t.me/myproject_debug_bot)

---

## 🚀 Быстрая настройка (2 минуты)

### Шаг 1: Создай файл `.env.local` в корне проекта

Создай файл `.env.local` (он уже в `.gitignore`, не будет закоммичен) и добавь:

```bash
# Telegram уведомления для Debug системы
TELEGRAM_BOT_TOKEN=8581334096:AAG2-h00EDRFj9kXiYCf5jpRw6WQjheTpUY
TELEGRAM_CHAT_ID=163996864
```

### Шаг 2: Запусти бота

1. Открой: https://t.me/myproject_debug_bot
2. Нажми **"Start"** (или напиши `/start`)
3. Готово! 🎉

### Шаг 3: Перезапусти приложение

```bash
# Останови dev сервер (Ctrl+C)
# Запусти заново
npm run dev
```

### Шаг 4: Протестируй

Открой Debug консоль (`Ctrl+Shift+D`), напиши комментарий и нажми "Send Report".

Через 1-2 секунды получишь уведомление в Telegram! 📱

---

## 📱 Пример уведомления в Telegram

```
🐞 Новый Debug Report

❌ Ошибка: Failed to award loyalty points
👤 Пользователь: test@example.com
📱 URL: https://yoursite.com/checkout
🕐 Время: 13.01.2026, 12:30:45

💬 Комментарий: Баллы не начислились после оплаты наличными

📂 Лог файл: 2026-01-13T12-30-45_user-95_logs.txt
📸 Скриншот: 2026-01-13T12-30-45_user-95_screenshot.png
```

---

## 🌍 Настройка для Production (на сервере)

### На VPS (SSH):

```bash
# 1. Подключись к серверу
ssh root@5.129.194.168

# 2. Перейди в папку проекта
cd /root/my-project

# 3. Отредактируй .env.local
nano .env.local

# 4. Добавь переменные:
TELEGRAM_BOT_TOKEN=8581334096:AAG2-h00EDRFj9kXiYCf5jpRw6WQjheTpUY
TELEGRAM_CHAT_ID=163996864

# 5. Сохрани (Ctrl+O, Enter, Ctrl+X)

# 6. Перезапусти приложение
pm2 restart my-project
# или
npm run build && pm2 restart all
```

---

## 🔒 Безопасность

- ✅ `.env.local` уже в `.gitignore` - не попадёт в Git
- ✅ Токен хранится только на сервере
- ✅ Только ты (Chat ID: 163996864) будешь получать уведомления

**⚠️ ВАЖНО:** Никому не показывай токен бота! С ним можно управлять ботом.

---

## 🧪 Тест без деплоя

Можешь протестировать локально:

```bash
# 1. Создай .env.local с токенами
# 2. Запусти dev сервер
npm run dev

# 3. Открой Debug консоль (Ctrl+Shift+D)
# 4. Напиши комментарий: "Тестирую Telegram уведомления"
# 5. Отправь отчёт
# 6. Проверь Telegram - должно прийти уведомление! 📲
```

---

## ❓ Troubleshooting

### Уведомления не приходят?

**1. Проверь, что бот запущен:**
- Открой https://t.me/myproject_debug_bot
- Нажми "Start"

**2. Проверь переменные окружения:**
```bash
# В терминале
echo $TELEGRAM_BOT_TOKEN
echo $TELEGRAM_CHAT_ID
```

**3. Проверь логи сервера:**
```bash
# В консоли разработчика или в логах PM2
pm2 logs
```

Должны быть сообщения:
- `[DEBUG] Telegram notification sent successfully` ✅
- Или ошибки, если что-то не так ❌

**4. Проверь, что .env.local загружается:**

В `app/api/debug/report/route.ts` временно добавь в начало функции POST:
```typescript
console.log('[DEBUG] TELEGRAM_BOT_TOKEN:', process.env.TELEGRAM_BOT_TOKEN ? 'SET' : 'NOT SET');
console.log('[DEBUG] TELEGRAM_CHAT_ID:', process.env.TELEGRAM_CHAT_ID ? 'SET' : 'NOT SET');
```

Если показывает `NOT SET` - значит `.env.local` не загружается.

### Решение: Перезапусти dev сервер!

---

## 📚 Дополнительно

### Как добавить других получателей?

Можешь добавить несколько Chat ID через запятую в `.env.local`:

```bash
TELEGRAM_CHAT_ID=163996864,987654321,123456789
```

Затем обнови `app/api/debug/report/route.ts`:

```typescript
const chatIds = process.env.TELEGRAM_CHAT_ID?.split(',') || [];

for (const chatId of chatIds) {
  await fetch(`https://api.telegram.org/bot${token}/sendMessage`, {
    // ... отправка для каждого chatId
  });
}
```

### Как получить Chat ID другого пользователя?

1. Попроси пользователя написать боту `/start`
2. Зайди на https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
3. Найди `chat.id` в ответе

---

## ✅ Checklist

- [ ] Создан файл `.env.local` с токенами
- [ ] Бот запущен (написали `/start`)
- [ ] Dev сервер перезапущен
- [ ] Протестировано локально
- [ ] Получено тестовое уведомление в Telegram
- [ ] Настроено на продакшен сервере (если нужно)

---

🎉 **Готово! Теперь ты будешь получать мгновенные уведомления о всех ошибках в Telegram!**

