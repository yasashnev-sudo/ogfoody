name: Run Tests

on:
  pull_request:
    branches: [ main, master ]
  push:
    branches: [ main, master ]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧪 Run linter
        run: npm run lint
        continue-on-error: true

      - name: 🏗️ Build project
        run: npm run build
        env:
          NODE_ENV: production
          # Используем тестовые переменные окружения
          NOCODB_URL: https://test.example.com
          NOCODB_TOKEN: test_token
          NOCODB_TABLE_MEALS: test_id

      - name: ✅ Tests completed
        run: echo "All tests passed!"

