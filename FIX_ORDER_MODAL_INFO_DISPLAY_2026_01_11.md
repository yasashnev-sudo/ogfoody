# ✅ ИСПРАВЛЕНО: Информация о заказе не отображалась в модальном окне

**Дата:** 2026-01-11  
**Проблема:** В модальном окне оплаченного заказа отсутствовала информация о сумме, доставке и начисленных баллах

---

## 🐛 Описание проблемы

### Что происходило:

По логу из `debug_reports/2026-01-11T10-30-08-309Z_user-95_logs.txt`:

**USER COMMENT:**
> "здесь в модалке должна быть информация о сумме заказа, стоимости доставки и тд"

При открытии **оплаченного заказа** в модальном окне показывалось:
- ✅ Статус "оплачен"
- ✅ Список блюд
- ❌ **Отсутствовала** информация о:
  - Сумме заказа (subtotal)
  - Стоимости доставки (deliveryFee)
  - Использованных баллах (loyaltyPointsUsed)
  - Итоговой сумме (total)
  - Начисленных баллах (loyaltyPointsEarned)

---

## 🔍 Причина

В компоненте `components/order-modal.tsx` **не было блока** для отображения финансовой информации о существующем заказе.

Модальное окно отображало только:
1. Заголовок с датой
2. Бейдж "оплачен" (если paid=true)
3. Список персон с блюдами
4. Кнопки действий

**Но нигде не показывалась** информация из `existingOrder.total`, `existingOrder.deliveryFee` и т.д.

---

## ✅ Решение

Добавлен новый блок **"Информация о заказе"** для существующих/оплаченных заказов.

### Файл: `components/order-modal.tsx`

#### 1. Добавлены импорты иконок

```typescript
import {
  // ... existing imports
  Receipt,  // ← НОВОЕ: для заголовка блока
  Coins     // ← НОВОЕ: для начисленных баллов
} from "lucide-react"
```

#### 2. Добавлен блок с информацией (строка ~923)

```tsx
{/* ✅ НОВОЕ 2026-01-11: Информация о заказе для оплаченных/существующих заказов */}
{existingOrder && (existingOrder.paid || existingOrder.total !== undefined) && (
  <div className="mb-4 p-4 bg-purple-50 dark:bg-purple-900/20 border-2 border-purple-200 dark:border-purple-800 rounded-lg shadow-sm">
    <h3 className="font-bold text-purple-900 dark:text-purple-300 mb-3 flex items-center gap-2">
      <Receipt className="w-5 h-5" />
      Информация о заказе
    </h3>
    <div className="space-y-2 text-sm">
      {/* Subtotal */}
      {existingOrder.subtotal !== undefined && (
        <div className="flex items-center justify-between">
          <span className="text-purple-700 dark:text-purple-400">Сумма заказа:</span>
          <span className="font-bold text-purple-900 dark:text-purple-300">
            {existingOrder.subtotal.toLocaleString()} ₽
          </span>
        </div>
      )}
      
      {/* Delivery Fee */}
      {existingOrder.deliveryFee !== undefined && existingOrder.deliveryFee !== null && (
        <div className="flex items-center justify-between">
          <span className="text-orange-700 dark:text-orange-400 flex items-center gap-1">
            <Truck className="w-4 h-4" />
            Доставка:
          </span>
          <span className="font-bold text-orange-700 dark:text-orange-400">
            {existingOrder.deliveryFee === 0 ? 'Бесплатно' : `+${existingOrder.deliveryFee.toLocaleString()} ₽`}
          </span>
        </div>
      )}
      
      {/* Loyalty Points Used */}
      {existingOrder.loyaltyPointsUsed && existingOrder.loyaltyPointsUsed > 0 && (
        <div className="flex items-center justify-between">
          <span className="text-purple-700 dark:text-purple-400">Списано баллов:</span>
          <span className="font-bold text-purple-700 dark:text-purple-400">
            -{existingOrder.loyaltyPointsUsed} ₽
          </span>
        </div>
      )}
      
      {/* Total */}
      {existingOrder.total !== undefined && (
        <div className="flex items-center justify-between pt-2 border-t border-purple-200 dark:border-purple-800">
          <span className="font-bold text-purple-900 dark:text-purple-300">ИТОГО:</span>
          <span className="font-black text-xl text-purple-900 dark:text-purple-300">
            {existingOrder.total.toLocaleString()} ₽
          </span>
        </div>
      )}
      
      {/* Loyalty Points Earned */}
      {existingOrder.loyaltyPointsEarned && existingOrder.loyaltyPointsEarned > 0 && (
        <div className="flex items-center justify-between pt-2 bg-purple-100 dark:bg-purple-900/30 -mx-4 -mb-4 px-4 py-2 rounded-b-lg mt-2">
          <span className="text-purple-700 dark:text-purple-400 flex items-center gap-1">
            <Coins className="w-4 h-4" />
            Начислено баллов:
          </span>
          <span className="font-black text-purple-900 dark:text-purple-300">
            +{existingOrder.loyaltyPointsEarned}
          </span>
        </div>
      )}
    </div>
  </div>
)}
```

---

## 🎯 Как это работает

### Условие отображения:

```typescript
existingOrder && (existingOrder.paid || existingOrder.total !== undefined)
```

Блок показывается если:
- ✅ Заказ существует (`existingOrder !== null`)
- ✅ И хотя бы одно из условий:
  - Заказ оплачен (`paid = true`)
  - Или есть итоговая сумма (`total !== undefined`)

### Адаптивность полей:

Каждое поле показывается **только если данные есть**:
- `subtotal !== undefined` → показываем сумму заказа
- `deliveryFee !== undefined && deliveryFee !== null` → показываем доставку
- `loyaltyPointsUsed > 0` → показываем использованные баллы
- `total !== undefined` → показываем итого
- `loyaltyPointsEarned > 0` → показываем начисленные баллы

---

## 🎨 Дизайн

### Стилизация блока:
- Фон: `bg-purple-50` (светло-фиолетовый)
- Граница: `border-2 border-purple-200`
- Тень: `shadow-sm`
- Отступы: `p-4 mb-4`

### Цветовая схема:
| Элемент | Цвет | Иконка |
|---------|------|--------|
| Заголовок | `text-purple-900` | 🧾 Receipt |
| Сумма заказа | `text-purple-700` | - |
| Доставка | `text-orange-700` | 🚚 Truck |
| Списано баллов | `text-purple-700` | - |
| Итого | `text-purple-900` (жирный, 1.25x) | - |
| Начислено баллов | `text-purple-900` (на цветном фоне) | 🪙 Coins |

### Dark mode:
- Все цвета адаптированы: `dark:text-purple-300`, `dark:bg-purple-900/20`

---

## 📊 Пример отображения

### Для заказа из лога (Order #505):

```
┌─────────────────────────────────────────┐
│ 🧾 Информация о заказе                  │
├─────────────────────────────────────────┤
│ Сумма заказа:                  3,019 ₽  │
│ 🚚 Доставка:                  Бесплатно  │
│ ─────────────────────────────────────── │
│ ИТОГО:                         3,019 ₽  │
│                                         │
│ 🪙 Начислено баллов:              +90   │
└─────────────────────────────────────────┘
```

### Если были использованы баллы:

```
┌─────────────────────────────────────────┐
│ 🧾 Информация о заказе                  │
├─────────────────────────────────────────┤
│ Сумма заказа:                  3,019 ₽  │
│ 🚚 Доставка:                     +350 ₽  │
│ Списано баллов:                  -200 ₽  │
│ ─────────────────────────────────────── │
│ ИТОГО:                         3,169 ₽  │
│                                         │
│ 🪙 Начислено баллов:              +95   │
└─────────────────────────────────────────┘
```

---

## 🧪 Тестирование

### Сценарий 1: Оплаченный заказ с доставкой

1. Откройте календарь заказов
2. Кликните на **оплаченный** заказ (зеленый бейдж)
3. **Ожидается:**
   - ✅ Бейдж "оплачен" вверху
   - ✅ Блок "Информация о заказе"
   - ✅ Сумма заказа
   - ✅ Доставка (если != 0)
   - ✅ Итого
   - ✅ Начисленные баллы

### Сценарий 2: Неоплаченный заказ

1. Откройте неоплаченный заказ
2. **Ожидается:**
   - ✅ Блок "Информация о заказе" **показывается** (если total определен)
   - ✅ Кнопка "Оплатить заказ" внизу

### Сценарий 3: Заказ без доставки

1. Откройте заказ с `deliveryFee = 0`
2. **Ожидается:**
   - ✅ Строка "Доставка: Бесплатно"

### Сценарий 4: Заказ с использованными баллами

1. Откройте заказ с `loyaltyPointsUsed > 0`
2. **Ожидается:**
   - ✅ Строка "Списано баллов: -XXX ₽"

---

## 📋 Связанные изменения

### Из лога также видно, что:

**✅ Начисление баллов работает:**
```
🎁 Расчет начисленных баллов: {
  "oldLoyaltyPoints": 95,
  "newLoyaltyPoints": 185,
  "pointsDifference": 90,     ← Наше исправление из предыдущего фикса!
  "используем": 90
}
```

Это значит что **оба фикса** работают вместе:
1. ✅ Уведомление о баллах показывается (`FIX_LOYALTY_NOTIFICATION_2026_01_11.md`)
2. ✅ Информация о заказе отображается в модальном окне (этот фикс)

---

## 🔗 Связанные файлы

- `components/order-modal.tsx` (строки 6-33, 923-993)
- `lib/types.ts` (Order interface)

---

## 🎓 Ключевые уроки

1. **UI должен отражать все данные** - если API возвращает total/deliveryFee, они должны быть видны пользователю
2. **Адаптивность** - показываем только те поля, которые есть в заказе
3. **Консистентность** - используем ту же цветовую схему (фиолетовый для баллов, оранжевый для доставки)
4. **Dark mode** - всегда добавляем `dark:` варианты стилей

---

## ✅ Статус

**Готово к использованию!**

Теперь при открытии оплаченного заказа пользователь видит полную информацию о:
- ✅ Сумме заказа
- ✅ Стоимости доставки
- ✅ Использованных баллах
- ✅ Итоговой сумме
- ✅ Начисленных баллах

**Проблема решена!** 🎉


