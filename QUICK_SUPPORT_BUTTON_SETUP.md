# 🚀 Быстрая настройка кнопки поддержки

## ✅ Что уже сделано

Для заказов на **сегодняшний день** вместо кнопки "Отменить заказ" теперь показывается кнопка "Связаться с поддержкой", которая открывает WhatsApp с предзаполненным сообщением.

## 📱 Как это работает

1. **Заказ на сегодня** → Кнопка "ПОДДЕРЖКА" (зелёная)
2. **Заказ на завтра и позже** → Кнопка "ОТМЕНИТЬ" (серая)
3. При клике на "ПОДДЕРЖКА" → Открывается WhatsApp с текстом: "У меня вопрос по заказу №..."

## 🔧 Настройка номера телефона

**Текущий номер**: +7 (495) 123-45-67

### Если нужно изменить номер:

1. Откройте файл `components/order-history.tsx`
2. Найдите строку:
   ```typescript
   const whatsappUrl = `https://wa.me/74951234567?text=${encodeURIComponent(message)}`
   ```
3. Замените `74951234567` на ваш номер

4. Откройте файл `components/order-modal.tsx`
5. Найдите такую же строку и замените номер

### Формат номера

- Без пробелов, скобок и дефисов
- Начинается с кода страны
- Примеры:
  - +7 (999) 123-45-67 → `79991234567`
  - +7 (812) 456-78-90 → `78124567890`

## 🎨 Стилизация кнопки

Кнопка имеет зелёный цвет для обозначения связи с поддержкой:
- Цвет: `bg-green-500` (можно изменить на `bg-blue-500`, `bg-purple-500` и т.д.)
- Иконка: `MessageCircle` (чат)
- Текст: "ПОДДЕРЖКА" в истории, "Связаться с поддержкой" в модальном окне

## 📋 Файлы изменений

- ✅ `components/order-history.tsx` - история заказов
- ✅ `components/order-modal.tsx` - модальное окно заказа
- ✅ `app/api/orders/[id]/route.ts` - API защита от отмены
- 📄 `CANCEL_ORDER_TODAY_RESTRICTION_2026_01_13.md` - полная документация

## 🧪 Быстрый тест

```bash
# 1. Запустите проект локально
npm run dev

# 2. Создайте заказ на сегодня

# 3. Проверьте, что вместо кнопки "Отменить" есть кнопка "Поддержка"

# 4. Нажмите на неё - должен открыться WhatsApp
```

## ❓ Часто задаваемые вопросы

**В: Можно ли использовать Telegram вместо WhatsApp?**  
О: Да, замените URL на `https://t.me/{username}?text=...`

**В: Как добавить email вместо WhatsApp?**  
О: Замените `window.open(whatsappUrl, '_blank')` на `window.location.href = 'mailto:support@example.com?subject=...'`

**В: Можно ли показывать обе кнопки?**  
О: Да, измените условие с `? ... : ...` на отображение обеих кнопок

## 📞 Интеграция с другими мессенджерами

### Telegram
```typescript
const telegramUrl = `https://t.me/your_support_bot?start=${encodeURIComponent(message)}`
```

### Email
```typescript
const subject = `Вопрос по заказу ${orderNumber}`
const emailUrl = `mailto:support@example.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(message)}`
```

### Viber
```typescript
const viberUrl = `viber://chat?number=%2B79991234567&text=${encodeURIComponent(message)}`
```

