# ПРАВИЛА РАЗДЕЛЕНИЯ ОТВЕТСТВЕННОСТИ МОДАЛЬНЫХ ОКОН
**Дата:** 2026-01-11  
**Статус:** ✅ **ОБЯЗАТЕЛЬНЫЕ К СОБЛЮДЕНИЮ**

---

## OrderModal - Окно оформления заказа

### ✅ ЧТО ДОЛЖНО БЫТЬ:
1. **Выбор блюд** (завтрак, обед, ужин) для каждого дня
2. **Управление персонами** (добавить/удалить человека)
3. **Выбор дополнительных позиций** (extras)
4. **Выбор времени доставки** (подгружается по району через API)
5. **Применение промокода** (если есть)
6. **Кнопка "Продолжить"** для перехода к оплате

### ❌ ЧТО НЕ ДОЛЖНО БЫТЬ:
1. ❌ **Способ оплаты** (карта/СБП/наличные)
2. ❌ **Баллы лояльности** (выбор использования баллов)
3. ❌ **Кнопка "Оплатить"**

### 📋 Ответственность:
- Формирование состава заказа (блюда + extras)
- Выбор времени доставки
- Расчет subtotal (стоимость блюд + extras + промокод)
- Передача Order объекта в `onRequestAuth` или `onSave`

---

## PaymentModal - Окно выбора способа оплаты

### ✅ ЧТО ДОЛЖНО БЫТЬ:
1. **Выбор способа оплаты:**
   - Банковская карта
   - СБП (Система быстрых платежей)
   - Наличные при получении (ТОЛЬКО для новых заказов, `allowCash={true}`)
2. **Баллы лояльности:**
   - Переключатель использования баллов
   - Ввод количества баллов для списания
   - Кнопка "Списать все"
   - Отображение доступных баллов
3. **Применение промокода** (опционально, если еще не применен)
4. **Итоговая сумма** с учетом всех скидок
5. **Кнопка оплаты:**
   - "Оплатить — {сумма} ₽" (для карты/СБП)
   - "Подтвердить заказ — {сумма} ₽" (для наличных)

### ❌ ЧТО НЕ ДОЛЖНО БЫТЬ:
1. ❌ **Выбор блюд**
2. ❌ **Выбор времени доставки**
3. ❌ **Редактирование состава заказа**

### 📋 Ответственность:
- Выбор способа оплаты
- Управление баллами лояльности
- Расчет финальной суммы (total = subtotal + deliveryFee - баллы)
- Вызов `onPaymentComplete` с выбранным способом оплаты и количеством использованных баллов

### 🔧 Параметр `allowCash`:
```typescript
allowCash={paymentOrder.isNewOrder ?? true}
```
- `true` - для **новых** заказов (можно выбрать наличные)
- `false` - для **существующих** неоплаченных заказов (только карта/СБП для смены способа оплаты)

---

## ProfileModal - Окно редактирования профиля

### ✅ ЧТО ДОЛЖНО БЫТЬ:
1. **Обязательные поля** (помечены `*`):
   - Имя
   - Район доставки
   - Улица
   - Дом
2. **Необязательные поля:**
   - Корпус/строение
   - Квартира
   - Подъезд
   - Этаж
   - Домофон
   - Дополнительный телефон
   - Комментарий к доставке

### 📋 Ответственность:
- Сбор полных данных пользователя
- Валидация обязательных полей
- Сохранение профиля в БД
- Передача обновленного профиля в `onSave`

---

## DistrictSelectionModal - Окно выбора района

### ✅ ЧТО ДОЛЖНО БЫТЬ:
1. **Список районов** с:
   - Названием района
   - Стоимостью доставки (если платная)
   - "Бесплатно" (если бесплатная)
   - Минимальной суммой заказа
2. **Кнопка "Выбрать"** для каждого района

### 📋 Ответственность:
- Отображение доступных зон доставки
- Передача выбранного района в `onSelect`

---

## AuthModal - Окно авторизации

### ✅ ЧТО ДОЛЖНО БЫТЬ:
1. **Ввод номера телефона**
2. **Ввод кода подтверждения**
3. **Демо-режим** (автоматический код для тестирования)

### 📋 Ответственность:
- Аутентификация пользователя по телефону
- Создание нового пользователя если не существует
- Передача успешной авторизации в `onLogin`

---

## ФЛОУ ОФОРМЛЕНИЯ ЗАКАЗА

### 1️⃣ Новый заказ (авторизованный пользователь с полным профилем):
```
OrderModal → handlePayAndOrder → onRequestAuth → 
→ handleAutoCheckout → создание заказа в БД → 
→ PaymentModal (с isNewOrder=true, allowCash=true) → 
→ выбор способа оплаты → onPaymentComplete → 
→ обновление заказа в БД → SuccessDialog
```

### 2️⃣ Новый заказ (авторизованный пользователь БЕЗ полного профиля):
```
OrderModal → handlePayAndOrder → onRequestAuth → 
→ ProfileModal → заполнение профиля → 
→ handleAutoCheckout → создание заказа в БД → 
→ PaymentModal (с isNewOrder=true, allowCash=true) → 
→ выбор способа оплаты → onPaymentComplete → 
→ обновление заказа в БД → SuccessDialog
```

### 3️⃣ Новый заказ (гость):
```
OrderModal → handlePayAndOrder → onRequestAuth → 
→ DistrictSelectionModal → выбор района → 
→ AuthModal → авторизация → 
→ ProfileModal → заполнение профиля → 
→ handleAutoCheckout → создание заказа в БД → 
→ PaymentModal (с isNewOrder=true, allowCash=true) → 
→ выбор способа оплаты → onPaymentComplete → 
→ обновление заказа в БД → SuccessDialog
```

### 4️⃣ Оплата существующего неоплаченного заказа:
```
Calendar → клик на заказ → OrderModal (существующий заказ) → 
→ кнопка "Оплатить заказ" → onRequestPayment → 
→ PaymentModal (с isNewOrder=false, allowCash=false) → 
→ выбор карты/СБП → onPaymentComplete → 
→ обновление заказа в БД → SuccessDialog
```

---

## ВАЖНЫЕ ФЛАГИ

### `isNewOrder` в `paymentOrder`:
```typescript
{
  order: Order,
  total: number,
  isNewOrder?: boolean // true = созданный через автооформление, false = существующий
}
```

**Использование:**
- `isNewOrder: true` → при закрытии PaymentModal **удаляется** заказ
- `isNewOrder: false` или `undefined` → при закрытии PaymentModal заказ **остается**
- `allowCash={isNewOrder ?? true}` → разрешить выбор наличных только для новых заказов

---

## ПРАВИЛО ЗОЛОТОЕ:

🔥 **НИКОГДА не смешивайте ответственность между модальными окнами!**

- OrderModal = ЧТО заказать
- PaymentModal = КАК оплатить
- ProfileModal = КТО заказывает
- DistrictSelectionModal = КУДА доставить
- AuthModal = ПРОВЕРИТЬ, что это ты

Каждое окно делает **ОДНО** дело и делает его **ХОРОШО**.

---

**Последнее обновление:** 2026-01-11 после удаления баллов из OrderModal


