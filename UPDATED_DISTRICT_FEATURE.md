# ✅ ОБНОВЛЕНО: Выбор района для неавторизованных пользователей

**Дата**: 2026-01-09  
**Статус**: ✅ ГОТОВО К ТЕСТИРОВАНИЮ

---

## 🎯 Обновленная логика (Вариант Б)

### Для неавторизованных пользователей (гостей)

1. **Первый заход на сайт**:
   - ✅ Сразу показывается модалка выбора района
   - ✅ Текст: "Добро пожаловать! 👋"
   - ✅ "Мы работаем в Санкт-Петербурге"
   - ✅ Пользователь ДОЛЖЕН выбрать район (модалка не закрывается без выбора)

2. **После выбора района**:
   - ✅ Район сохраняется в localStorage как `guest_district`
   - ✅ Устанавливается флаг `district_modal_seen`
   - ✅ Показывается календарь
   - ✅ При повторном заходе модалка не показывается

3. **При авторизации**:
   - ✅ Гостевой район автоматически переносится в профиль пользователя
   - ✅ Сохраняется в базу данных NocoDB
   - ✅ Удаляется из гостевого хранилища

### Для авторизованных пользователей

1. **Пользователь С районом**:
   - ✅ Модалка НЕ показывается
   - ✅ Сразу показывается календарь

2. **Пользователь БЕЗ района**:
   - ✅ После входа показывается модалка выбора района
   - ✅ Текст: "Приятно познакомиться, {Имя}! 👋"
   - ✅ После выбора район сохраняется в профиль и базу данных

---

## 🧪 Тестирование

### Тест 1: Новый гость (первый заход)

```bash
# Шаги:
1. Очистить localStorage (DevTools → Application → Clear All)
2. Открыть http://localhost:3000 в режиме Инкогнито
3. ✅ СРАЗУ должна показаться модалка выбора района
4. ✅ Должен быть текст "Добро пожаловать! 👋"
5. ✅ Должен быть текст "Мы работаем в Санкт-Петербурге"
6. Выбрать любой район (например, "Адмиралтейский район")
7. Нажать "Продолжить"
8. ✅ Модалка должна закрыться
9. ✅ Должен показаться календарь
10. ✅ Должно появиться уведомление "Район ... сохранен"

# Проверка localStorage:
DevTools → Application → Local Storage → должны быть:
- guest_district: "Адмиралтейский район"
- district_modal_seen: "true"
```

### Тест 2: Гость повторно (модалка не должна показываться)

```bash
# Шаги:
1. НЕ очищать localStorage (после Теста 1)
2. Обновить страницу (F5)
3. ✅ Модалка НЕ должна показаться
4. ✅ Сразу должен показаться календарь
```

### Тест 3: Гость авторизуется (район переносится в профиль)

```bash
# Шаги:
1. После Теста 1 или 2 (guest_district уже выбран)
2. Нажать "Войти"
3. Ввести НОВЫЙ номер: +7 (999) 888-77-66
4. Нажать "СМС-код"
5. Скопировать код из консоли браузера
6. Ввести код и подтвердить
7. ✅ Модалка выбора района НЕ должна показаться
8. ✅ Сразу должен показаться календарь
9. ✅ Район из guest_district должен перенестись в профиль

# Проверка localStorage:
- guest_district: УДАЛЕН
- district_modal_seen: УДАЛЕН
- profile_79998887766: должен содержать district: "Адмиралтейский район"

# Проверка в NocoDB:
Таблица Users → найти пользователя 79998887766
→ District должен быть "Адмиралтейский район"
```

### Тест 4: Авторизованный пользователь БЕЗ района

```bash
# Подготовка:
1. В NocoDB → таблица Users → создать пользователя:
   - Phone: 79991112233
   - Name: "Тестовый"
   - District: ПУСТО или NULL

# Шаги:
1. Очистить localStorage
2. Открыть сайт
3. Нажать "Войти"
4. Ввести +7 (999) 111-22-33
5. Подтвердить код
6. ✅ Должна показаться модалка выбора района
7. ✅ Должен быть текст "Приятно познакомиться, Тестовый! 👋"
8. Выбрать район
9. ✅ Район должен сохраниться в базу данных
```

### Тест 5: Авторизованный пользователь С районом

```bash
# Подготовка:
1. В NocoDB → таблица Users → найти/создать пользователя:
   - Phone: 79995554433
   - District: "Центральный район"

# Шаги:
1. Очистить localStorage
2. Открыть сайт
3. Нажать "Войти"
4. Ввести +7 (999) 555-44-33
5. Подтвердить код
6. ✅ Модалка НЕ должна показаться
7. ✅ Сразу должен показаться календарь
```

---

## 📊 Что изменилось

### Новые localStorage ключи

1. **`guest_district`** - район, выбранный неавторизованным пользователем
2. **`district_modal_seen`** - флаг, что модалка уже была показана гостю

### Изменения в коде

#### `app/page.tsx`

1. **Добавлен useEffect для проверки гостя**:
```typescript
useEffect(() => {
  const isAuth = localStorage.getItem("currentUser")
  const guestDistrict = localStorage.getItem("guest_district")
  const hasSeenDistrictModal = localStorage.getItem("district_modal_seen")
  
  if (!isAuth && !guestDistrict && !hasSeenDistrictModal) {
    setShowDistrictModal(true)
  }
}, [])
```

2. **Обновлен `handleDistrictSelected`**:
   - Теперь работает как для авторизованных, так и для гостей
   - Для гостей сохраняет в `guest_district`
   - Для авторизованных - в профиль и базу данных

3. **Обновлен `handleLogin`**:
   - Проверяет наличие `guest_district`
   - Переносит его в профиль при авторизации
   - Удаляет гостевые данные после переноса
   - Сохраняет район в базу данных при создании пользователя

#### `components/district-selection-modal.tsx`

1. **Добавлен prop `isGuest`**:
   - Показывает разное приветствие для гостей и авторизованных

---

## 🔄 Сценарии использования

### Сценарий A: Гость → Выбирает район → Остается гостем

```
1. Заход на сайт → Модалка района
2. Выбор района → Сохранение в localStorage
3. Использование сайта как гость
4. Повторный заход → Модалка НЕ показывается
```

### Сценарий B: Гость → Выбирает район → Авторизуется

```
1. Заход на сайт → Модалка района
2. Выбор района → Сохранение в localStorage (guest_district)
3. Авторизация → Перенос района в профиль
4. Удаление guest_district
5. Сохранение в NocoDB
```

### Сценарий C: Новый пользователь авторизуется БЕЗ выбора района

```
1. Заход на сайт → Модалка района
2. Пользователь НЕ выбирает район, а сразу нажимает "Войти"
3. Авторизация
4. Модалка района показывается снова (т.к. района нет)
5. Выбор района → Сохранение в профиль
```

### Сценарий D: Гость БЕЗ района → Авторизуется

```
1. Заход на сайт → Модалка района
2. Каким-то образом обходит модалку (F5 быстро, закрывает браузер и т.д.)
3. Авторизуется
4. Модалка района показывается (т.к. ни guest_district, ни района в профиле нет)
```

---

## ✅ Преимущества текущей реализации

1. **Не мешает гостям**: 
   - Модалка показывается только один раз при первом заходе
   - После выбора района больше не беспокоит

2. **Плавный переход гость → пользователь**:
   - Район автоматически переносится при авторизации
   - Не нужно выбирать повторно

3. **Гарантия наличия района**:
   - Система всегда знает район пользователя
   - Корректный расчет доставки

4. **Информативность**:
   - Четко указано, что работа ведется в СПб
   - Видна стоимость доставки для каждого района

---

## 🚀 Готово к тестированию

Сервер запущен на **http://localhost:3000**

**Быстрый тест**:
1. Откройте http://localhost:3000 в режиме Инкогнито
2. ✅ Должна сразу показаться модалка выбора района
3. Выберите район → Нажмите "Продолжить"
4. ✅ Должен показаться календарь

**Проверь и дай обратную связь!** 🎉



