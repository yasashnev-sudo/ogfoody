# Исправление: Кнопка "Отменить" в черновике заказа - 2026-01-13

## 🐛 Проблема (из баг-репорта):

> "Проверь лог повторного заказа, я его не стал сохранять. Вроде бы ок, только странно что в нем была кнопка «отменить заказ»"

**User:** 122 (iPhone, Safari)  
**Device:** iPhone (iOS 18.7)  
**Time:** 2026-01-13T16:15:14.824Z  
**Report ID:** `2026-01-13T16-15-14-909Z_user-122_logs.txt`

## ✅ Подтверждение исправления #10:

Пользователь подтвердил, что **основное исправление работает правильно**:
- Повторение заказа создает черновик ✅
- Закрытие без сохранения не добавляет заказ в историю ✅
- Draft Pattern работает как ожидается ✅

**Но обнаружена UX проблема:** В черновике заказа показывается кнопка "Отменить заказ", хотя заказа еще нет в базе данных.

## 🔍 Причина:

В компоненте `OrderModal` логика `canCancel` проверяла только:
1. Наличие `existingOrder`
2. Дату в будущем

Но **НЕ проверяла**, что заказ существует в базе (имеет `id`):

```typescript
// ❌ БЫЛО (строки 573-577):
const canCancel = !!(
  existingOrder &&
  orderStartDate &&
  orderStartDate.getTime() > today.getTime()
)
```

**Проблема:** Черновик тоже является `existingOrder` (передается через prop), но не имеет `id`. Поэтому кнопка "Отменить" показывалась для черновиков.

## ✅ Решение:

Добавлена проверка на наличие `id`:

```typescript:573:579:components/order-modal.tsx
// ✅ ИСПРАВЛЕНО 2026-01-13: Кнопка "Отменить" только для существующих заказов (с id)
const canCancel = !!(
  existingOrder &&
  existingOrder.id && // Черновики (без id) не могут быть отменены
  orderStartDate &&
  orderStartDate.getTime() > today.getTime()
)
```

## 📊 Логика отображения кнопки "Отменить":

### Когда кнопка ПОКАЗЫВАЕТСЯ:
1. ✅ Заказ существует (`existingOrder` не null)
2. ✅ Заказ имеет `id` (сохранен в базе)
3. ✅ Дата доставки в будущем (не сегодня и не прошлое)

### Когда кнопка СКРЫТА:
- ❌ Черновик заказа (нет `id`)
- ❌ Заказ на сегодня или прошлую дату
- ❌ Заказ не существует (новый заказ)

## 🎯 Сценарии:

### Сценарий 1: Новый заказ
```
1. Выбрать дату в календаре
2. Открыть OrderModal
3. ✅ Кнопки "Отменить" НЕТ (нет existingOrder)
```

### Сценарий 2: Повторить заказ (черновик)
```
1. Нажать "Повторить заказ"
2. Открыть OrderModal с черновиком
3. ✅ Кнопки "Отменить" НЕТ (existingOrder.id === undefined)
4. Можно сохранить или закрыть
```

### Сценарий 3: Редактировать существующий заказ
```
1. Открыть существующий заказ
2. OrderModal с existingOrder.id
3. ✅ Кнопка "Отменить" ЕСТЬ (если дата в будущем)
```

### Сценарий 4: Заказ на сегодня
```
1. Открыть заказ на сегодняшний день
2. OrderModal с existingOrder.id
3. ✅ Кнопки "Отменить" НЕТ (дата = сегодня)
4. Вместо кнопки "Отменить" показывается "Поддержка"
```

## 📁 Измененные файлы:

- `components/order-modal.tsx` (строки 573-579)

## 🧪 Тестирование:

**Черновик заказа:**
- ✅ Кнопки "Отменить" нет
- ✅ Есть только "Сохранить" и "Закрыть"

**Существующий заказ (будущая дата):**
- ✅ Кнопка "Отменить" есть
- ✅ Можно отменить заказ

**Существующий заказ (сегодня):**
- ✅ Кнопки "Отменить" нет
- ✅ Показывается "Поддержка"

## 💡 UX улучшение:

Теперь интерфейс более логичный:
- **Черновик** = можно сохранить или закрыть (отмена не нужна, т.к. ничего не сохранено)
- **Существующий заказ** = можно отменить (если дата позволяет)

Это соответствует ментальной модели пользователя:
- "Отменить" = удалить что-то существующее
- Черновик = временные данные, которые не нужно отменять

## 🚀 Деплой:

```bash
# Время: 2026-01-13T16:18:00Z
# Статус: Готов к деплою
```

---

**Итог:** UX исправлен. Кнопка "Отменить заказ" теперь показывается только для реально существующих заказов в базе данных, а не для черновиков. 🎉

