# 🔴 СРОЧНО: Товары не загружаются - РЕШЕНИЕ

## Проблема
API возвращает ошибку 503, товары не загружаются на сайт.

## Причина
Не установлены переменные окружения для подключения к базе данных NocoDB.

---

## ✅ БЫСТРОЕ РЕШЕНИЕ (2 минуты)

### Вариант 1: Автоматическая настройка (рекомендуется)

**⚠️ ВАЖНО:** Dev сервер запускается из worktree директории!

```bash
# СПОСОБ A: Из корня проекта (автоматически перейдет в worktree)
./setup-env-worktree.sh

# СПОСОБ B: Напрямую из worktree директории
cd /Users/sergejasasnev/.cursor/worktrees/my-project__1_/zsx
./setup-env.sh

# Введите ваш NocoDB токен когда попросит
# (Токен можно найти в: https://noco.povarnakolesah.ru → Settings → API Tokens)

# Перезапустите dev сервер
# Нажмите Ctrl+C в терминале где запущен npm run dev
# Затем снова:
npm run dev
```

### Вариант 2: Ручная настройка

**⚠️ ВАЖНО:** Файл .env.local нужно создать в worktree директории!

```bash
# Перейдите в worktree директорию
cd /Users/sergejasasnev/.cursor/worktrees/my-project__1_/zsx

# 1. Скопируйте шаблон
cp .env.local.example .env.local

# 2. Откройте файл в редакторе
nano .env.local
# или
code .env.local

# 3. Замените YOUR_NOCODB_TOKEN_HERE на ваш реальный токен
#    (Найти токен: https://noco.povarnakolesah.ru → Settings → API Tokens)

# 4. Сохраните файл (Ctrl+O, Enter, Ctrl+X в nano)

# 5. Перезапустите dev сервер
# Ctrl+C, затем:
npm run dev
```

---

## 📋 Где взять токен NocoDB?

1. Откройте: https://noco.povarnakolesah.ru
2. Войдите в систему
3. Нажмите на иконку настроек (⚙️) в правом верхнем углу
4. Выберите **Account Settings** → **Tokens**
5. Создайте новый токен или скопируйте существующий
6. Скопируйте токен в буфер обмена

---

## 🔍 Проверка результата

### 1. После перезапуска dev сервера

В терминале должно появиться:

```
[MENU API] Environment check:
  - NOCODB_URL: https://noco.povarnakolesah... ✅
  - NOCODB_TOKEN: ********** ✅
  - NOCODB_TABLE_MEALS: mmtctn4fls... ✅
```

### 2. В браузере

Откройте http://localhost:3000 - товары должны загрузиться!

### 3. Диагностика

```bash
# В браузере откройте:
http://localhost:3000/api/diagnose

# Или в терминале:
curl http://localhost:3000/api/diagnose
```

Должен вернуть:
```json
{
  "summary": {
    "configured": true,
    "issues": []
  }
}
```

---

## ⚠️ Типичные ошибки

### 1. "Permission denied: ./setup-env.sh"

```bash
chmod +x setup-env.sh
./setup-env.sh
```

### 2. Товары всё равно не загружаются

```bash
# Проверьте, что файл .env.local создан В WORKTREE ДИРЕКТОРИИ
ls -la /Users/sergejasasnev/.cursor/worktrees/my-project__1_/zsx/.env.local

# Проверьте содержимое (токен должен быть заполнен)
cat /Users/sergejasasnev/.cursor/worktrees/my-project__1_/zsx/.env.local | grep NOCODB_TOKEN

# Обязательно перезапустите dev сервер!
# Ctrl+C
npm run dev
```

### 3. "NocoDB not configured" в логах

```bash
# ВАЖНО: Файл .env.local должен быть в WORKTREE директории!
# Проверьте правильную директорию:
cd /Users/sergejasasnev/.cursor/worktrees/my-project__1_/zsx

# Проверьте, что .env.local находится здесь
ls -la .env.local  # Файл должен существовать

# Проверьте, что токен правильный (без пробелов)
cat .env.local

# Если файла нет - запустите setup-env.sh из этой директории
./setup-env.sh
```

---

## 📝 Что содержит .env.local

Все ID таблиц уже настроены, нужно только добавить токен:

```env
NOCODB_URL=https://noco.povarnakolesah.ru
NOCODB_TOKEN=<ваш токен>

# ID таблиц (уже настроены)
NOCODB_TABLE_MEALS=mmtctn4flssh2ua
NOCODB_TABLE_EXTRAS=mksy21hmttmo855
NOCODB_TABLE_DELIVERY_ZONES=mpoppulqhsz1der
NOCODB_TABLE_USERS=mvrp4r9o3z69c45
NOCODB_TABLE_ORDERS=meddiicl0gr0r8y
NOCODB_TABLE_ORDER_PERSONS=mvr08d33zm5i8oi
NOCODB_TABLE_ORDER_MEALS=mz9uw5by177ygug
NOCODB_TABLE_ORDER_EXTRAS=mksy21hmttmo855
NOCODB_TABLE_PROMO_CODES=mgov8ce836696fy
NOCODB_TABLE_REVIEWS=mv8c69ib9muz9ki
```

---

## 🎉 Готово!

После настройки:
- ✅ API `/api/menu` вернёт данные о товарах
- ✅ Товары будут отображаться на сайте
- ✅ Все функции работы с БД заработают
- ✅ Можно работать с заказами, пользователями и т.д.

---

## 💡 Дополнительная информация

### Безопасность
- `.env.local` уже добавлен в `.gitignore`
- Токен не попадёт в Git репозиторий
- Не делитесь токеном публично!

### Для production (Vercel)
Переменные окружения для production настраиваются отдельно в Vercel Dashboard:
- Vercel Dashboard → Ваш проект → Settings → Environment Variables
- Подробности в файле: `VERCEL_SETUP.md`

---

## 📞 Нужна помощь?

1. ✅ Проверьте логи dev сервера (терминал где запущен `npm run dev`)
2. ✅ Откройте `/api/diagnose` для детальной диагностики
3. ✅ Убедитесь что NocoDB доступен: https://noco.povarnakolesah.ru
4. ✅ Проверьте, что токен активен и не истёк

---

**Время решения: 2-3 минуты** ⏱️

Просто запустите `./setup-env.sh`, введите токен и перезапустите dev сервер!

