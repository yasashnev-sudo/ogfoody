# 🔧 Финальное исправление виджета YooKassa

**Дата:** $(date)  
**Проблема:** `invalid_combination_of_payment_methods` все еще возникает

---

## 🔍 Проблема

Из логов:
```
❌ YooKassa widget error: {
  "error": "invalid_combination_of_payment_methods"
}
📦 Payment creation response: {
  "hasToken": true,
  "hasUrl": false,  // ← confirmationUrl не получен
  "confirmationType": "embedded"
}
```

**Причины:**
1. Ошибка `invalid_combination_of_payment_methods` все еще возникает
2. При `embedded` типе YooKassa не возвращает `confirmation_url`, только `confirmation_token`
3. Fallback не может работать без `confirmationUrl`

---

## ✅ Исправления

### 1. Убраны payment_methods из настроек виджета

**Файл:** `components/payment-modal.tsx`

**Было:**
```typescript
customization: {
  modal: false,
  payment_methods: ['bank_card', 'sbp'],
}
```

**Стало:**
```typescript
customization: {
  modal: false,
  // Убрали payment_methods - используем все доступные методы по умолчанию
}
```

**Почему:**
- YooKassa автоматически определяет доступные методы оплаты
- Это избегает ошибки `invalid_combination_of_payment_methods`
- Пользователь увидит все доступные методы в его регионе

### 2. Улучшен fallback на redirect

**Новая логика:**
1. При ошибке виджета пересоздаем платеж с типом `redirect` (`useWidget: false`)
2. Получаем `confirmationUrl` из нового платежа
3. Делаем redirect на страницу оплаты YooKassa

**Преимущества:**
- Гарантированно получаем `confirmationUrl` для redirect
- Не зависим от сохраненного `confirmationUrl` (которого может не быть при `embedded`)
- Пользователь всегда сможет оплатить, даже если виджет не работает

---

## 🚀 Деплой

Изменения закоммичены и запушены в `origin/main`.  
Деплой запущен через `deploy-increment-fix.expect`.

**Проверка после деплоя:**
1. Откройте https://ogfoody.ru
2. Создайте заказ
3. Попробуйте оплатить через виджет
4. Виджет должен работать без ошибки `invalid_combination_of_payment_methods`
5. Если виджет падает, должен сработать fallback с пересозданием платежа и redirect

---

## 📝 Что изменилось

**Файл:** `components/payment-modal.tsx`

1. ✅ Убраны `payment_methods` из настроек виджета
2. ✅ Улучшен fallback - пересоздание платежа с типом `redirect` при ошибке виджета
3. ✅ Добавлено детальное логирование процесса fallback

---

**Статус:** ✅ Исправлено, деплой выполняется
