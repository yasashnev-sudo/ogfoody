# Диагностика создания заказов

## Проблема
Заказы не попадают в базу данных NocoDB при создании через интерфейс.

## Что проверить

### 1. Проверка авторизации
Убедитесь, что вы авторизованы в системе:
- Откройте консоль браузера (F12)
- Проверьте, есть ли в localStorage ключ `currentUser`
- Проверьте, что `userProfile.id` установлен

### 2. Проверка логов браузера
При создании заказа в консоли браузера должны появиться:
- `📤 Отправка заказа на сервер:`
- `📥 Ответ сервера:`
- `✅ Результат создания заказа:`

Если видите ошибки - скопируйте их.

### 3. Проверка логов сервера
В терминале, где запущен `npm run dev`, должны появиться:
- `📥 POST /api/orders - получен запрос на создание заказа`
- `✅ Заказ получен:`
- `✅ Created NocoDB order`
- `✅ Заказ успешно создан и сохранен в базу`

Если видите ошибки - скопируйте их.

### 4. Проверка базы данных
Проверьте, что заказ действительно не создался:
```bash
curl http://localhost:3000/api/db/recent-orders
```

Или откройте в браузере:
```
http://localhost:3000/api/db/recent-orders
```

### 5. Тестовое создание заказа
Попробуйте создать тестовый заказ:
```bash
curl -X POST http://localhost:3000/api/db/test-real-order
```

Если тестовый заказ создается, значит проблема в данных, которые отправляются с фронтенда.

## Возможные причины

1. **Пользователь не авторизован** - заказ создается только для авторизованных пользователей
2. **Ошибка при создании персон/блюд** - заказ создается, но процесс прерывается
3. **Неправильный формат данных** - данные не проходят валидацию
4. **Ошибка сети** - запрос не доходит до сервера

## Что делать

1. Откройте консоль браузера (F12 → Console)
2. Создайте заказ
3. Скопируйте все сообщения из консоли (особенно ошибки)
4. Проверьте логи сервера в терминале
5. Сообщите, что видите в логах

