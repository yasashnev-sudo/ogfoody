# 🚀 Инструкция по деплою доработки DailyStatus

## 📋 Что было сделано

### ✅ Изменения в коде:

1. **Компонент `components/daily-status.tsx`**:
   - Реализована логика пополнения (Replenishment Logic) для авторизованных пользователей
   - Добавлена функция `calculateReplenishmentDate()` с использованием `date-fns`
   - Добавлены новые UI состояния:
     - **Food Streak Active**: Белый фон, фиолетовая иконка, желтая кнопка "СДЕЛАТЬ ЗАКАЗ"
     - **No Food**: Серый фон, пунктирная рамка, черная кнопка "ЗАКАЗАТЬ НА {дата}"
   - Добавлены функции форматирования дат: `formatDateWithDayOfWeek()`, `formatShortDateWithDayOfWeek()`
   - Сохранена обратная совместимость для гостевых пользователей

2. **Компонент `app/page.tsx`**:
   - Добавлен prop `isAuthenticated` в компонент `DailyStatus`
   - **Удалена прыгающая желтая табличка** "Выберите дату здесь ↓" для гостей

3. **Документация**:
   - Полностью обновлен файл `DAILY_STATUS_COMPONENT_DOCUMENTATION.md`
   - Добавлено описание логики пополнения, алгоритма расчета дат, всех UI состояний

---

## 🔄 Процесс деплоя

### Вариант 1: Автоматический деплой (рекомендуется)

```bash
# Из корня проекта
./deploy-daily-status-refactor.sh
```

Скрипт выполнит:
1. Подключение к серверу
2. Обновление кода из GitHub
3. Установку зависимостей
4. Сборку проекта
5. Перезапуск PM2
6. Проверку статуса

### Вариант 2: Ручной деплой

```bash
# 1. Закоммить и запушить изменения
git add .
git commit -m "feat: реализована логика пополнения для DailyStatus, удалена желтая табличка"
git push origin main

# 2. Подключиться к серверу
ssh root@5.129.194.168

# 3. На сервере выполнить
cd /var/www/ogfoody
git pull origin main
npm ci --production=false
npm run build
pm2 restart ogfoody

# 4. Проверить логи
pm2 logs ogfoody --lines 50
```

---

## ✅ Чеклист проверки после деплоя

### 1. Проверка компонента DailyStatus

- [ ] Компонент отображается на главной странице
- [ ] Для авторизованных пользователей:
  - [ ] Если есть еда → показывается "ВАШ ЗАПАС ЕДЫ ЗАКАНЧИВАЕТСЯ {дата}"
  - [ ] Если нет еды → показывается "ВАШ ХОЛОДИЛЬНИК ПУСТ" с датой ближайшей доставки
  - [ ] Кнопки работают и открывают календарь с правильной датой
- [ ] Для гостей:
  - [ ] Работает оригинальная логика (обратная совместимость)
  - [ ] Желтая прыгающая табличка **НЕ отображается**

### 2. Проверка остального функционала

- [ ] Календарь работает корректно
- [ ] Создание заказов работает
- [ ] История заказов отображается
- [ ] Авторизация работает
- [ ] Профиль пользователя работает
- [ ] Оплата работает

### 3. Проверка производительности

- [ ] Страница загружается быстро
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах PM2

---

## 🐛 Возможные проблемы и решения

### Проблема: Компонент не отображается

**Решение:**
1. Проверить логи PM2: `pm2 logs ogfoody`
2. Проверить консоль браузера на ошибки
3. Убедиться, что сборка прошла успешно: `npm run build`

### Проблема: Даты отображаются неправильно

**Решение:**
1. Проверить, что `availableDates` передается в компонент
2. Проверить формат дат в `orders` (должен быть `Date` или строка ISO)
3. Проверить работу `date-fns` функций

### Проблема: Логика пополнения не работает

**Решение:**
1. Убедиться, что `isAuthenticated` передается корректно
2. Проверить логику в функции `calculateReplenishmentDate()`
3. Проверить, что заказы имеют правильный формат `startDate`

---

## 📊 Мониторинг

После деплоя рекомендуется проверить:

1. **Логи приложения:**
   ```bash
   pm2 logs ogfoody --lines 100
   ```

2. **Статус PM2:**
   ```bash
   pm2 status ogfoody
   ```

3. **Метрики производительности:**
   - Время загрузки страницы
   - Время выполнения функций расчета дат

---

## 🔙 Откат изменений (если нужно)

Если что-то пошло не так, можно откатить:

```bash
# На сервере
cd /var/www/ogfoody
git log --oneline -10  # Найти коммит до изменений
git reset --hard <commit-hash>
npm run build
pm2 restart ogfoody
```

Или использовать последний рабочий коммит из ветки `main`:

```bash
git reset --hard origin/main
npm run build
pm2 restart ogfoody
```

---

## 📝 Контакты

При возникновении проблем:
1. Проверить логи PM2
2. Проверить консоль браузера
3. Проверить документацию: `DAILY_STATUS_COMPONENT_DOCUMENTATION.md`

---

## ✨ Дополнительная информация

- **Документация компонента:** `DAILY_STATUS_COMPONENT_DOCUMENTATION.md`
- **Скрипт деплоя:** `deploy-daily-status-refactor.sh`
- **Файлы изменений:**
  - `components/daily-status.tsx`
  - `app/page.tsx`
  - `DAILY_STATUS_COMPONENT_DOCUMENTATION.md`
