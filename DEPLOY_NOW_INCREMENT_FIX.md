# 🚀 ДЕПЛОЙ ИСПРАВЛЕНИЙ ИНКРЕМЕНТА ПРОМОКОДА - ИНСТРУКЦИЯ

**Дата:** 2026-01-15  
**Статус:** ✅ Изменения закоммичены и запушены в GitHub

---

## ✅ Что уже сделано

1. ✅ Все исправления закоммичены
2. ✅ Изменения запушены в `main` ветку GitHub
3. ✅ Тест переделан для локального запуска
4. ✅ Добавлены npm скрипты для тестирования

---

## 🚀 ДЕПЛОЙ НА ПРОДАКШН (выберите способ)

### Способ 1: Через SSH (ручной деплой)

```bash
# 1. Подключитесь к серверу
ssh root@5.129.194.168

# 2. Перейдите в директорию проекта
cd /root/my-project
# ИЛИ если проект в другом месте:
# cd /var/www/ogfoody

# 3. Получите последние изменения
git fetch origin
git reset --hard origin/main

# 4. Установите зависимости
npm install

# 5. Соберите проект
npm run build

# 6. Перезапустите приложение
pm2 restart all

# 7. Проверьте статус
pm2 status

# 8. Проверьте логи
pm2 logs --lines 50
```

### Способ 2: Через скрипт (если SSH настроен)

```bash
# На локальном компьютере:
cd "/Users/sergejasasnev/Downloads/my-project (1)"
./deploy-increment-fix.sh
```

### Способ 3: Автоматический (если настроен GitHub Actions)

Если у вас настроен автоматический деплой через GitHub Actions:
- Изменения уже запушены в `main`
- GitHub Actions автоматически задеплоит (проверьте в Actions на GitHub)

---

## 🧪 ТЕСТИРОВАНИЕ ПОСЛЕ ДЕПЛОЯ

### Локальное тестирование (перед деплоем)

```bash
# 1. Запустите локальный сервер в одном терминале
npm run dev

# 2. В другом терминале запустите тест
npm run test:fixes:local
```

### Тестирование на продакшн (после деплоя)

```bash
# Запустите тест на продакшн API
npm run test:fixes:prod
```

---

## 📊 ПРОВЕРКА ЛОГОВ НА СЕРВЕРЕ

После деплоя проверьте логи:

```bash
ssh root@5.129.194.168
pm2 logs --lines 100 | grep -E "(Проверка инкремента|fetchPromoCode|incrementPromoCodeUsage|promo code)"
```

Ищите записи:
- `🔍 [POST] Проверка инкремента промокода:` - проверка условия
- `Calling fetchPromoCode` - поиск промокода  
- `fetchPromoCode result` - результат поиска
- `Calling incrementPromoCodeUsage` - вызов инкремента
- `clientFetch update completed` - завершение обновления
- `✅ Счетчик промокода "..." инкрементирован` - успешный инкремент

---

## 🔍 ДИАГНОСТИКА ПРОБЛЕМ

### Проблема: Build failed

```bash
cd /root/my-project
rm -rf .next node_modules
npm install
npm run build
```

### Проблема: PM2 не перезапускается

```bash
pm2 status
pm2 restart all
# ИЛИ
pm2 stop all
pm2 start ecosystem.config.js
```

### Проблема: Логи не видны

```bash
# Проверить логи PM2
pm2 logs --lines 100

# Проверить конкретное приложение
pm2 logs ogfoody --lines 100
```

---

## ✅ CHECKLIST ДЕПЛОЯ

- [x] Изменения закоммичены
- [x] Изменения запушены в `main`
- [ ] Подключение к серверу работает
- [ ] `git pull` выполнен успешно
- [ ] `npm install` выполнен успешно
- [ ] `npm run build` выполнен успешно
- [ ] `pm2 restart all` выполнен успешно
- [ ] Логи проверены (нет ошибок)
- [ ] Тест на продакшн запущен
- [ ] Инкремент работает (проверено вручную)

---

## 📝 ЧТО ИСПРАВЛЕНО

1. **Улучшена проверка условия оплаты**
   - Поддержка разных типов данных (`true`, `"true"`, строки)
   - Проверка `nocoOrderId` в разных форматах
   - Детальное логирование условия

2. **Исправлена функция `fetchPromoCode()`**
   - Убрано условие `Active,eq,true`
   - Добавлено экранирование специальных символов
   - Детальное логирование

3. **Исправлена функция `incrementPromoCodeUsage()`**
   - Правильный формат обновления (получение текущего значения и обновление)
   - Добавлена функция `fetchPromoCodeById()`
   - Детальное логирование всех шагов

4. **Добавлено логирование в PATCH**
   - Проверка условия оплаты
   - Логирование поиска промокода
   - Логирование вызова инкремента

5. **Переделан тест для локального запуска**
   - Поддержка `localhost:3000` для локального тестирования
   - Добавлены npm скрипты

---

**Готово к деплою!** 🚀

Выполните команды из раздела "ДЕПЛОЙ НА ПРОДАКШН" выше.
