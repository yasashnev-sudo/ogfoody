# Исправление: Оплата наличными при создании заказа из повтора

## 🐛 Проблема:
При создании нового заказа из повтора существующего заказа не отображалась опция оплаты наличными в `PaymentModal`. Пользователь мог выбрать только СБП или карту.

**Комментарий пользователя:**
> "уже лучше, но нельзя было выбрать оплату наличными"

## 🔍 Причина:
При создании нового заказа в `handleSaveOrder` (строка 1319-1322) не устанавливался флаг `isNewOrder: true` при вызове `setPaymentOrder`. 

В `PaymentModal` опция оплаты наличными показывается только если `allowCash === true`, а `allowCash` устанавливается на основе `paymentOrder.isNewOrder === true` (строка 3966 в `app/page.tsx`).

Поскольку `isNewOrder` не был установлен, `allowCash` становился `false`, и опция наличных не отображалась.

## ✅ Решение:
Добавлен флаг `isNewOrder: true` при создании нового заказа в `handleSaveOrder`:

```typescript
setPaymentOrder({ 
  order: savedOrder, 
  total: savedOrder.total,
  isNewOrder: true // ✅ ИСПРАВЛЕНО 2026-01-16: Помечаем как новый заказ, чтобы разрешить оплату наличными
})
```

## 📁 Файлы:
- `app/page.tsx` (строка 1319-1323)

## 🧪 Тестирование:
1. Создать заказ из повтора существующего заказа
2. После создания заказа открывается `PaymentModal`
3. Проверить, что отображаются все три опции оплаты:
   - ✅ Карта
   - ✅ СБП
   - ✅ Наличные

## 📊 Результат:
- **До:** При создании заказа из повтора не отображалась опция оплаты наличными
- **После:** При создании заказа из повтора отображаются все три опции оплаты (карта, СБП, наличные)

## 🚀 Деплой:
- **Время:** 2026-01-16 19:32 UTC
- **Коммит:** `e6c74d82`
- **Статус:** ✅ Успешно
- **Результат:** ✅ Сработало! Пользователь подтвердил, что опция оплаты наличными теперь отображается

## 📝 Связанные исправления:
- `FIX_REPEAT_ORDER_DATE_ISSUE_2026_01_16.md` - Исправление проблемы с датами при повторе заказа
- `FIX_REPEAT_ORDER_PROMOCODE_2026_01_16.md` - Исправление проблемы с промокодами при повторе заказа
