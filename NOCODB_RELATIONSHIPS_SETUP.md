# Настройка связей между таблицами в NocoDB

## 📋 Необходимые связи

### 1. Orders ↔ Users
- **Orders.user_id** → **Users.Id** (Many-to-One / BelongsTo)
- Создает обратную связь: **Users** → **Orders** (One-to-Many / HasMany)

### 2. Orders ↔ Order_Persons
- **Order_Persons.order_id** → **Orders.Id** (Many-to-One / BelongsTo)
- Создает обратную связь: **Orders** → **Order_Persons** (One-to-Many / HasMany)

### 3. Order_Persons ↔ Order_Meals
- **Order_Meals.order_person_id** → **Order_Persons.Id** (Many-to-One / BelongsTo)
- Создает обратную связь: **Order_Persons** → **Order_Meals** (One-to-Many / HasMany)

### 4. Orders ↔ Order_Extras
- **Order_Extras.order_id** → **Orders.Id** (Many-to-One / BelongsTo)
- Создает обратную связь: **Orders** → **Order_Extras** (One-to-Many / HasMany)

### 5. Order_Meals ↔ Meals
- **Order_Meals.meal_id** → **Meals.Id** (Many-to-One / BelongsTo)
- Создает обратную связь: **Meals** → **Order_Meals** (One-to-Many / HasMany)

### 6. Order_Extras ↔ Extras
- **Order_Extras.extra_id** → **Extras.Id** (Many-to-One / BelongsTo)
- Создает обратную связь: **Extras** → **Order_Extras** (One-to-Many / HasMany)

### 7. Orders ↔ Reviews
- **Reviews.order_id** → **Orders.Id** (Many-to-One / BelongsTo)
- Создает обратную связь: **Orders** → **Reviews** (One-to-Many / HasMany)

## 🔧 Как настроить связи вручную

### Шаг 1: Откройте NocoDB
1. Войдите в ваш NocoDB проект
2. Найдите таблицу, в которой нужно создать связь (например, `Orders`)

### Шаг 2: Добавьте колонку типа "Linked Record"
1. В таблице `Orders` найдите колонку `user_id`
2. Нажмите на заголовок колонки `user_id`
3. Выберите "Edit Column" или "Изменить колонку"
4. Измените тип колонки на **"Link to another record"** (Связанная запись)
5. Настройте связь:
   - **Link to table**: Выберите `Users`
   - **Link from field**: Выберите `Id`
   - **Link to field**: Выберите `user_id`
   - **Relation type**: Выберите **"Many to One"** (Многие к одному)
6. Сохраните изменения

### Шаг 3: Повторите для всех связей

Выполните те же шаги для остальных связей:

1. **Order_Persons.order_id** → **Orders.Id** (Many to One)
2. **Order_Meals.order_person_id** → **Order_Persons.Id** (Many to One)
3. **Order_Extras.order_id** → **Orders.Id** (Many to One)
4. **Order_Meals.meal_id** → **Meals.Id** (Many to One)
5. **Order_Extras.extra_id** → **Extras.Id** (Many to One)
6. **Reviews.order_id** → **Orders.Id** (Many to One)

## ⚠️ Важно

- При создании связи типа "Many to One" (Многие к одному) NocoDB автоматически создаст обратную связь "One to Many" (Один ко многим)
- После настройки связей данные будут автоматически связаны
- Проверьте, что колонки `user_id`, `order_id`, `order_person_id`, `meal_id`, `extra_id` существуют в соответствующих таблицах

## 🎯 Результат

После настройки всех связей:
- В таблице `Users` появится виртуальная колонка со всеми заказами пользователя
- В таблице `Orders` появится виртуальная колонка со всеми персонами заказа
- В таблице `Order_Persons` появится виртуальная колонка со всеми блюдами персоны
- И так далее для всех связей

