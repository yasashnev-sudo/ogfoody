# Исправление: Загрузка логотипа в Preloader и шкала на ползунке баллов - 2026-01-14

## 🐛 Проблемы:

### Проблема 1: Долгая загрузка логотипа в Preloader
**Описание:**
При первом открытии сайта логотип в прелоадере долго грузится, что выглядит плохо - пользователь видит пустое место вместо логотипа.

**Причина:**
- Изображение загружается асинхронно через Next.js Image
- Нет placeholder'а, пока изображение загружается
- Пользователь видит пустой контейнер до загрузки

### Проблема 2: Нет шкалы на ползунке выбора баллов
**Описание:**
Ползунок выбора количества баллов для списания не имеет визуальной шкалы, пользователю сложно понять, какое значение он выбирает.

**Причина:**
- Ползунок был без визуальных меток
- Нет делений или подписей значений
- Пользователь не видит границы и промежуточные значения

## ✅ Решение:

### 1. Добавлен placeholder для логотипа в Preloader

**Изменения в `components/preloader.tsx`:**
```typescript
// Добавлен белый фон для контейнера
<div className="h-24 w-24 sm:h-32 sm:w-32 rounded-full border-4 border-black overflow-hidden flex items-center justify-center shadow-brutal bg-white">
  <Image
    src="/logo-small.png"
    alt="OGFooDY Logo"
    width={128}
    height={128}
    className="rounded-full object-cover"
    priority
    placeholder="blur"
    blurDataURL="data:image/svg+xml;base64,..." // SVG placeholder с цветами бренда
  />
</div>
```

**Что это дает:**
- ✅ Показывается placeholder сразу (не пустое место)
- ✅ Плавный переход от placeholder к реальному изображению
- ✅ Placeholder использует цвета бренда (желтый фон, фиолетовый логотип)
- ✅ Улучшенный UX - пользователь сразу видит что-то вместо пустоты

### 2. Добавлена визуальная шкала на ползунке

**Изменения в `components/payment-modal.tsx`:**
```typescript
<div className="py-3 px-1 -mx-1 relative">
  {/* Шкала с делениями */}
  <div className="absolute top-1 left-1 right-1 h-2 flex items-center justify-between pointer-events-none z-0">
    {[0, 25, 50, 75, 100].map((percent) => {
      return (
        <div key={percent} className="flex flex-col items-center">
          <div className="w-[1px] h-2 bg-muted-foreground/40" />
          {percent === 0 || percent === 100 ? (
            <span className="text-[9px] text-muted-foreground mt-0.5 font-bold leading-none">
              {percent === 0 ? '0' : maxPointsToUse}
            </span>
          ) : null}
        </div>
      )
    })}
  </div>
  <input
    type="range"
    min="0"
    max={maxPointsToUse}
    value={pointsToUse}
    onChange={(e) => handlePointsChange(Number(e.target.value))}
    className="w-full range-slider-mobile relative z-10"
  />
</div>
```

**Что это дает:**
- ✅ Визуальные деления на 0%, 25%, 50%, 75%, 100%
- ✅ Подписи значений на краях (0 и максимум)
- ✅ Пользователь видит границы и может точнее выбрать значение
- ✅ Улучшенный UX - понятно, где находится текущее значение

## 🎯 Результат:

**После исправления:**

1. **Preloader:**
   - ✅ Логотип показывается сразу с placeholder'ом
   - ✅ Нет пустого места при загрузке
   - ✅ Плавный переход к реальному изображению
   - ✅ Улучшенный визуальный опыт

2. **Ползунок баллов:**
   - ✅ Визуальная шкала с делениями
   - ✅ Подписи значений на краях
   - ✅ Пользователь видит границы выбора
   - ✅ Улучшенная точность выбора значения

**Файлы:**
- `components/preloader.tsx` (изменен)
- `components/payment-modal.tsx` (изменен)

## 📝 Технические детали:

### Placeholder для логотипа:
- Используется SVG в base64 для мгновенной загрузки
- Цвета соответствуют бренду (желтый #FFEA00, фиолетовый #9D00FF)
- Next.js автоматически применяет blur эффект при загрузке

### Шкала ползунка:
- Деления на 0%, 25%, 50%, 75%, 100% от максимального значения
- Подписи только на краях (0 и максимум) для чистоты интерфейса
- Позиционирование через absolute для наложения на ползунок
- z-index: 0 для шкалы, z-10 для input, чтобы ползунок был кликабельным
