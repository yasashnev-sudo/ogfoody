# Ограничение отмены заказа на сегодняшний день

**Дата**: 13 января 2026  
**Статус**: ✅ Завершено

## Описание изменения

Добавлено ограничение, запрещающее клиентам отменять заказы на сегодняшний день. Теперь клиенты могут отменить только заказы на будущие даты (завтра и позже).

## Причина изменения

Клиенты не должны иметь возможность отменить заказ в день доставки, так как заказ уже может быть в процессе приготовления или доставки.

## Изменённые файлы

### 1. Frontend: `components/order-history.tsx`

**Функция**: `canCancelOrder` (строка 292-304)

**Было**:
```typescript
const dateIsValid = orderDate.getTime() >= today.getTime()
```

**Стало**:
```typescript
// ✅ ИЗМЕНЕНО 2026-01-13: Клиент НЕ может отменить заказ на сегодняшний день
// Можно отменить только заказы на будущие даты (завтра и позже)
const dateIsValid = orderDate.getTime() > today.getTime()
```

### 2. Frontend: `components/order-modal.tsx`

**Переменная**: `canCancel` (строка 557-564)

**Было**:
```typescript
const canCancel = !!(
  existingOrder &&
  orderStartDate &&
  orderStartDate.getTime() >= today.getTime()
)
```

**Стало**:
```typescript
// ✅ ИЗМЕНЕНО 2026-01-13: Клиент НЕ может отменить заказ на сегодняшний день
// Можно отменить заказ, если:
// 1. Заказ существует
// 2. Дата доставки > сегодня (только будущие даты, завтра и позже)
const canCancel = !!(
  existingOrder &&
  orderStartDate &&
  orderStartDate.getTime() > today.getTime()
)
```

### 3. Backend: `app/api/orders/[id]/route.ts`

**Метод**: `DELETE` (после строки 1168)

**Добавлено**:
```typescript
// ✅ ДОБАВЛЕНО 2026-01-13: Проверка на дату доставки - нельзя отменить заказ на сегодня
const today = new Date()
today.setHours(0, 0, 0, 0)

let orderDate: Date
if (currentOrder.start_date) {
  orderDate = new Date(currentOrder.start_date)
} else if ((currentOrder as any).delivery_date) {
  orderDate = new Date((currentOrder as any).delivery_date)
} else {
  console.error(`⚠️ Заказ ${id} не имеет start_date или delivery_date`)
  return NextResponse.json({ 
    error: "Cannot determine order delivery date" 
  }, { status: 400 })
}
orderDate.setHours(0, 0, 0, 0)

// Запрещаем отмену заказа на сегодняшний день или прошедшие даты
if (orderDate.getTime() <= today.getTime()) {
  console.log(`❌ Попытка отменить заказ ${id} на дату ${orderDate.toISOString().split('T')[0]} (сегодня или прошлое)`)
  return NextResponse.json({ 
    error: "Cannot cancel order for today or past dates. Only future orders can be cancelled." 
  }, { status: 403 })
}
```

## Поведение системы

### До изменения
- ✅ Клиент мог отменить заказ на сегодняшний день
- ✅ Клиент мог отменить заказ на будущие даты
- ❌ Клиент не мог отменить заказ на прошедшие даты

### После изменения
- ❌ Клиент **не может** отменить заказ на сегодняшний день (403 Forbidden)
- ✅ Клиент может отменить заказ на будущие даты (завтра и позже)
- ❌ Клиент не может отменить заказ на прошедшие даты (403 Forbidden)

## UI изменения

### ✅ ОБНОВЛЕНО 2026-01-13: Добавлена кнопка связи с поддержкой

1. **История заказов** (`order-history.tsx`): 
   - Кнопка "ОТМЕНИТЬ" больше не отображается для заказов на сегодня
   - ✨ **Вместо неё показывается кнопка "ПОДДЕРЖКА"** (зелёная с иконкой чата)
   - При клике открывается WhatsApp с предзаполненным сообщением: "У меня вопрос по заказу №{orderNumber}"

2. **Модальное окно заказа** (`order-modal.tsx`): 
   - Кнопка "Отменить заказ" больше не отображается для заказов на сегодня
   - ✨ **Вместо неё показывается кнопка "Связаться с поддержкой"** (зелёная с иконкой чата)
   - При клике открывается WhatsApp с предзаполненным сообщением: "У меня вопрос по заказу №{orderNumber}"

## API изменения

**Endpoint**: `DELETE /api/orders/[id]`

### Новый ответ при попытке отменить заказ на сегодня:

**Статус**: `403 Forbidden`

**Тело ответа**:
```json
{
  "error": "Cannot cancel order for today or past dates. Only future orders can be cancelled."
}
```

## Визуальное представление

### Заказ на сегодня
```
┌─────────────────────────────────────┐
│ 📅 13 янв                           │
│ № ORD-20260113-ABC123               │
│ ⏰ 18:00 - 21:00 | 👥 2             │
│                                     │
│ [🔄 ПОВТОРИТЬ ЗАКАЗ] [💬 ПОДДЕРЖКА] │ ← Зелёная кнопка вместо "Отменить"
└─────────────────────────────────────┘
```

### Заказ на завтра или позже
```
┌─────────────────────────────────────┐
│ 📅 14 янв                           │
│ № ORD-20260114-XYZ789               │
│ ⏰ 18:00 - 21:00 | 👥 2             │
│                                     │
│ [🔄 ПОВТОРИТЬ ЗАКАЗ] [🗑️ ОТМЕНИТЬ] │ ← Обычная кнопка отмены
└─────────────────────────────────────┘
```

## Тестирование

### Сценарий 1: Заказ на сегодня
1. Создайте заказ на сегодняшний день
2. Откройте историю заказов
   - ✅ Вместо кнопки "ОТМЕНИТЬ" должна быть зелёная кнопка "ПОДДЕРЖКА"
3. Нажмите кнопку "ПОДДЕРЖКА"
   - ✅ Должен открыться WhatsApp с сообщением "У меня вопрос по заказу №..."
4. Попытайтесь отменить заказ через API: `DELETE /api/orders/[id]`
   - ✅ Должен вернуться статус 403 с сообщением об ошибке

### Сценарий 2: Заказ на завтра
1. Создайте заказ на завтра
2. Откройте историю заказов
   - ✅ Кнопка "ОТМЕНИТЬ" должна отображаться
3. Нажмите "ОТМЕНИТЬ"
   - ✅ Заказ должен успешно отмениться

### Сценарий 3: Модальное окно заказа
1. Откройте заказ на сегодня в модальном окне
2. Прокрутите вниз
   - ✅ Вместо "Отменить заказ" должна быть кнопка "Связаться с поддержкой"
3. Нажмите кнопку
   - ✅ Должен открыться WhatsApp с предзаполненным сообщением

## Функция связи с поддержкой

### Реализация в `order-history.tsx`

```typescript
const isTodayOrder = (startDate: Date | string) => {
  const today = new Date()
  today.setHours(0, 0, 0, 0)
  const orderDate = toDate(startDate)
  orderDate.setHours(0, 0, 0, 0)
  
  return orderDate.getTime() === today.getTime()
}

const handleContactSupport = (orderNumber?: string) => {
  const message = orderNumber 
    ? `У меня вопрос по заказу №${orderNumber}`
    : `У меня вопрос по заказу`
  
  // Открываем WhatsApp с предзаполненным сообщением
  const whatsappUrl = `https://wa.me/74951234567?text=${encodeURIComponent(message)}`
  window.open(whatsappUrl, '_blank')
}
```

### Реализация в `order-modal.tsx`

```typescript
const handleContactSupport = () => {
  const orderNumber = existingOrder?.orderNumber
  const message = orderNumber 
    ? `У меня вопрос по заказу №${orderNumber}`
    : `У меня вопрос по заказу`
  
  // Открываем WhatsApp с предзаполненным сообщением
  const whatsappUrl = `https://wa.me/74951234567?text=${encodeURIComponent(message)}`
  window.open(whatsappUrl, '_blank')
}
```

### Контакты поддержки

- **WhatsApp**: +7 (495) 123-45-67
- **Формат сообщения**: "У меня вопрос по заказу №{orderNumber}"

### ⚠️ Важно: Обновление номера телефона

Если номер телефона поддержки отличается, обновите его в двух местах:

1. **`components/order-history.tsx`** - строка с `https://wa.me/74951234567`
2. **`components/order-modal.tsx`** - строка с `https://wa.me/74951234567`

Замените `74951234567` на актуальный номер в формате: код страны + номер (без пробелов и знаков).

**Пример**: для номера +7 (999) 123-45-67 используйте `79991234567`

## Примечания

- Проверка времени выполняется на уровне полуночи (00:00:00) для корректного сравнения дат
- Проверка выполняется как на frontend (UI), так и на backend (API) для обеспечения безопасности
- Сообщение об ошибке на английском языке для единообразия с другими API ошибками
- ✨ **Кнопка связи с поддержкой** открывает WhatsApp с предзаполненным сообщением о заказе
- Номер телефона поддержки можно изменить, обновив значение в функциях `handleContactSupport`

