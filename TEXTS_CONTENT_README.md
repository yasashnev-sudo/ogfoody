# 📝 Файл с текстами для проекта

## Описание

Файл `TEXTS_CONTENT.json` содержит все тексты, которые отображаются пользователям в приложении:
- Уведомления об успехе
- Сообщения об ошибках
- Информация о компании
- FAQ
- Тексты в модалках
- И другие пользовательские сообщения

## Как использовать

1. **Откройте файл `TEXTS_CONTENT.json`**
2. **Замените тестовые тексты на реальные**
3. **Сохраните файл**

## Структура файла

### 1. Уведомления (`notifications`)
- `profileSaved` - уведомление о сохранении профиля
- `welcome` - приветственное сообщение после входа
- `reviewSubmitted` - уведомление об отправке отзыва

### 2. Ошибки (`errors`)
- `orderUpdate` - ошибка обновления заказа
- `orderCreate` - ошибка создания заказа
- `orderExists` - заказ уже существует
- `payment` - ошибка оплаты
- `profileSave` - ошибка сохранения профиля
- `checkout` - ошибка оформления
- `review` - ошибки при отправке отзыва
- `districtRequired` - район не указан
- `orderDataMissing` - отсутствуют данные о блюдах

### 3. Информация о компании (`company`)
- `name` - название компании
- `tagline` - слоган
- `about` - информация о компании
- `delivery` - информация о доставке
- `support` - контакты поддержки
- `faq` - часто задаваемые вопросы
- `chat` - тексты чата поддержки
- `terms` - пользовательское соглашение

### 4. Информационный баннер (`infoBanner`)
- Тексты для баннера с информацией о доставке

### 5. Выбор района (`districtSelection`)
- Тексты модалки выбора района доставки

### 6. История заказов (`orderHistory`)
- Тексты для истории заказов и диалогов

### 7. Другие компоненты
- `reviewModal` - модалка отзывов
- `orderLoading` - загрузка заказа
- `calendar` - календарь
- `menu` - меню приложения

## Особенности

### Переменные в текстах
Некоторые тексты содержат переменные, которые будут заменены на реальные значения:
- `{userName}` - имя пользователя
- `{date}` - дата заказа
- `{orderNumber}` - номер заказа

Пример:
```json
"title": "Добро пожаловать, {userName}!"
```

### HTML в текстах
Некоторые тексты могут содержать HTML-теги для форматирования:
```json
"description": "Доставка в большинстве районов — <span class=\"font-semibold text-green-700\">бесплатная</span>!"
```

## После заполнения

После того, как вы заполните все тексты в файле `TEXTS_CONTENT.json`, разработчик интегрирует их в код приложения, заменив все тестовые тексты на ваши реальные.

## Примечания

- Сохраняйте структуру JSON (запятые, кавычки, скобки)
- Не удаляйте ключи (названия полей), только меняйте значения
- Если какой-то текст не нужен, оставьте пустую строку `""`
- Для многострочных текстов используйте `\n` для переноса строки
