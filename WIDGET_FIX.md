# 🔧 Исправление проблемы с виджетом YooKassa

**Дата:** $(date)  
**Проблема:** Виджет инициализировался, но затем падал с ошибкой "iframe was removed from DOM"

---

## 🔍 Проблема

Из логов браузера:
```
✅ YooKassa widget initialized with token: ct-30ff5dba-000f-500...
❌ YooKassa widget error: Object
⚠️ Widget error, falling back to redirect
YooMoney Checkout Widget: iframe was removed from DOM, it can disrupt the widget. Please use destroy method for it
```

**Причина:**
- Контейнер виджета (`yookassa-widget-container`) удалялся из DOM до полной инициализации виджета
- Виджет инициализировался до того, как контейнер был полностью отрендерен
- При ошибке не использовался сохраненный `confirmationUrl` для fallback

---

## ✅ Исправления

### 1. Добавлено сохранение `confirmationUrl`
```typescript
const [confirmationUrl, setConfirmationUrl] = useState<string | null>(null)
```

Теперь `confirmationUrl` сохраняется при создании платежа и используется для fallback при ошибках виджета.

### 2. Исправлена инициализация виджета

**Было:**
- Виджет инициализировался сразу, когда `showWidget` и `confirmationToken` были установлены
- Не проверялось, существует ли контейнер в DOM

**Стало:**
- Добавлена функция `initWidget()`, которая проверяет наличие контейнера в DOM
- Если контейнер не найден, функция повторяет попытку через 100ms
- Добавлена задержка 100ms перед инициализацией, чтобы контейнер точно был в DOM

```typescript
const initWidget = () => {
  const container = document.getElementById('yookassa-widget-container')
  if (!container) {
    console.warn('⚠️ Widget container not found, retrying...')
    setTimeout(initWidget, 100)
    return
  }
  // ... инициализация виджета
}

setTimeout(initWidget, 100)
```

### 3. Улучшен cleanup виджета

- При закрытии модального окна виджет правильно уничтожается
- `checkoutWidgetRef.current` устанавливается в `null` после уничтожения
- Очищается `confirmationUrl` при закрытии

### 4. Улучшен fallback на redirect

При ошибке виджета теперь используется сохраненный `confirmationUrl` вместо запроса к API:

```typescript
if (confirmationUrl) {
  window.location.href = confirmationUrl
} else if (order.paymentId) {
  // Fallback на API запрос
}
```

---

## 🚀 Деплой

Изменения закоммичены и запушены в `origin/main`.  
Деплой запущен через `deploy-increment-fix.expect`.

**Проверка после деплоя:**
1. Откройте https://ogfoody.ru
2. Создайте заказ
3. Попробуйте оплатить через виджет
4. Виджет должен отображаться без ошибок
5. При ошибке должен сработать fallback на redirect

---

## 📝 Что изменилось

**Файл:** `components/payment-modal.tsx`

1. ✅ Добавлено состояние `confirmationUrl`
2. ✅ Исправлена инициализация виджета с проверкой контейнера
3. ✅ Улучшен cleanup виджета
4. ✅ Улучшен fallback на redirect при ошибках

---

**Статус:** ✅ Исправлено, деплой выполняется
