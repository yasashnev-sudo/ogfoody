# ⚡ Шпаргалка: Деплой ogfoody.ru

## 🎯 ЗА 3 КОМАНДЫ:

```bash
# 1. Закоммитить созданные файлы
git add . && git commit -m "Setup deployment" && git push

# 2. На сервере: Автонастройка
ssh root@5.129.194.168
curl -o setup.sh https://raw.githubusercontent.com/yasashnev-sudo/ogfoody/main/server-setup.sh
bash setup.sh

# 3. Готово! 
# Откройте: https://ogfoody.ru
```

---

## 📋 ПОЛНЫЙ ПРОЦЕСС:

### 1️⃣ Сейчас на вашем компьютере:

```bash
cd "/Users/sergejasasnev/Downloads/my-project (1)"
git add .
git commit -m "🚀 Setup deployment for ogfoody.ru"
git push
```

### 2️⃣ Настройка DNS (reg.ru):

```
A-запись: ogfoody.ru → 5.129.194.168
A-запись: www.ogfoody.ru → 5.129.194.168
```

### 3️⃣ На сервере:

```bash
ssh root@5.129.194.168

# Автоматическая настройка (если есть server-setup.sh на GitHub)
curl -o server-setup.sh https://raw.githubusercontent.com/yasashnev-sudo/ogfoody/main/server-setup.sh
chmod +x server-setup.sh
bash server-setup.sh

# Или вручную:
# Установка ПО
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs nginx certbot python3-certbot-nginx
npm install -g pm2

# Клонирование проекта
mkdir -p /var/www/ogfoody
cd /var/www/ogfoody
git clone https://github.com/yasashnev-sudo/ogfoody.git .

# Настройка .env
nano .env.production
# Вставьте содержимое из ENV_PRODUCTION_EXAMPLE.txt
# Замените YOUR_NOCODB_TOKEN_HERE на реальный токен

# Сборка
npm install
npm run build

# Запуск
pm2 start ecosystem.config.js
pm2 save

# Nginx
cp nginx-ogfoody.conf /etc/nginx/sites-available/ogfoody.conf
ln -s /etc/nginx/sites-available/ogfoody.conf /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx

# SSL
certbot --nginx -d ogfoody.ru -d www.ogfoody.ru
```

### 4️⃣ GitHub Secrets:

```
Settings → Secrets and variables → Actions → New secret

SERVER_HOST = 5.129.194.168
SERVER_USER = root
SERVER_PASSWORD = pULRoAvF@P-@4Y
SERVER_PATH = /var/www/ogfoody
```

---

## 🔄 WORKFLOW ПОСЛЕ ДЕПЛОЯ:

```bash
# Делаете изменения локально
# Потом:
git add .
git commit -m "Описание изменений"
git push

# GitHub Actions автоматически задеплоит на ogfoody.ru
# Ждёте 2-3 минуты
# Готово! ✨
```

---

## 🛠️ КОМАНДЫ ДЛЯ СЕРВЕРА:

```bash
# Статус
pm2 status
systemctl status nginx

# Логи
pm2 logs ogfoody
tail -f /var/log/nginx/ogfoody_error.log

# Перезапуск
pm2 restart ogfoody
systemctl reload nginx

# Проверка сайта
curl https://ogfoody.ru/api/health
curl https://ogfoody.ru/api/diagnose
```

---

## 🆘 БЫСТРЫЙ FIX:

```bash
# Приложение упало
pm2 restart ogfoody

# 502 Bad Gateway
pm2 status
pm2 logs ogfoody

# Товары не загружаются
cat /var/www/ogfoody/.env.production
curl https://noco.povarnakolesah.ru

# Обновить код вручную
cd /var/www/ogfoody
git pull
npm install
npm run build
pm2 restart ogfoody
```

---

## 📚 ДОКУМЕНТАЦИЯ:

- **Начать:** `START_HERE_DEPLOYMENT.md`
- **Быстро:** `QUICK_START_DEPLOYMENT.md`
- **Подробно:** `DEPLOYMENT_GUIDE_OGFOODY.md`
- **Проверка:** `DEPLOYMENT_CHECKLIST.md`

---

## ✅ CHECKLIST:

- [ ] `git push` выполнен
- [ ] DNS настроен на reg.ru
- [ ] Сервер настроен (Node.js, PM2, Nginx)
- [ ] Проект склонирован на сервер
- [ ] `.env.production` создан и заполнен
- [ ] `npm run build` успешно
- [ ] PM2 запустил приложение
- [ ] Nginx конфиг скопирован
- [ ] SSL сертификат получен
- [ ] Сайт открывается: https://ogfoody.ru
- [ ] GitHub Secrets настроены
- [ ] Автодеплой работает

---

## 🎉 ГОТОВО!

**Сайт:** https://ogfoody.ru  
**Репо:** https://github.com/yasashnev-sudo/ogfoody  
**Статус:** ✅ Готов к деплою

---

*Если нужна помощь - смотрите подробную документацию!*

