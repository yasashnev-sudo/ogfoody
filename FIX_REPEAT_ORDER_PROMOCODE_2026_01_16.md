# Исправление: Промокод при повторе заказа

**Дата:** 2026-01-16  
**Статус:** ✅ Исправлено

## 🐛 Проблема:

После внедрения промокодов функция "Повторить заказ" перестала работать корректно. Промокод и скидка из старого заказа переносились в новый заказ при повторе.

## 🔍 Причина:

В функции `handleRepeatOrder` при создании нового заказа через `...validatedOrder` копировались все поля из старого заказа, включая `promoCode` и `promoDiscount`. Это приводило к тому, что:

1. Промокод из старого заказа оставался в новом заказе
2. Скидка промокода применялась автоматически
3. Пользователь не мог применить новый промокод или создать заказ без промокода

### Технические детали:

- `validateOrderItems` использует `...order`, что копирует все поля, включая промокод
- В `handleRepeatOrder` используется `...validatedOrder`, что снова копирует промокод
- Промокод не очищался явно при создании нового заказа

## ✅ Решение:

Добавлена явная очистка промокода и скидки при создании нового заказа для повтора:

```typescript
const newOrder: Order = {
  ...validatedOrder,
  // ... другие поля
  // ✅ ИСПРАВЛЕНО 2026-01-16: Очищаем промокод и скидку при повторе заказа
  promoCode: undefined,
  promoDiscount: undefined,
}
```

## 📊 Результат:

- ✅ При повторе заказа промокод очищается
- ✅ Пользователь может применить новый промокод
- ✅ Пользователь может создать заказ без промокода
- ✅ Существующая логика промокодов не затронута
- ✅ Логика баллов лояльности не изменена

## 🚀 Деплой:

**Время:** 2026-01-16  
**Статус:** ✅ Задеплоено на production

## 📝 Файлы:

- `app/page.tsx`:
  - Функция `handleRepeatOrder` (строки 1709-1869)
  - Очистка промокода при создании нового заказа (строки 1819-1820)

## ⚠️ Важно:

- **НЕ изменялась** логика работы с промокодами
- **НЕ изменялась** логика работы с баллами лояльности
- Изменение минимальное и безопасное - только очистка полей при повторе заказа

---

**Проверено:** ✅  
**Тестирование:** Требуется проверка на production после деплоя
