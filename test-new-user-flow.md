# 🧪 Тестирование flow нового пользователя

## Шаги для ручного тестирования

### 1. Подготовка
```bash
# Откройте браузер в режиме Инкогнито
# Откройте DevTools (F12) → вкладка Console
# Откройте http://localhost:3000
```

### 2. Проверка логов при загрузке

**Ожидаемые логи в консоли:**
```
⚠️ Гость без района, показываем модалку выбора района
```

**Что должно быть на экране:**
- ✅ Модалка "Укажите район доставки"
- ✅ Текст "Мы работаем в Санкт-Петербурге"

### 3. Выбор района

**Действия:**
1. Выберите "Адмиралтейский район"
2. Нажмите "Продолжить"

**Ожидаемые логи:**
```
💾 Сохраняем выбранный район: Адмиралтейский район
✅ Район сохранен для гостя: Адмиралтейский район
```

**Проверка localStorage:**
```javascript
// В консоли браузера:
localStorage.getItem('guest_district')
// Должно быть: "Адмиралтейский район"

localStorage.getItem('district_modal_seen')
// Должно быть: "true"
```

### 4. Создание заказа

**Действия:**
1. Выберите дату в календаре
2. Добавьте блюда
3. Проверьте что внизу показывается стоимость доставки

**Ожидаемые логи:**
```
🗺️ Загружен район гостя: Адмиралтейский район
```

### 5. Нажатие "Заказать"

**Действия:**
1. Нажмите кнопку "Заказать · XXXX ₽"

**Ожидаемые логи:**
```
🚀 Неавторизованный пользователь нажал 'Заказать': { order: {...}, total: XXXX }
🔐 Запрос авторизации с заказом: { order: {...}, total: XXXX }
```

**Что должно быть на экране:**
- ✅ Модалка авторизации
- ✅ Поле ввода телефона

### 6. Авторизация

**Действия:**
1. Введите номер: +7 (999) 555-11-22
2. Нажмите "СМС-код"
3. Скопируйте код из консоли
4. Введите код

**Ожидаемые логи при вводе кода:**
```
🔐 Демо-код для +7 (999) 555-11-22: XXXX
```

**После подтверждения кода:**
```
🔄 Синхронизация пользователя с базой данных...
📡 Ищем пользователя в базе по телефону: 79995551122
⚠️ Пользователя нет в базе, создаем нового...
✅ Пользователь создан в базе данных: XX userProfile.id установлен: XX с районом: Адмиралтейский район
💾 Профиль сохранен в localStorage с id: XX
👤 Устанавливаем userProfile: { id: XX, phone: '79995551122', hasId: true }
✅ У пользователя уже есть район: Адмиралтейский район
```

**КРИТИЧЕСКИЙ МОМЕНТ - должен сработать useEffect:**
```
🔍 Проверяем заполненность профиля после авторизации: {
  name: undefined,
  street: undefined,
  building: undefined,
  district: "Адмиралтейский район"
}
⚠️ Профиль не заполнен, открываем ProfileModal
```

**Что должно быть на экране:**
- ✅ Модалка "Заполните данные для доставки"
- ✅ Синий баннер "📦 Оформление заказа..."
- ✅ Район уже заполнен
- ✅ Нет кнопки закрытия (X)
- ✅ Кнопка "Оформить заказ"

### 7. Заполнение профиля

**Действия:**
1. Заполните имя: "Тест"
2. Заполните улицу: "Невский проспект"
3. Заполните дом: "1"
4. Нажмите "Оформить заказ"

**Ожидаемые логи:**
```
🚀 После заполнения профиля оформляем заказ
🚀 Автооформление заказа после авторизации
💰 Стоимость доставки: 250₽ для района "Адмиралтейский район"
```

**Что должно быть на экране:**
- ✅ ProfileModal закрывается
- ✅ Уведомление "Заказ оформлен!"
- ✅ PaymentModal открывается

---

## Возможные проблемы и решения

### Проблема 1: useEffect не срабатывает

**Симптомы:**
- После авторизации ProfileModal не открывается
- В логах нет "🔍 Проверяем заполненность профиля..."

**Причина:**
- useEffect зависит от изменения состояния
- Возможно состояние не обновляется

**Проверка:**
```javascript
// В консоли браузера после авторизации:
console.log('isAuthenticated:', window.__REACT_DEVTOOLS_GLOBAL_HOOK__)
```

### Проблема 2: Пользователь не создается в БД

**Симптомы:**
- В логах: "⚠️ Пользователь с User ID=XX не найден"

**Причина:**
- Ошибка при создании пользователя
- Неправильный формат данных

**Проверка в NocoDB:**
1. Откройте NocoDB
2. Таблица Users
3. Найдите пользователя по телефону 79995551122

### Проблема 3: ProfileModal не открывается

**Возможные причины:**
1. `shouldAutoCheckout` не установлен в `true`
2. `pendingCheckout` пустой
3. `userProfile` не обновился
4. `isAuthenticated` не `true`

**Проверка в консоли:**
```javascript
// После авторизации проверьте в React DevTools или добавьте временный console.log
```

---

## Отладочные логи для добавления

Если не работает, добавьте эти логи для отладки:

### В app/page.tsx в useEffect:

```typescript
useEffect(() => {
  console.log('🔍 useEffect сработал:', {
    isAuthenticated,
    hasUserProfile: !!userProfile,
    shouldAutoCheckout,
    hasPendingCheckout: !!pendingCheckout,
    userName: userProfile?.name,
    userStreet: userProfile?.street,
    userBuilding: userProfile?.building,
  })
  
  if (isAuthenticated && userProfile && shouldAutoCheckout && pendingCheckout) {
    // ... остальной код
  }
}, [isAuthenticated, userProfile, shouldAutoCheckout, pendingCheckout])
```

---

## Что проверить в коде

1. **В handleLogin** - убедитесь что `setUserProfile(profile)` вызывается
2. **В onRequestAuth** - проверьте что `setShouldAutoCheckout(true)` вызывается
3. **В useEffect** - проверьте что все зависимости правильные
4. **В ProfileModal** - проверьте что `isCheckoutFlow` передается

---

## Следующие шаги

Если тест не проходит:
1. Скопируйте ВСЕ логи из консоли браузера
2. Проверьте Network вкладку на ошибки API
3. Проверьте React DevTools - состояние компонента
4. Отправьте логи для анализа



