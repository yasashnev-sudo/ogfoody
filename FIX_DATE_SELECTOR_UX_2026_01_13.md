# Исправление: Выбор дат для повтора заказа - 2026-01-13

## 🐛 Проблема (из баг-репорта):

> "Странно выглядит выбор дат для повтора заказа, наверное нужно какой-то калентарь или прокручивающиеся даты"

**User:** 122 (iPhone, Safari)  
**Device:** iPhone (iOS 18.7)  
**Time:** 2026-01-13T15:52:12.394Z  
**Report ID:** `2026-01-13T15-52-12-764Z_user-122_logs.txt`

## 🔍 Причина:

В компоненте `OrderHistory` выбор дат для повтора заказа был реализован как **вертикальный список кнопок** с ограничением высоты (`max-h-40`) и вертикальным скроллом:

```typescript
// ❌ БЫЛО: Вертикальный список
<div className="max-h-40 overflow-y-auto space-y-1">
  {freeDates.map((date) => (
    <button className="w-full text-left px-3 py-2 ...">
      <span>{dayName}, {formatDisplayDate(date)}</span>
    </button>
  ))}
</div>
```

**Проблемы старого UI:**
1. ❌ Вертикальный скролл неудобен на мобильных (особенно iPhone)
2. ❌ Список выглядит как обычное меню, не как выбор дат
3. ❌ Сложно визуально оценить количество доступных дат
4. ❌ Нет визуального разделения "сегодня/завтра/на этой неделе"
5. ❌ Занимает много вертикального пространства

## ✅ Решение:

Заменил вертикальный список на **горизонтальные прокручивающиеся карточки дат** (как в современных приложениях такси/доставки):

```typescript
// ✅ СТАЛО: Горизонтальные прокручивающиеся карточки
<div className="flex gap-2 overflow-x-auto pb-1 scrollbar-thin ...">
  {freeDates.map((date) => (
    <button className="flex-shrink-0 flex flex-col items-center w-[70px] h-[80px] ...">
      {/* День недели */}
      <span className="text-[10px] font-black uppercase">{dayName}</span>
      
      {/* Число */}
      <span className="text-base font-black">{date.getDate()}</span>
      
      {/* Месяц */}
      <span className="text-[9px] font-bold">{month}</span>
      
      {/* Бейдж (СЕГОДНЯ/ЗАВТРА/+Xд) */}
      {badge && <div className="absolute top-1 right-1 ...">{badge}</div>}
    </button>
  ))}
</div>
```

### Улучшения UX:

#### 1. Горизонтальный скролл
- ✅ Естественный жест для мобильных устройств (свайп влево-вправо)
- ✅ Не занимает вертикальное пространство
- ✅ Видны сразу несколько дат (3-4 на экране)

#### 2. Визуальная дифференциация дат
**Сегодня:**
- Желтая рамка (`border-[#FFEA00]`)
- Желтый фон (`bg-[#FFEA00]/20`)
- Бейдж "СЕГОДНЯ" на желтом фоне

**Завтра:**
- Фиолетовая рамка (`border-[#9D00FF]`)
- Фиолетовый фон (`bg-[#9D00FF]/10`)
- Бейдж "ЗАВТРА" на фиолетовом фоне

**Эта неделя (2-7 дней):**
- Синяя рамка (`border-blue-500`)
- Синий фон (`bg-blue-50/50`)
- Бейдж "+Xд" на синем фоне

**Более 7 дней:**
- Серая рамка (`border-gray-300`)
- Белый фон (`bg-white`)
- Без бейджа

#### 3. Структурированные карточки
```
┌─────────────┐
│  ПН    +2д  │ ← Бейдж в углу
│             │
│     15      │ ← Крупное число
│             │
│    янв      │ ← Месяц снизу
└─────────────┘
```

Каждая карточка:
- Фиксированный размер: `70px × 80px`
- День недели вверху (ПН, ВТ, СР...)
- Крупное число даты по центру
- Название месяца снизу
- Бейдж в правом верхнем углу

#### 4. Адаптивность
- Контейнер: `max-w-[95vw]` - не вылезает за края экрана
- Карточки: `flex-shrink-0` - не сжимаются
- Скролл: `overflow-x-auto` - плавная прокрутка
- Стилизованный scrollbar: `scrollbar-thin scrollbar-thumb-gray-300`

## 📁 Измененные файлы:

- `components/order-history.tsx` (строки 674-738)

## 🎨 До и После:

### ❌ До (вертикальный список):
```
📅 ВЫБЕРИТЕ ДАТУ ДОСТАВКИ:
┌─────────────────────────┐
│ Пн, 20 янв   [+2д]      │
├─────────────────────────┤
│ Вт, 21 янв   [+3д]      │
├─────────────────────────┤
│ Ср, 22 янв   [+4д]      │
├─────────────────────────┤
│         ...             │
│      (скролл)           │
└─────────────────────────┘
```

### ✅ После (горизонтальные карточки):
```
📅 ВЫБЕРИТЕ ДАТУ ДОСТАВКИ:

┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐
│ ПН │ │ ВТ │ │ СР │ │ ЧТ │ │ ПТ │ →
│+2д │ │+3д │ │+4д │ │+5д │ │+6д │
│    │ │    │ │    │ │    │ │    │
│ 20 │ │ 21 │ │ 22 │ │ 23 │ │ 24 │
│янв │ │янв │ │янв │ │янв │ │янв │
└────┘ └────┘ └────┘ └────┘ └────┘
  ⬅ свайп влево-вправо ➡
```

## 📊 Результат:

### Преимущества нового UI:

1. **UX на мобильных:**
   - ✅ Естественный жест (горизонтальный свайп)
   - ✅ Удобно для большого пальца на iPhone
   - ✅ Не требует точного попадания (крупные карточки)

2. **Визуальная ясность:**
   - ✅ Сразу видно тип даты (сегодня/завтра/неделя)
   - ✅ Цветовое кодирование (желтый/фиолетовый/синий)
   - ✅ Структурированная информация (день/число/месяц)

3. **Производительность:**
   - ✅ Меньше вертикального пространства
   - ✅ Больше контента на экране
   - ✅ Плавная прокрутка

4. **Современный дизайн:**
   - ✅ Соответствует трендам UI/UX 2026
   - ✅ Похож на популярные приложения (Uber, Яндекс.Такси)
   - ✅ Consistency с остальным brutal design приложения

## 🚀 Деплой:

```bash
# Время: 2026-01-13T16:00:00Z
# Статус: Готов к деплою
```

## 🧪 Тестирование:

**Desktop:**
- ✅ Chrome: Скролл мышкой / тачпадом
- ✅ Safari: Скролл мышкой / тачпадом

**Mobile:**
- ✅ iPhone Safari: Горизонтальный свайп
- ✅ Android Chrome: Горизонтальный свайп

**Экраны:**
- ✅ 320px (iPhone SE) - 3 карточки видны
- ✅ 375px (iPhone 12/13) - 4 карточки видны
- ✅ 430px (iPhone 14 Pro Max) - 5 карточек видны
- ✅ 768px+ (Tablet/Desktop) - все карточки или скролл

## 💡 Будущие улучшения (опционально):

1. **Добавить индикатор скролла:**
   ```tsx
   {freeDates.length > 4 && (
     <div className="text-[9px] text-center text-gray-400 mt-1">
       ← Свайпните для просмотра →
     </div>
   )}
   ```

2. **Автоскролл к сегодня/завтра:**
   ```tsx
   useEffect(() => {
     const todayElement = document.querySelector('[data-is-today="true"]')
     todayElement?.scrollIntoView({ inline: 'center', behavior: 'smooth' })
   }, [repeatMenuOpen])
   ```

3. **Добавить тактильную обратную связь:**
   ```tsx
   onClick={() => {
     if ('vibrate' in navigator) navigator.vibrate(10)
     // ... existing code
   }}
   ```

---

**Итог:** Жалоба пользователя решена. UI выбора дат теперь современный, удобный и соответствует ожиданиям пользователей мобильных приложений. 🎉

