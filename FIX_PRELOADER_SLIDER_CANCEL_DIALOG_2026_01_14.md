# Исправление: Прелоадер, ползунок баллов и диалог отмены - 2026-01-14

## 🐛 Проблемы:

### 1. Прелоадер - долгая загрузка логотипа
**Проблема:**
- Логотип в прелоадере долго грузится и выглядит плохо
- Нужно уменьшить вес изображения

### 2. Ползунок выбора баллов - нет визуальной полоски
**Проблема:**
- Ползунок выбора количества баллов не имеет визуальной полоски прогресса
- Пользователю сложно понять, сколько баллов выбрано

### 3. Диалог отмены заказа - крестик вместо кнопок Да/Нет
**Проблема:**
- В диалоге "Прекратить оформление заказа?" есть только крестик для закрытия
- Нужны кнопки "Да" и "Нет" для явного выбора

## ✅ Решение:

### 1. Оптимизация прелоадера

**Изменения в `components/preloader.tsx`:**
- Уменьшен размер логотипа с 128x128 до 80x80 пикселей
- Уменьшен размер контейнера с h-24/h-32 до h-20/h-24
- Снижено качество изображения с 100% до 70%
- Добавлен `loading="eager"` для приоритетной загрузки
- Убран blur placeholder (не нужен для прелоадера)

```typescript
<div className="h-20 w-20 sm:h-24 sm:w-24 rounded-full border-4 border-black overflow-hidden flex items-center justify-center shadow-brutal bg-[#FFEA00]">
  <Image
    src="/logo-small.png"
    alt="OGFooDY Logo"
    width={80}
    height={80}
    className="rounded-full object-cover"
    priority
    quality={70}
    loading="eager"
  />
</div>
```

**Результат:**
- ✅ Логотип загружается быстрее (меньший размер файла)
- ✅ Меньше потребление памяти
- ✅ Более плавное отображение

### 2. Визуальная полоска на ползунке

**Изменения в `components/payment-modal.tsx`:**
- Добавлена визуальная полоска прогресса под ползунком
- Полоска показывает процент выбранных баллов
- Полоска показывается только если выбраны баллы (pointsToUse > 0)
- Плавная анимация при изменении значения

```typescript
{pointsToUse > 0 && maxPointsToUse > 0 && (
  <div 
    className="absolute top-1/2 left-1 -translate-y-1/2 h-2 bg-primary/40 rounded-full pointer-events-none z-0"
    style={{ 
      width: `calc(${((pointsToUse / maxPointsToUse) * 100)}% - 8px)`,
      transition: 'width 0.2s ease-out'
    }}
  />
)}
```

**Результат:**
- ✅ Пользователь видит визуальную индикацию выбранных баллов
- ✅ Полоска плавно изменяется при движении ползунка
- ✅ Улучшен UX - понятно, сколько баллов выбрано

### 3. Диалог отмены заказа с кнопками Да/Нет

**Создан новый компонент `components/cancel-payment-dialog.tsx`:**
- Убран крестик для закрытия
- Добавлены две кнопки: "Нет" (отмена) и "Да" (подтверждение)
- Кнопка "Нет" - белая с черной рамкой
- Кнопка "Да" - желтая в стиле проекта
- Диалог нельзя закрыть кликом вне области

**Изменения в `app/page.tsx`:**
- Заменен `WarningDialog` на `CancelPaymentDialog` для отмены заказа
- Добавлена функция `handleCancelPaymentDialogCancel` для обработки "Нет"
- Функция `handleConfirmCancelPayment` вызывается при нажатии "Да"

```typescript
<CancelPaymentDialog
  open={cancelPaymentDialog.open}
  onConfirm={handleConfirmCancelPayment}
  onCancel={handleCancelPaymentDialogCancel}
  title="Прекратить оформление заказа?"
  description="Заказ будет удален без возможности восстановления. Вы уверены?"
/>
```

**Результат:**
- ✅ Пользователь явно выбирает "Да" или "Нет"
- ✅ Нет возможности случайно закрыть диалог
- ✅ Более понятный UX

## 🎯 Результат:

**После исправления:**

1. **Прелоадер:**
   - ✅ Логотип загружается быстрее
   - ✅ Меньший размер файла
   - ✅ Более плавное отображение

2. **Ползунок баллов:**
   - ✅ Визуальная полоска показывает выбранные баллы
   - ✅ Плавная анимация при изменении
   - ✅ Улучшен UX

3. **Диалог отмены:**
   - ✅ Кнопки "Да" и "Нет" вместо крестика
   - ✅ Явный выбор пользователя
   - ✅ Невозможно случайно закрыть

**Файлы:**
- `components/preloader.tsx` (изменен)
- `components/payment-modal.tsx` (изменен)
- `components/cancel-payment-dialog.tsx` (новый)
- `app/page.tsx` (изменен)
