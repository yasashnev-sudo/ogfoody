# Результаты тестирования системы начисления баллов

## Дата тестирования
2026-01-06

## Результаты тестов

### Тест 1: Полный цикл заказа
✅ **УСПЕХ**: Баллы начисляются правильно при создании заказа
- Заказ на 1000 руб. → начислено 30 баллов (3% кэшбэк)
- Пользователь обновлен корректно

### Тест 2: Детальное тестирование (несколько заказов подряд)
⚠️ **ПРОБЛЕМА**: Обнаружены аномалии при последовательных заказах

#### Проблемы:
1. **Первый заказ**: Баллы уменьшаются вместо увеличения
   - Заказ на 500 руб. → ожидалось +15, получено -45
   - Возможная причина: неправильное обновление `total_spent` или двойной вызов функции

2. **Последующие заказы**: Начисляется только половина ожидаемых баллов
   - Заказ на 1000 руб. → ожидалось +30, получено +15 (50%)
   - Заказ на 2000 руб. → ожидалось +60, получено +30 (50%)

## Анализ проблемы

### Возможные причины:
1. **Обновление `total_spent` влияет на расчет следующего заказа**
   - `total_spent` обновляется ДО расчета баллов следующего заказа
   - Это может влиять на уровень кэшбэка (bronze/silver/gold)

2. **Двойной вызов функции `awardLoyaltyPoints`**
   - Функция может вызываться дважды при создании заказа
   - Или вызываться при создании и при оплате

3. **Проблема с расчетом `currentTotalSpent`**
   - `currentTotalSpent` может включать предыдущие заказы неправильно
   - Или обновляться асинхронно

## Рекомендации

1. **Проверить логи сервера** при создании заказа:
   - Логи `🔍 Проверка начисления баллов`
   - Логи `📊 Данные для расчета баллов`
   - Логи `💰 Рассчитано баллов`
   - Логи `💾 Создание транзакции`
   - Логи `💳 Обновление баланса пользователя`

2. **Добавить защиту от двойного вызова**:
   - Проверять, не были ли баллы уже начислены для этого заказа
   - Использовать флаг или проверку `loyalty_points_earned` в заказе

3. **Проверить порядок операций**:
   - Убедиться, что `total_spent` обновляется ПОСЛЕ расчета баллов
   - Или использовать `currentTotalSpent` БЕЗ учета текущего заказа

4. **Проверить транзакции в базе данных**:
   - Убедиться, что транзакции создаются правильно
   - Проверить, нет ли дублирующих транзакций

## Следующие шаги

1. Проверить логи сервера при создании реального заказа через интерфейс
2. Добавить проверку на дублирование начисления баллов
3. Исправить логику обновления `total_spent` если необходимо
4. Протестировать с реальным пользователем через интерфейс





