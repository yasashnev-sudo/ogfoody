# 🔧 Исправление: Переменные окружения только для Preview

## Проблема

Переменные окружения установлены только для **Preview** окружения, а не для **Production**. Поэтому они не работают в Production деплое.

## Решение

### Вариант 1: Изменить существующие переменные (рекомендуется)

1. В Vercel Dashboard → ваш проект → **Settings** → **Environment Variables**
2. Для **каждой переменной** (NOCODB_URL, NOCODB_TOKEN, и все NOCODB_TABLE_*):
   - Нажмите на **три точки (⋯)** справа от переменной
   - Выберите **Edit**
   - В разделе **Environments** выберите:
     - ✅ **Production** (для Production деплоев)
     - ✅ **Preview** (уже выбрано)
     - ✅ **Development** (опционально)
   - Или выберите **All Environments** чтобы применить ко всем
   - Нажмите **Save**

### Вариант 2: Удалить и создать заново

1. Удалите все переменные (или только те, что помечены как "Preview")
2. Создайте их заново, выбрав **"All Environments"** при создании

## После исправления

1. **ОБЯЗАТЕЛЬНО пересоберите Production деплой:**
   - Vercel Dashboard → **Deployments**
   - Найдите Production деплой (обычно помечен как "Production")
   - Нажмите на **три точки (⋯)** → **Redeploy**
   - Выберите **Use existing Build Cache** = OFF (чтобы пересобрать с новыми переменными)

2. **Проверьте результат:**
   - Откройте `https://ваш-домен.vercel.app/api/diagnose`
   - Должно показать `"configured": true` и `"vercelEnv": "production"`

## Как проверить, что переменные установлены для Production

1. В списке переменных окружения посмотрите на метки под каждой переменной
2. Должны быть метки: **Production**, **Preview** (не только Preview!)
3. Если видите только "Preview" - переменная не будет работать в Production

## Визуальная подсказка

В Vercel Dashboard переменные должны выглядеть так:
```
NOCODB_URL
Preview  Production  ← должны быть обе метки!
```

А не так:
```
NOCODB_URL
Preview  ← только Preview - НЕ РАБОТАЕТ в Production!
```






