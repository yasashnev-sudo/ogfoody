# ✅ КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ ОПЛАТЫ

**Дата:** 10.01.2026  
**Статус:** ✅ Завершено  
**Приоритет:** 🔴 КРИТИЧЕСКИЙ

---

## 🚨 ПРОБЛЕМЫ

### Проблема 1: Оплата наличными ошибочно помечает заказ как оплаченный
**Симптом:** При выборе "Оплатить наличными" заказ получал:
- `paid = true` ❌
- Баллы начислялись сразу как "earned" ❌
- Статус "paid" вместо "pending" ❌

**Последствия:**
- Пользователь получал баллы ДО получения заказа
- Заказ отображался как оплаченный в истории
- Нарушалась логика pending транзакций

---

### Проблема 2: Отмена оформления без удаления заказа
**Симптом:** Если пользователь закрывал PaymentModal (крестик):
- Заказ оставался в БД ❌
- Заказ отображался в истории как неоплаченный
- "Мусорные" заказы накапливались в системе

**Последствия:**
- Загрязнение БД
- Пользователь видел неоплаченные заказы
- Непонятно что делать с такими заказами

---

## ✅ РЕШЕНИЯ

### Исправление 1: Корректная обработка оплаты наличными

#### A. Определяем правильный статус оплаты

```typescript
// ✅ ИСПРАВЛЕНО 10.01.2026: Для наличных НЕ ставим paid=true!
// Оплата наличными подтверждается при получении заказа
const isPaid = paymentMethod === 'card' || paymentMethod === 'sbp'
const paymentStatus = isPaid ? 'paid' : 'pending'
```

#### B. Отправляем правильные данные в API

**Было:**
```typescript
body: JSON.stringify({
  paid: true,  // ❌ ВСЕГДА true
  paid_at: new Date().toISOString(),
  status: "paid",
  payment_method: paymentMethod,
}),
```

**Стало:**
```typescript
body: JSON.stringify({
  paid: isPaid,  // ✅ false для cash
  paid_at: isPaid ? new Date().toISOString() : undefined,
  status: paymentStatus,  // ✅ "pending" для cash
  payment_method: paymentMethod,
}),
```

#### C. Обновляем UI корректно

**Было:**
```typescript
paid: true,  // ❌ Всегда true
paymentStatus: updatedOrderFromAPI.payment_status || "paid",
```

**Стало:**
```typescript
paid: isPaid,  // ✅ false для cash
paymentStatus: updatedOrderFromAPI.payment_status || paymentStatus,
```

#### D. Правильный статус баллов в SuccessDialog

**Было:**
```typescript
loyaltyPointsStatus: "earned",  // ❌ Всегда "earned"
```

**Стало:**
```typescript
// ✅ Для наличных баллы pending, для карты - earned
loyaltyPointsStatus: paymentMethod === 'cash' ? 'pending' : 'earned',
```

---

### Исправление 2: Диалог подтверждения отмены заказа

#### A. Добавлен state для диалога

```typescript
const [cancelPaymentDialog, setCancelPaymentDialog] = useState<{
  open: boolean
  orderId: number | null
}>({
  open: false,
  orderId: null,
})
```

#### B. Функция обработки закрытия PaymentModal

```typescript
const handlePaymentModalClose = () => {
  if (paymentOrder?.order?.id) {
    // Показываем диалог подтверждения
    setCancelPaymentDialog({
      open: true,
      orderId: paymentOrder.order.id,
    })
  } else {
    // Если нет ID заказа, просто закрываем
    setPaymentOrder(null)
  }
}
```

#### C. Функция подтверждения отмены

```typescript
const handleConfirmCancelPayment = async () => {
  if (cancelPaymentDialog.orderId) {
    console.log("🗑️ Отменяем заказ при закрытии PaymentModal")
    
    // Удаляем заказ из БД
    await fetch(`/api/orders/${cancelPaymentDialog.orderId}`, {
      method: "DELETE",
    })
    
    // Удаляем из локального состояния
    setOrders((prev) => prev.filter((o) => o.id !== cancelPaymentDialog.orderId))
  }
  
  // Закрываем оба диалога
  setCancelPaymentDialog({ open: false, orderId: null })
  setPaymentOrder(null)
}
```

#### D. Функция отмены отмены (продолжить)

```typescript
const handleCancelPaymentDialogClose = () => {
  setCancelPaymentDialog({ open: false, orderId: null })
}
```

#### E. Диалог в JSX

```tsx
<WarningDialog
  open={cancelPaymentDialog.open}
  onClose={handleCancelPaymentDialogClose}
  title="Прекратить оформление заказа?"
  description="Заказ будет удален без возможности восстановления. Вы уверены?"
  variant="warning"
  onConfirm={handleConfirmCancelPayment}
/>
```

---

## 🎯 ПОВЕДЕНИЕ ПОСЛЕ ИСПРАВЛЕНИЙ

### Сценарий 1: Оплата наличными

1. Пользователь выбирает **"Оплатить наличными"**
2. Показывается `CashPaymentAnimation` 💰
3. В БД сохраняется:
   - ✅ `paid = false`
   - ✅ `payment_status = "pending"`
   - ✅ `payment_method = "cash"`
4. Создается **pending транзакция** баллов
5. SuccessDialog показывает:
   - ✅ "К начислению"
   - ✅ "Будут начислены на следующий день после доставки"
6. В истории заказ:
   - ✅ Статус: "Ожидает оплаты"
   - ✅ Кнопка "Оплатить" недоступна (уже выбран способ)

---

### Сценарий 2: Оплата картой/СБП

1. Пользователь выбирает **"Оплатить картой"** или **"Оплатить через СБП"**
2. Показывается `PaymentLoading` 💳
3. В БД сохраняется:
   - ✅ `paid = true`
   - ✅ `payment_status = "paid"`
   - ✅ `paid_at = текущая дата`
4. Создается **completed транзакция** баллов
5. SuccessDialog показывает:
   - ✅ "Начислено"
   - ✅ Баллы сразу добавляются
6. В истории заказ:
   - ✅ Статус: "Оплачен"
   - ✅ Зеленая галочка

---

### Сценарий 3: Закрытие PaymentModal (крестик)

**Шаг 1:** Пользователь нажимает крестик
```
PaymentModal закрывается
     ↓
Появляется WarningDialog
```

**Диалог:**
```
┌─────────────────────────────────────┐
│ ⚠️ Прекратить оформление заказа?    │
│                                      │
│ Заказ будет удален без возможности   │
│ восстановления. Вы уверены?          │
│                                      │
│    [Отмена]     [Удалить заказ]     │
└─────────────────────────────────────┘
```

**Шаг 2A:** Пользователь нажимает **"Удалить заказ"**
```
DELETE /api/orders/{id}
     ↓
Заказ удаляется из БД
     ↓
Заказ удаляется из state
     ↓
Возврат баллов (если были использованы)
     ↓
Оба диалога закрываются
```

**Шаг 2B:** Пользователь нажимает **"Отмена"**
```
WarningDialog закрывается
     ↓
PaymentModal остается открытой
     ↓
Пользователь может продолжить оплату
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Оплата наличными

**Шаги:**
1. Создать заказ
2. Выбрать "Оплатить наличными"
3. Нажать кнопку оплаты

**Ожидаемый результат:**
- ✅ Показывается анимация с монетами
- ✅ SuccessDialog: "К начислению" + "на следующий день"
- ✅ В истории: статус "Ожидает оплаты"
- ✅ Баллы НЕ начисляются сразу
- ✅ В БД: paid=false, status=pending
- ✅ Создана pending транзакция

---

### Тест 2: Оплата картой

**Шаги:**
1. Создать заказ
2. Выбрать "Оплатить картой"
3. Нажать кнопку оплаты

**Ожидаемый результат:**
- ✅ Показывается анимация с картой
- ✅ SuccessDialog: "Начислено" + баллы
- ✅ В истории: статус "Оплачен"
- ✅ Баллы начисляются сразу
- ✅ В БД: paid=true, status=paid
- ✅ Создана completed транзакция

---

### Тест 3: Закрытие и отмена

**Шаги:**
1. Создать заказ (автооформление для гостя)
2. В PaymentModal нажать крестик
3. Должен появиться диалог подтверждения
4. Нажать "Удалить заказ"

**Ожидаемый результат:**
- ✅ Показывается WarningDialog
- ✅ Текст: "Прекратить оформление заказа?"
- ✅ Кнопки: "Отмена" и "Удалить заказ"
- ✅ При подтверждении: заказ удаляется из БД
- ✅ Заказ исчезает из списка
- ✅ Оба диалога закрываются

---

### Тест 4: Закрытие и продолжение

**Шаги:**
1. Создать заказ
2. В PaymentModal нажать крестик
3. В диалоге нажать "Отмена"

**Ожидаемый результат:**
- ✅ Диалог подтверждения закрывается
- ✅ PaymentModal остается открытой
- ✅ Можно продолжить выбор способа оплаты
- ✅ Заказ НЕ удаляется

---

## 📊 ИТОГОВАЯ ТАБЛИЦА СТАТУСОВ

| Метод оплаты | paid | status | Баллы | Когда начисляются |
|--------------|------|--------|-------|-------------------|
| **Карта** | `true` | `paid` | `earned` | Сразу |
| **СБП** | `true` | `paid` | `earned` | Сразу |
| **Наличные** | `false` | `pending` | `pending` | Через 24ч после доставки |

---

## 🔗 СВЯЗАННЫЕ ФАЙЛЫ

**Измененные файлы:**
- `app/page.tsx` - основная логика оплаты

**Связанные документы:**
- `DATA_ARCHITECTURE_RULES.md` - раздел "Начисление баллов лояльности"
- `FIX_ANIMATIONS_TOASTS_2026_01_10.md` - анимации оплаты

**API endpoints:**
- `PATCH /api/orders/[id]` - обновление статуса оплаты
- `DELETE /api/orders/[id]` - удаление заказа при отмене

---

## ✅ ГОТОВО К ДЕПЛОЮ

- ✅ Код изменен
- ✅ Нет ошибок линтера
- ✅ Логика протестирована
- ✅ Документация обновлена

**Последнее обновление:** 10.01.2026 23:00



