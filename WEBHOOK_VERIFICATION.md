# ✅ Как проверить, что webhook работает

## 🔍 Быстрая проверка

### 1. Проверка доступности endpoint

Откройте в браузере или через curl:

```bash
# Проверка конфигурации
curl https://ogfoody.ru/api/payments/yookassa/webhook/check

# Проверка основного endpoint
curl https://ogfoody.ru/api/payments/yookassa/webhook
```

Оба должны вернуть JSON с информацией о конфигурации.

### 2. Проверка через браузер

Откройте:
- **Конфигурация**: https://ogfoody.ru/api/payments/yookassa/webhook/check
- **Основной endpoint**: https://ogfoody.ru/api/payments/yookassa/webhook

### 3. Проверка логов на сервере

```bash
# Подключитесь к серверу
ssh root@5.129.194.168

# Проверьте последние логи webhook
pm2 logs ogfoody --lines 200 | grep -i "webhook\|yookassa\|payment"

# Или все логи за последние 5 минут
pm2 logs ogfoody --lines 500 | tail -100
```

## 📋 Что искать в логах

### ✅ Если webhook работает:

Вы увидите:
```
🔍 Webhook request details: { clientIp: '...', ... }
📥 YooKassa webhook received: { type: 'payment.succeeded', ... }
✅ Payment succeeded for order 1062
💎 Awarding loyalty points for order 1062
```

### ❌ Если webhook не приходит:

1. **Проверьте настройки в личном кабинете ЮKassa:**
   - URL должен быть: `https://ogfoody.ru/api/payments/yookassa/webhook`
   - Должны быть подписаны события: `payment.succeeded`, `payment.canceled`

2. **Проверьте, что платеж создан:**
   ```bash
   # В логах должно быть:
   ✅ YooKassa payment created: { paymentId: '...', orderId: 1062, ... }
   ```

3. **Проверьте статус платежа в ЮKassa:**
   - Откройте https://yookassa.ru/my/payments
   - Найдите платеж по `payment_id`
   - Проверьте статус и историю уведомлений

## 🧪 Тестирование webhook

### Шаг 1: Создайте тестовый платеж

1. Откройте https://ogfoody.ru
2. Создайте заказ
3. Выберите "Оплатить сейчас"
4. Используйте тестовую карту: `5555555555554444`
5. Завершите оплату

### Шаг 2: Проверьте логи

```bash
ssh root@5.129.194.168
pm2 logs ogfoody --lines 100 | grep -E "webhook|payment|YooKassa"
```

### Шаг 3: Проверьте статус заказа

```bash
# Через API
curl https://ogfoody.ru/api/orders/1062 | jq '.order.paid'

# Должно быть: true
```

## 🔧 Диагностика проблем

### Проблема: Webhook не приходит

**Причины:**
1. Webhook не настроен в личном кабинете ЮKassa
2. Неправильный URL
3. Проблемы с SSL-сертификатом
4. Блокировка на уровне сервера/файрвола

**Решение:**
1. Проверьте настройки в https://yookassa.ru/my
2. Убедитесь, что URL правильный: `https://ogfoody.ru/api/payments/yookassa/webhook`
3. Проверьте логи сервера на наличие входящих запросов
4. В тестовом режиме проверка IP менее строгая

### Проблема: Webhook приходит, но заказ не обновляется

**Признаки в логах:**
```
📥 YooKassa webhook received: { ... }
❌ Order 1062 not found
```

**Решение:**
- Проверьте, что `orderId` передается в `metadata` при создании платежа
- Проверьте логи создания платежа

### Проблема: IP блокируется

**Признаки в логах:**
```
⚠️ Test mode: IP validation failed, but allowing request: <IP>
```

**Решение:**
- В тестовом режиме это нормально - запрос все равно обрабатывается
- В продакшене проверка IP будет строже

## 📊 Мониторинг

### Регулярная проверка

1. **Ежедневно:** Проверяйте логи на наличие ошибок webhook
2. **После каждого платежа:** Убедитесь, что статус заказа обновляется
3. **При проблемах:** Используйте `/api/payments/yookassa/webhook/check` для диагностики

### Автоматическая проверка

Можно настроить мониторинг через cron:

```bash
# Проверка доступности endpoint каждые 5 минут
*/5 * * * * curl -f https://ogfoody.ru/api/payments/yookassa/webhook/check > /dev/null || echo "Webhook endpoint недоступен" | mail -s "Alert" admin@example.com
```

## 🔗 Полезные ссылки

- **Проверка конфигурации**: https://ogfoody.ru/api/payments/yookassa/webhook/check
- **Основной endpoint**: https://ogfoody.ru/api/payments/yookassa/webhook
- **Личный кабинет ЮKassa**: https://yookassa.ru/my
- **История платежей**: https://yookassa.ru/my/payments

## ✅ Чек-лист проверки

- [ ] Endpoint доступен: `curl https://ogfoody.ru/api/payments/yookassa/webhook/check`
- [ ] Webhook настроен в личном кабинете ЮKassa
- [ ] URL правильный: `https://ogfoody.ru/api/payments/yookassa/webhook`
- [ ] Подписаны события: `payment.succeeded`, `payment.canceled`
- [ ] Создан тестовый платеж
- [ ] В логах видно: `📥 YooKassa webhook received`
- [ ] Статус заказа обновляется после платежа
- [ ] Баллы лояльности начисляются (если применимо)
