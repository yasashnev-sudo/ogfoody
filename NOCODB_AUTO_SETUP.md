# Автоматическое создание таблиц NocoDB

## 🚀 Быстрый старт

Вы можете автоматически создать все необходимые таблицы в NocoDB одним запросом!

### Шаг 1: Запустите сервер разработки

```bash
npm run dev
```

### Шаг 2: Создайте таблицы

Откройте в браузере или выполните curl:

```bash
# Через браузер
http://localhost:3000/api/db/setup-tables

# Или через curl
curl -X POST http://localhost:3000/api/db/setup-tables
```

### Шаг 3: Обновите переменные окружения

После создания таблиц вы получите ответ с новыми ID таблиц. Скопируйте их и обновите переменные окружения:

**Для локальной разработки (.env):**
```env
NOCODB_TABLE_MEALS=<новый_id>
NOCODB_TABLE_EXTRAS=<новый_id>
NOCODB_TABLE_DELIVERY_ZONES=<новый_id>
NOCODB_TABLE_USERS=<новый_id>
NOCODB_TABLE_ORDERS=<новый_id>
NOCODB_TABLE_ORDER_PERSONS=<новый_id>
NOCODB_TABLE_ORDER_MEALS=<новый_id>
NOCODB_TABLE_ORDER_EXTRAS=<новый_id>
NOCODB_TABLE_PROMO_CODES=<новый_id>
NOCODB_TABLE_REVIEWS=<новый_id>
```

**Для Vercel:**
1. Зайдите в Vercel Dashboard → ваш проект → Settings → Environment Variables
2. Обновите каждую переменную `NOCODB_TABLE_*` с новыми ID
3. Пересоберите проект (Redeploy)

## 📋 Что делает этот endpoint?

1. **Проверяет существующие таблицы** - если таблица уже существует, использует её ID
2. **Создает недостающие таблицы** - автоматически создает таблицы с правильной структурой
3. **Настраивает колонки** - добавляет все необходимые колонки с правильными типами данных
4. **Возвращает ID таблиц** - предоставляет все ID для обновления переменных окружения

## 📊 Создаваемые таблицы

Endpoint автоматически создаст следующие таблицы:

1. **Meals** - Блюда меню
2. **Extras** - Дополнения к заказам
3. **Delivery_Zones** - Зоны доставки
4. **Users** - Пользователи
5. **Orders** - Заказы
6. **Order_Persons** - Персоны в заказе
7. **Order_Meals** - Блюда в заказе
8. **Order_Extras** - Дополнения в заказе
9. **Promo_Codes** - Промокоды
10. **Reviews** - Отзывы

## ✅ Проверка результата

После создания таблиц проверьте их структуру:

```bash
# Проверить структуру всех таблиц
curl http://localhost:3000/api/db/check-structure

# Или откройте в браузере
http://localhost:3000/api/db/check-structure
```

## 🔄 Повторное использование

Endpoint безопасен для повторного использования:
- Если таблица уже существует, она не будет пересоздана
- Используется существующий ID таблицы
- Можно запускать несколько раз без проблем

## ⚠️ Важные замечания

1. **Требуется NOCODB_BASE_ID** - убедитесь, что установлена переменная `NOCODB_PROJECT_ID` или `NOCODB_BASE_ID`
2. **Требуется NOCODB_TOKEN** - нужен токен доступа к NocoDB
3. **Требуется NOCODB_URL** - URL вашего NocoDB инстанса

## 🐛 Решение проблем

### Ошибка "NocoDB not configured"
- Проверьте, что установлены `NOCODB_URL` и `NOCODB_TOKEN`

### Ошибка "NOCODB_BASE_ID not configured"
- Установите `NOCODB_PROJECT_ID` или `NOCODB_BASE_ID` в переменных окружения

### Таблицы не создаются
- Проверьте права доступа токена
- Убедитесь, что NocoDB доступен по указанному URL
- Проверьте логи сервера для деталей ошибок

## 📝 Пример ответа

```json
{
  "success": true,
  "summary": {
    "total": 10,
    "created": 10,
    "errors": 0,
    "existing": 0
  },
  "results": {
    "Meals": {
      "success": true,
      "tableId": "mmtctn4flssh2ua"
    },
    ...
  },
  "createdTableIds": {
    "Meals": "mmtctn4flssh2ua",
    "Extras": "mksy21hmttmo855",
    ...
  },
  "envVars": "NOCODB_TABLE_MEALS=mmtctn4flssh2ua\n..."
}
```

## 🎯 Следующие шаги

1. Обновите переменные окружения с новыми ID
2. Проверьте структуру таблиц через `/api/db/check-structure`
3. Начните использовать таблицы в вашем приложении!






