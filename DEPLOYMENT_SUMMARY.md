# ✅ Деплой админ-панели завершен!

## 🎉 Что сделано:

1. ✅ **Код задеплоен** на сервер `ogfoody.ru`
2. ✅ **Таблицы созданы** в NocoDB:
   - `Messages` - для чата с клиентами
   - `Push_Notifications` - для пуш-уведомлений
3. ✅ **Приложение перезапущено** и работает

## 📝 Что нужно сделать вручную:

### 1. Получить Table ID из NocoDB

1. Откройте https://noco.povarnakolesah.ru
2. Войдите в базу данных **FooD**
3. Найдите таблицы:
   - **Messages**
   - **Push_Notifications**
4. Откройте каждую таблицу
5. Скопируйте Table ID из URL (например: `md_xxxxx`)

### 2. Добавить Table ID в .env.production

На сервере выполните:

```bash
ssh root@5.129.194.168
cd /var/www/ogfoody
nano .env.production
```

Добавьте строки (замените `md_xxxxx` на реальные ID):

```env
NOCODB_TABLE_MESSAGES=md_xxxxx
NOCODB_TABLE_PUSH_NOTIFICATIONS=md_xxxxx
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 3. Перезапустить приложение

```bash
pm2 restart ogfoody
```

## 🌐 Доступ к админ-панели:

- **URL**: https://ogfoody.ru/admin/login
- **Пароль по умолчанию**: `admin123`

Для изменения пароля установите переменную окружения:
```bash
NEXT_PUBLIC_ADMIN_PASSWORD=ваш_новый_пароль
```

## 📋 Функциональность админ-панели:

- ✅ Дашборд со статистикой
- ✅ Управление заказами
- ✅ Управление пользователями
- ✅ Управление блюдами
- ✅ Чат с клиентами (после настройки Table ID)
- ✅ Пуш-уведомления (после настройки Table ID)
- ✅ Управление промокодами
- ✅ Статистика и аналитика

## 🔧 Если что-то не работает:

1. Проверьте логи: `pm2 logs ogfoody`
2. Проверьте что Table ID добавлены в `.env.production`
3. Проверьте что таблицы существуют в NocoDB
4. Перезапустите приложение: `pm2 restart ogfoody`

## 📚 Документация:

Полная документация по настройке: `ADMIN_PANEL_SETUP.md`
