# 📊 Статус деплоя исправлений инкремента промокода

**Дата:** 2026-01-15  
**Время:** $(date)

---

## ✅ Что уже сделано

1. ✅ Все исправления закоммичены локально
2. ✅ Изменения запушены в GitHub (`main` ветка)
3. ✅ Тест переделан для локального запуска
4. ✅ Добавлены npm скрипты для тестирования

**Последние коммиты:**
- `307e647f` - 📦 Add deployment script and instructions for increment fix
- `c498fbde` - 🔧 Fix promo code increment: improve payment condition check, fix fetchPromoCode, fix incrementPromoCodeUsage, add comprehensive debug logging

---

## ⚠️ Требуется ручной деплой

SSH требует аутентификацию (пароль или ключ), поэтому автоматический деплой через SSH невозможен.

### Вариант 1: Через GitHub Actions (если настроен)

1. Откройте: https://github.com/yasashnev-sudo/ogfoody/actions
2. Найдите последний workflow run
3. Если он не запустился автоматически, нажмите "Run workflow" → "Run workflow"

### Вариант 2: Ручной деплой через SSH

Выполните команды вручную:

```bash
ssh root@5.129.194.168

# После подключения:
cd /root/my-project  # или /var/www/ogfoody
git pull origin main
npm install
npm run build
pm2 restart all
pm2 logs --lines 50
```

---

## 🧪 Тестирование после деплоя

После деплоя запустите тест:

```bash
npm run test:fixes:prod
```

Или локально (если сервер запущен):

```bash
# Терминал 1
npm run dev

# Терминал 2  
npm run test:fixes:local
```

---

## 📊 Проверка логов

После деплоя проверьте логи на сервере:

```bash
ssh root@5.129.194.168
pm2 logs --lines 100 | grep -E "(Проверка инкремента|fetchPromoCode|incrementPromoCodeUsage)"
```

---

**Следующий шаг:** Выполните ручной деплой через SSH (Вариант 2) или проверьте GitHub Actions.
