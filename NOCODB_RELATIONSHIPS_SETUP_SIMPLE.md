# Простая инструкция по настройке связей в NocoDB

## ⚠️ Важно
Автоматическая настройка связей через API не работает из-за сложности формата API NocoDB. 
Настройте связи вручную через интерфейс NocoDB - это займет 5-10 минут.

## 📌 Важно о типах связей в NocoDB
В NocoDB нет опции "Many to One" в интерфейсе. Вместо этого:
- Выберите **"Has Many"** - это создаст связь "Many to One" с обратной стороны
- Или просто выберите таблицу для связи - NocoDB автоматически определит правильный тип связи
- **НЕ выбирайте "Many to Many"** - это для связей многие-ко-многим

## 📋 Список связей для настройки

### 1. Orders → Users
- **Таблица**: `Orders`
- **Колонка**: `user_id`
- **Связанная таблица**: `Users`
- **Связанная колонка**: `Id`

**Шаги:**
1. Откройте таблицу `Orders` в NocoDB
2. Найдите колонку `user_id`
3. Нажмите на заголовок колонки → "Edit Column" (Изменить колонку)
4. Измените тип на **"Link to another record"** (Связанная запись)
5. В разделе "Links":
   - В поле "select table to link" выберите таблицу `Users`
   - **НЕ выбирайте "Many to Many"** - оставьте по умолчанию или выберите **"Has Many"** (это создаст связь Many to One с обратной стороны)
6. Нажмите **"Save Field"**

### 2. Order_Persons → Orders
- **Таблица**: `Order_Persons`
- **Колонка**: `order_id`
- **Связанная таблица**: `Orders`
- **Связанная колонка**: `Id`

**Шаги:** Аналогично пункту 1, но для колонки `order_id` в таблице `Order_Persons`, связываем с таблицей `Orders`

### 3. Order_Meals → Order_Persons
- **Таблица**: `Order_Meals`
- **Колонка**: `order_person_id`
- **Связанная таблица**: `Order_Persons`
- **Связанная колонка**: `Id`

**Шаги:** Аналогично пункту 1, но для колонки `order_person_id` в таблице `Order_Meals`, связываем с таблицей `Order_Persons`

### 4. Order_Meals → Meals
- **Таблица**: `Order_Meals`
- **Колонка**: `meal_id` (или `Meals__2_id` если уже есть связь)
- **Связанная таблица**: `Meals`
- **Связанная колонка**: `Id`

**Шаги:** Аналогично пункту 1, но для колонки `meal_id` в таблице `Order_Meals`, связываем с таблицей `Meals`

### 5. Order_Extras → Orders
- **Таблица**: `Order_Extras`
- **Колонка**: `order_id`
- **Связанная таблица**: `Orders`
- **Связанная колонка**: `Id`

**Шаги:** Аналогично пункту 1, но для колонки `order_id` в таблице `Order_Extras`, связываем с таблицей `Orders`

### 6. Order_Extras → Extras
- **Таблица**: `Order_Extras`
- **Колонка**: `extra_id`
- **Связанная таблица**: `Extras`
- **Связанная колонка**: `Id`

**Шаги:** Аналогично пункту 1, но для колонки `extra_id` в таблице `Order_Extras`, связываем с таблицей `Extras`

### 7. Reviews → Orders
- **Таблица**: `Reviews`
- **Колонка**: `order_id`
- **Связанная таблица**: `Orders`
- **Связанная колонка**: `Id`

**Шаги:** Аналогично пункту 1, но для колонки `order_id` в таблице `Reviews`, связываем с таблицей `Orders`

## ✅ После настройки

После настройки всех связей:
- В таблице `Users` появится виртуальная колонка со всеми заказами пользователя
- В таблице `Orders` появится виртуальная колонка со всеми персонами заказа
- В таблице `Order_Persons` появится виртуальная колонка со всеми блюдами персоны
- И так далее для всех связей

## 🎯 Результат

Все связи будут работать автоматически, и вы сможете:
- Видеть все заказы пользователя в таблице Users
- Видеть все персоны заказа в таблице Orders
- Видеть все блюда персоны в таблице Order_Persons
- И так далее


