# ✅ ИСПРАВЛЕНО: Порядок модальных окон для гостя при оформлении заказа

**Дата:** 2026-01-11  
**Проблема:** После создания заказа гость сразу видел форму авторизации (телефон), пропуская выбор района.

---

## 🐛 Описание проблемы

### Что происходило:

Для **гостя** при оформлении заказа:
1. Гость создает заказ в OrderModal ❌
2. Система сразу открывает **AuthModal** (запрос телефона) ❌  
   _(пропускается выбор района, даже если он не был выбран ранее)_
3. После авторизации открывается ProfileModal

### Правильный флоу для гостя:

1. Гость создает заказ в OrderModal ✅
2. Система открывает **DistrictSelectionModal** (выбор района) ✅
3. После выбора района открывается **AuthModal** (запрос телефона) ✅
4. После авторизации открывается **ProfileModal** (если профиль неполный) ✅

---

## 🔍 Причина

В функции `onRequestAuth` была логика, которая для гостя с сохраненным районом **пропускала** выбор района и сразу открывала AuthModal:

```typescript
// ❌ БЫЛО:
if (!hasDistrict) {
  setShowDistrictModal(true)
} else {
  // Район уже есть
  if (!isAuthenticated) {
    setShowAuthModal(true)  // ❌ Сразу AuthModal!
  }
}
```

Проблема: если гость уже выбирал район ранее (он сохранен в `localStorage.guest_district`), то при следующем заказе система считала, что район уже известен, и пропускала DistrictSelectionModal.

---

## ✅ Решение

### 1. Изменена логика в `onRequestAuth`

**Файл:** `app/page.tsx` (строки 2902-2944)

```typescript
// ✅ ИСПРАВЛЕНО: Для гостей ВСЕГДА показываем выбор района
if (!isAuthenticated) {
  console.log("👤 Гость → открываем выбор района")
  setSelectedDistrict(hasDistrict || "")  // Подставляем сохраненный район, если есть
  setShowDistrictModal(true)
} else if (!hasDistrict) {
  // Авторизован, но района нет
  console.log("⚠️ Авторизован, но район не выбран → DistrictSelectionModal")
  setShowDistrictModal(true)
} else {
  // Авторизован И район есть → проверяем профиль
  // ...
}
```

**Ключевые изменения:**
- Для **гостя** (`!isAuthenticated`) **ВСЕГДА** открываем DistrictSelectionModal
- Если у гостя уже есть сохраненный район, он будет **предзаполнен** в модальном окне (можно подтвердить или изменить)
- Для **авторизованного** пользователя показываем выбор района только если его нет

---

### 2. Изменена логика в `handleDistrictSelected`

**Файл:** `app/page.tsx` (строки 2494-2509)

```typescript
// После выбора района
setShowDistrictModal(false)

// ✅ ИСПРАВЛЕНО: Для гостя открываем AuthModal после выбора района
if (!currentUser) {
  console.log("👤 Гость выбрал район → открываем AuthModal")
  setShowAuthModal(true)
} else {
  // Для авторизованного - открываем обратно OrderModal
  console.log("✅ Авторизованный пользователь выбрал район → возвращаемся в OrderModal")
  setSelectedDate(toDate(pendingCheckout.order.startDate))
}
```

**Ключевые изменения:**
- Для **гостя**: после выбора района → открываем **AuthModal**
- Для **авторизованного**: после выбора района → возвращаемся в **OrderModal**

---

## 🎯 Итоговый флоу

### Для гостя (не авторизован):

```
1. OrderModal (выбор блюд)
   ↓ [Нажать "Оплатить"]
2. DistrictSelectionModal (выбор района)
   ↓ [Выбрать район → "Продолжить"]
3. AuthModal (ввод телефона + код)
   ↓ [Авторизоваться]
4. ProfileModal (заполнение имени, адреса)
   ↓ [Сохранить]
5. PaymentModal (выбор способа оплаты)
   ↓ [Оплатить]
6. SuccessOrderDialog (успешно!)
```

### Для авторизованного без района:

```
1. OrderModal (выбор блюд)
   ↓ [Нажать "Оплатить"]
2. DistrictSelectionModal (выбор района)
   ↓ [Выбрать район → "Продолжить"]
3. OrderModal (подтверждение заказа)
   ↓ [Оплатить]
4. ProfileModal (если профиль неполный)
   ↓ [Сохранить]
5. PaymentModal
   ↓ [Оплатить]
6. SuccessOrderDialog
```

### Для авторизованного с полным профилем:

```
1. OrderModal (выбор блюд + время + способ оплаты)
   ↓ [Нажать "Оплатить"]
2. PaymentModal (если card/sbp)
   ↓ [Оплатить]
3. SuccessOrderDialog
```

---

## 🧪 Как проверить

### Тест 1: Гость (первый раз)

1. Откройте сайт в **режиме инкогнито**
2. Выберите дату в календаре
3. Создайте заказ (добавьте блюда)
4. Нажмите "Оплатить"
5. **Ожидаемое:** Открывается выбор района ✅
6. Выберите район → "Продолжить"
7. **Ожидаемое:** Открывается форма авторизации (телефон) ✅
8. Введите телефон и код
9. **Ожидаемое:** Открывается форма профиля ✅

### Тест 2: Гость (повторный заказ)

1. Выйдите из аккаунта (или откройте в инкогнито, где уже выбирали район)
2. Создайте новый заказ
3. Нажмите "Оплатить"
4. **Ожидаемое:** Открывается выбор района (с предзаполненным районом) ✅
5. Подтвердите или измените район
6. **Ожидаемое:** Открывается форма авторизации ✅

### Тест 3: Авторизованный без района

1. Авторизуйтесь
2. Создайте заказ
3. Нажмите "Оплатить"
4. **Ожидаемое:** Открывается выбор района ✅
5. Выберите район
6. **Ожидаемое:** Возвращает в OrderModal для подтверждения ✅

---

## 📊 Логи для отладки

### Правильные логи для гостя:

```
🔐 Запрос оформления заказа: { isAuthenticated: false, hasDistrict: false }
👤 Гость → открываем выбор района
💾 Сохраняем выбранный район: Василеостровский район
✅ Район сохранен для гостя: Василеостровский район
👤 Гость выбрал район → открываем AuthModal
```

### Правильные логи для авторизованного:

```
🔐 Запрос оформления заказа: { isAuthenticated: true, hasDistrict: false }
⚠️ Авторизован, но район не выбран → открываем DistrictSelectionModal
💾 Сохраняем выбранный район: Центральный район
✅ Авторизованный пользователь выбрал район → возвращаемся в OrderModal
```

---

## 🔗 Связанные файлы

- `app/page.tsx` - основная логика флоу
  - `onRequestAuth` (строки 2902-2944)
  - `handleDistrictSelected` (строки 2448-2509)

---

## 📝 Commit Message

```
fix: порядок модальных окон для гостя при оформлении

- Для гостя теперь всегда показывается выбор района перед авторизацией
- После выбора района гость видит форму авторизации (телефон)
- Исправлено: гость пропускал выбор района и сразу видел форму телефона
- Для авторизованных без района также показывается выбор района
```

---

## ✅ Готово к использованию

Флоу теперь соответствует ожидаемому поведению: **Район → Телефон → Профиль → Оплата**


