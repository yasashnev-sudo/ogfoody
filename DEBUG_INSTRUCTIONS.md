# 🔍 Инструкции по отладке flow нового пользователя

**Дата**: 2026-01-09  
**Статус**: 🐛 ОТЛАДКА

---

## ✅ Что было добавлено

В код добавлены **детальные логи** для отслеживания каждого шага:

### Логи в useEffect (app/page.tsx):

```typescript
🔍 [useEffect] Проверка профиля сработала: {
  isAuthenticated: true/false,
  hasUserProfile: true/false,
  shouldAutoCheckout: true/false,
  hasPendingCheckout: true/false,
  userName: "...",
  userStreet: "...",
  userBuilding: "...",
  userDistrict: "..."
}
```

Если все условия выполнены:
```
🔍 Проверяем заполненность профиля после авторизации
⚠️ Профиль не заполнен, открываем ProfileModal
```

Если условия НЕ выполнены:
```
⚠️ [useEffect] Условия не выполнены, ProfileModal не открывается
```

---

## 🧪 Пошаговое тестирование

### Шаг 1: Подготовка

```bash
1. Откройте Chrome в режиме Инкогнито
2. Нажмите F12 (DevTools)
3. Перейдите на вкладку Console
4. Откройте http://localhost:3000
```

### Шаг 2: Выбор района

```bash
5. Должна показаться модалка выбора района
6. Выберите "Адмиралтейский район"
7. Нажмите "Продолжить"
```

**Проверьте логи в консоли:**
```
✅ Район сохранен для гостя: Адмиралтейский район
```

**Проверьте localStorage:**
```javascript
// Скопируйте и вставьте в консоль:
console.log('guest_district:', localStorage.getItem('guest_district'))
// Должно быть: "Адмиралтейский район"
```

### Шаг 3: Создание заказа

```bash
8. Выберите дату в календаре
9. Добавьте несколько блюд
10. Проверьте что внизу показывается "Доставка: 250 ₽"
```

**Проверьте логи:**
```
🗺️ Загружен район гостя: Адмиралтейский район
```

### Шаг 4: Нажатие "Заказать"

```bash
11. Нажмите кнопку "Заказать · XXXX ₽"
```

**КРИТИЧЕСКИЕ ЛОГИ - должны быть:**
```
🚀 Неавторизованный пользователь нажал 'Заказать': {...}
🔐 Запрос авторизации с заказом: { order: {...}, total: XXXX }
```

**Если этих логов НЕТ:**
- ❌ Проблема в OrderModal - onRequestAuth не вызывается
- Скопируйте ВСЕ логи и отправьте

### Шаг 5: Авторизация

```bash
12. Введите номер: +7 (999) 111-22-33
13. Нажмите "СМС-код"
14. Найдите в логах: "🔐 Демо-код для ..."
15. Скопируйте код (4 цифры)
16. Введите код и нажмите "Подтвердить"
```

**КРИТИЧЕСКИЕ ЛОГИ после ввода кода:**

```
1. 🔄 Синхронизация пользователя с базой данных...
2. 📡 Ищем пользователя в базе по телефону: 79991112233
3. ⚠️ Пользователя нет в базе, создаем нового...
4. ✅ Пользователь создан в базе данных: XX с районом: Адмиралтейский район
5. 💾 Профиль сохранен в localStorage с id: XX
6. 👤 Устанавливаем userProfile: { id: XX, phone: '...', hasId: true }
7. ✅ У пользователя уже есть район: Адмиралтейский район
```

### Шаг 6: Проверка useEffect (САМОЕ ВАЖНОЕ!)

**После шага 5 должны появиться логи:**

```
🔍 [useEffect] Проверка профиля сработала: {
  isAuthenticated: true,
  hasUserProfile: true,
  shouldAutoCheckout: true,
  hasPendingCheckout: true,
  userName: undefined,
  userStreet: undefined,
  userBuilding: undefined,
  userDistrict: "Адмиралтейский район"
}
🔍 Проверяем заполненность профиля после авторизации
⚠️ Профиль не заполнен, открываем ProfileModal
```

**Что должно быть на экране:**
- ✅ Модалка "Заполните данные для доставки"
- ✅ Синий баннер "📦 Оформление заказа..."
- ✅ Нет кнопки X
- ✅ Кнопка "Оформить заказ"

---

## 🚨 Возможные проблемы

### Проблема A: useEffect не срабатывает

**Симптомы:**
```
⚠️ [useEffect] Условия не выполнены, ProfileModal не открывается
```

**Что проверить:**

1. **isAuthenticated = false?**
   ```javascript
   // В консоли:
   console.log('currentUser:', localStorage.getItem('currentUser'))
   // Должно быть: "79991112233"
   ```

2. **shouldAutoCheckout = false?**
   - Проверьте логи на шаге 4
   - Должно быть: "🔐 Запрос авторизации с заказом"
   - Если нет - проблема в OrderModal

3. **pendingCheckout = null?**
   - Проверьте логи на шаге 4
   - Должен быть объект с order и total

4. **userProfile = null?**
   - Проверьте логи на шаге 5
   - Должно быть: "👤 Устанавливаем userProfile"

### Проблема B: ProfileModal открывается но пустой

**Симптомы:**
- Модалка открылась
- Но поля пустые или район не заполнен

**Решение:**
- Проверьте что `userProfile` передается в ProfileModal
- Проверьте логи: "👤 Устанавливаем userProfile"

### Проблема C: Заказ оформляется без адреса

**Симптомы:**
- ProfileModal не открылся
- Сразу показалась PaymentModal

**Причина:**
- `isProfileComplete` вернул `true`
- Значит `userProfile.name`, `userProfile.street`, `userProfile.building` заполнены

**Проверка:**
```javascript
// В консоли после авторизации:
console.log('userProfile:', JSON.parse(localStorage.getItem('profile_79991112233')))
```

---

## 📋 Чек-лист для отладки

Пройдите по этому списку и отметьте что работает:

### Шаг 1-2: Выбор района
- [ ] Модалка района показалась
- [ ] Лог: "✅ Район сохранен для гостя"
- [ ] localStorage.guest_district = "Адмиралтейский район"

### Шаг 3: Создание заказа
- [ ] Лог: "🗺️ Загружен район гостя"
- [ ] Показывается "Доставка: 250 ₽"

### Шаг 4: Нажатие "Заказать"
- [ ] Лог: "🚀 Неавторизованный пользователь нажал 'Заказать'"
- [ ] Лог: "🔐 Запрос авторизации с заказом"
- [ ] Модалка авторизации открылась

### Шаг 5: Авторизация
- [ ] Лог: "🔄 Синхронизация пользователя с базой данных"
- [ ] Лог: "✅ Пользователь создан в базе данных"
- [ ] Лог: "👤 Устанавливаем userProfile"
- [ ] Лог: "✅ У пользователя уже есть район"

### Шаг 6: useEffect
- [ ] Лог: "🔍 [useEffect] Проверка профиля сработала"
- [ ] isAuthenticated: true
- [ ] hasUserProfile: true
- [ ] shouldAutoCheckout: true
- [ ] hasPendingCheckout: true
- [ ] Лог: "⚠️ Профиль не заполнен, открываем ProfileModal"
- [ ] ProfileModal открылся

---

## 📤 Что отправить если не работает

Скопируйте и отправьте:

### 1. ВСЕ логи из консоли браузера

```
Ctrl+A в консоли → Ctrl+C → вставьте в текстовый файл
```

### 2. Состояние localStorage

```javascript
// Скопируйте результат:
console.log('=== localStorage ===')
console.log('currentUser:', localStorage.getItem('currentUser'))
console.log('guest_district:', localStorage.getItem('guest_district'))
console.log('district_modal_seen:', localStorage.getItem('district_modal_seen'))

const phone = localStorage.getItem('currentUser')
if (phone) {
  console.log('profile:', localStorage.getItem(`profile_${phone}`))
}
```

### 3. Network запросы

```
DevTools → Network → 
Найдите запросы к /api/orders
Скопируйте Response
```

### 4. Скриншот экрана

Сделайте скриншот того, что видите на экране после авторизации.

---

## 🔧 Временное решение (если совсем не работает)

Если ProfileModal не открывается автоматически, можно открыть вручную:

```javascript
// В консоли браузера после авторизации:
document.querySelector('[data-profile-button]')?.click()
// Или найдите кнопку профиля и кликните
```

---

**Следуй инструкциям, копируй логи и отправляй результаты!** 🔍



