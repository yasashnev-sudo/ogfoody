# 🔴 ПРОБЛЕМА: API /api/menu возвращает 503 ошибку

## Дата: 2026-01-11
## Статус: РЕШЕНИЕ НАЙДЕНО

---

## 🔍 Диагностика

Проблема была найдена в логах dev сервера:

```
[MENU API] Environment check:
  - NOCODB_URL: ❌ NOT SET
  - NOCODB_TOKEN: ❌ NOT SET
  - NOCODB_TABLE_MEALS: ❌ NOT SET
  - NOCODB_TABLE_EXTRAS: ❌ NOT SET
  - NOCODB_TABLE_DELIVERY_ZONES: ❌ NOT SET
```

**Причина:** Не установлены переменные окружения для подключения к NocoDB.

---

## ✅ РЕШЕНИЕ

### Шаг 1: Создайте файл `.env.local`

Файл `.env.local.example` уже создан в корне проекта. Выполните следующие команды:

```bash
# Скопируйте шаблон
cp .env.local.example .env.local
```

### Шаг 2: Получите токен NocoDB

1. Откройте в браузере: https://noco.povarnakolesah.ru
2. Войдите в систему
3. Перейдите в: **Settings** → **API Tokens**
4. Создайте новый токен или скопируйте существующий
5. Скопируйте токен в буфер обмена

### Шаг 3: Отредактируйте файл `.env.local`

Откройте файл `.env.local` в редакторе и замените `YOUR_NOCODB_TOKEN_HERE` на ваш реальный токен:

```bash
# Было:
NOCODB_TOKEN=YOUR_NOCODB_TOKEN_HERE

# Должно стать (пример):
NOCODB_TOKEN=ваш_реальный_токен_из_nocodb
```

### Шаг 4: Перезапустите dev сервер

```bash
# Остановите текущий dev сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

### Шаг 5: Проверьте результат

1. Откройте браузер: http://localhost:3000
2. Товары должны загрузиться
3. В консоли dev сервера должно появиться:

```
[MENU API] Environment check:
  - NOCODB_URL: https://noco.povarnakolesah... ✅
  - NOCODB_TOKEN: ********** ✅
  - NOCODB_TABLE_MEALS: mmtctn4fls... ✅
```

---

## 📋 Список переменных окружения

Все переменные уже настроены в `.env.local.example`, вам нужно только добавить токен:

```bash
NOCODB_URL=https://noco.povarnakolesah.ru
NOCODB_TOKEN=<ваш токен>
NOCODB_TABLE_MEALS=mmtctn4flssh2ua
NOCODB_TABLE_EXTRAS=mksy21hmttmo855
NOCODB_TABLE_DELIVERY_ZONES=mpoppulqhsz1der
NOCODB_TABLE_USERS=mvrp4r9o3z69c45
NOCODB_TABLE_ORDERS=meddiicl0gr0r8y
NOCODB_TABLE_ORDER_PERSONS=mvr08d33zm5i8oi
NOCODB_TABLE_ORDER_MEALS=mz9uw5by177ygug
NOCODB_TABLE_ORDER_EXTRAS=mksy21hmttmo855
NOCODB_TABLE_PROMO_CODES=mgov8ce836696fy
NOCODB_TABLE_REVIEWS=mv8c69ib9muz9ki
```

---

## 🎯 Быстрое решение (одна команда)

Если у вас уже есть токен, выполните:

```bash
# Замените YOUR_TOKEN на ваш реальный токен
echo "NOCODB_URL=https://noco.povarnakolesah.ru
NOCODB_TOKEN=YOUR_TOKEN
NOCODB_TABLE_MEALS=mmtctn4flssh2ua
NOCODB_TABLE_EXTRAS=mksy21hmttmo855
NOCODB_TABLE_DELIVERY_ZONES=mpoppulqhsz1der
NOCODB_TABLE_USERS=mvrp4r9o3z69c45
NOCODB_TABLE_ORDERS=meddiicl0gr0r8y
NOCODB_TABLE_ORDER_PERSONS=mvr08d33zm5i8oi
NOCODB_TABLE_ORDER_MEALS=mz9uw5by177ygug
NOCODB_TABLE_ORDER_EXTRAS=mksy21hmttmo855
NOCODB_TABLE_PROMO_CODES=mgov8ce836696fy
NOCODB_TABLE_REVIEWS=mv8c69ib9muz9ki" > .env.local

# Перезапустите dev сервер
```

---

## 🔧 Диагностика

После настройки можете проверить конфигурацию:

### В браузере:
```
http://localhost:3000/api/diagnose
```

### В терминале:
```bash
curl http://localhost:3000/api/diagnose | json_pp
```

---

## ⚠️ Важно

1. **Файл `.env.local` не должен попадать в Git** - он уже добавлен в `.gitignore`
2. **Токен - это секретная информация** - не делитесь им публично
3. **После изменения `.env.local` всегда перезапускайте dev сервер**

---

## 📝 Что было сделано

1. ✅ Создан файл `.env.local.example` с шаблоном переменных
2. ✅ Все ID таблиц уже заполнены из VERCEL_ENV_SETUP.txt
3. ✅ Добавлена инструкция по получению токена
4. ⏳ Осталось только добавить токен и перезапустить сервер

---

## 🎉 После исправления

После настройки переменных окружения:
- ✅ API `/api/menu` будет возвращать 200 вместо 503
- ✅ Товары будут загружаться на сайт
- ✅ Все функции работы с базой данных заработают

---

## 💡 Если проблема сохраняется

1. Проверьте, что файл `.env.local` находится в корне проекта
2. Проверьте, что токен скопирован правильно (без лишних пробелов)
3. Проверьте логи dev сервера на наличие других ошибок
4. Проверьте доступность NocoDB: https://noco.povarnakolesah.ru
5. Используйте `/api/diagnose` для детальной диагностики

