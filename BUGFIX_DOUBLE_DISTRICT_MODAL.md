# 🐛 ИСПРАВЛЕНО: Двойной запрос района для авторизованных пользователей

**Дата:** 11.01.2026  
**Приоритет:** 🔴 КРИТИЧЕСКИЙ  
**Статус:** ✅ ИСПРАВЛЕНО

---

## 🐛 Проблема

При оформлении заказа **авторизованным пользователем БЕЗ профиля** система **два раза** спрашивала район:

1. **Первый раз:** Сразу после авторизации (в `handleLogin`)
2. **Второй раз:** При нажатии "Заказать" (в `onRequestAuth`)

### Воспроизведение:

```
1. Пользователь авторизуется (вводит телефон)
2. В БД создается пользователь БЕЗ района
3. handleLogin показывает модалку района → пользователь ЗАКРЫВАЕТ ❌
4. Пользователь выбирает дату, добавляет блюдо
5. Нажимает "Заказать"
6. onRequestAuth СНОВА показывает модалку района ❌❌
```

### Логи из консоли:

```
⚠️ У пользователя нет района доставки, показываем модалку выбора района
💾 Сохраняем выбранный район: Василеостровский район
✅ Район тихо сохранен в профиле: Василеостровский район
---
⚠️ Профиль неполный, запускаем flow заполнения профиля
🔐 Запрос авторизации для гостя
💾 Сохраняем выбранный район: Калининский район  ← ВТОРОЙ РАЗ!
```

---

## 🔍 Причины

### Причина #1: `onRequestAuth` всегда открывает модалку района

**Файл:** `app/page.tsx` (строка 2907)

**Старый код:**
```typescript
onRequestAuth={(order, total) => {
  console.log("🔐 Запрос авторизации для гостя:", { order, total })
  setPendingCheckout({ order, total })
  setSelectedDate(null)
  setShowDistrictModal(true) // ❌ ВСЕГДА открывает, не проверяя наличие района
}}
```

**Проблема:** Не проверялось, выбран ли уже район (в `userProfile.district` или `localStorage`).

---

### Причина #2: `handleLogin` показывает модалку района после авторизации

**Файл:** `app/page.tsx` (строки 2181-2183)

**Старый код:**
```typescript
if (!userDistrict) {
  console.log("⚠️ У пользователя нет района доставки, показываем модалку выбора района")
  setShowDistrictModal(true) // ❌ Показывает даже если район выбран в процессе оформления
}
```

**Проблема:** Не учитывалось, что пользователь мог выбрать район **в процессе оформления заказа** (`selectedDistrict` из state).

---

## ✅ Решение

### Исправление #1: Умная логика в `onRequestAuth`

**Файл:** `app/page.tsx` (строки 2902-2927)

**Новый код:**
```typescript
onRequestAuth={(order, total) => {
  const hasDistrict = userProfile?.district || localStorage.getItem("guest_district")
  
  console.log("🔐 Запрос оформления заказа:", { 
    isAuthenticated, 
    hasDistrict: !!hasDistrict,
    userDistrict: userProfile?.district,
    guestDistrict: localStorage.getItem("guest_district")
  })
  
  setPendingCheckout({ order, total })
  setSelectedDate(null)
  
  // ✅ Если нет района - показываем выбор района (для ВСЕХ)
  if (!hasDistrict) {
    console.log("⚠️ Район не выбран, открываем DistrictSelectionModal")
    setShowDistrictModal(true)
  } else {
    // Если район есть, но не авторизован - показываем авторизацию
    if (!isAuthenticated) {
      console.log("✅ Район есть, но не авторизован - открываем AuthModal")
      setShowAuthModal(true)
    } else {
      // Авторизован И район есть → сразу к выбору времени
      console.log("✅ Авторизован И район есть - открываем выбор времени")
      setSelectedDistrict(hasDistrict)
      setShowDeliveryTimeModal(true)
    }
  }
}}
```

**Логика:**
1. Проверяем наличие района (`userProfile.district` ИЛИ `guest_district`)
2. **Если района НЕТ** → показываем DistrictSelectionModal
3. **Если район ЕСТЬ**:
   - **НЕ авторизован** → показываем AuthModal
   - **Авторизован** → сразу к выбору времени ✅

---

### Исправление #2: Учет `selectedDistrict` в `handleLogin`

**Файл:** `app/page.tsx` (строки 2179-2195)

**Новый код:**
```typescript
const userDistrict = profile.district

// ✅ ИСПРАВЛЕНО: Не показываем модалку района если:
// 1. Идет процесс оформления заказа (pendingCheckout)
// 2. И район уже выбран (selectedDistrict)
if (!userDistrict) {
  // Проверяем, не выбран ли район в процессе оформления
  if (pendingCheckout && selectedDistrict) {
    console.log("✅ Район уже выбран в процессе оформления:", selectedDistrict)
    // Не показываем модалку, useEffect запустит автооформление
  } else {
    console.log("⚠️ У пользователя нет района доставки, показываем модалку выбора района")
    setShowDistrictModal(true)
  }
} else {
  console.log("✅ У пользователя уже есть район:", userDistrict)
}
```

**Логика:**
- Если у пользователя нет района в БД (`!userDistrict`)
- НО район уже выбран в процессе оформления (`selectedDistrict`)
- И идет процесс оформления (`pendingCheckout`)
- → **НЕ показываем** модалку района снова ✅

---

## 🎯 Обновленные FLOW

### FLOW 1: Авторизован БЕЗ района → Оформление заказа

```
1. Выбрать дату, добавить блюдо
2. Нажать "Заказать"
3. handlePayAndOrder: профиль неполный → onRequestAuth
4. onRequestAuth: нет района → DistrictSelectionModal ✅ (1 раз)
5. Выбрать район → сохраняется в selectedDistrict + userProfile
6. DeliveryTimeSelectionModal → выбор времени
7. ProfileModal (isCheckoutFlow) → заполнение имени, адреса
8. Заказ создается
```

### FLOW 2: Гость → Авторизация → Оформление

```
1. Выбрать дату, добавить блюдо
2. Нажать "Оформить заказ"
3. onRequestAuth: нет района → DistrictSelectionModal ✅ (1 раз)
4. Выбрать район → сохраняется в guest_district
5. DeliveryTimeSelectionModal → выбор времени
6. AuthModal → авторизация
7. handleLogin: проверяет selectedDistrict → НЕ показывает модалку района ✅
8. ProfileModal (isCheckoutFlow) → заполнение остальных данных
9. Заказ создается
```

### FLOW 3: Авторизован С районом БЕЗ адреса → Оформление

```
1. Выбрать дату, добавить блюдо
2. Нажать "Заказать"
3. handlePayAndOrder: есть район, нет адреса → onRequestAuth
4. onRequestAuth: район есть → DeliveryTimeSelectionModal ✅ (пропускаем район)
5. Выбрать время
6. ProfileModal (isCheckoutFlow) → заполнение имени, адреса
7. Заказ создается
```

---

## ✅ Результат

### До исправления:
- ❌ Модалка района показывается **2 раза**
- ❌ Пользователь выбирает район дважды
- ❌ Плохой UX

### После исправления:
- ✅ Модалка района показывается **1 раз**
- ✅ Выбранный район сохраняется и переиспользуется
- ✅ Отличный UX

---

## 📊 Изменения

| Файл | Функция | Строки изменены |
|------|---------|-----------------|
| `app/page.tsx` | `onRequestAuth` (callback) | 2902-2927 (25 строк) |
| `app/page.tsx` | `handleLogin` | 2179-2195 (16 строк) |

**Итого:** ~40 строк изменений, 2 функции ✅

---

## 🧪 Тестирование

### Ручной тест:

1. Очистить localStorage
2. Открыть сайт
3. Авторизоваться (новый пользователь)
4. Выбрать дату, добавить блюдо
5. Нажать "Заказать"
6. **Ожидается:** Модалка района появляется **1 раз** ✅

### Автоматический тест:

Обновить `tests/profile-completion-flow.spec.ts` для проверки:
- Модалка района появляется только 1 раз
- selectedDistrict сохраняется между шагами

---

## 🎉 Статус

✅ **ИСПРАВЛЕНО И ГОТОВО К ТЕСТИРОВАНИЮ**

Теперь flow работает корректно:
- Район запрашивается **только 1 раз**
- Выбранный район сохраняется и переиспользуется
- Логика учитывает все сценарии (гость, авторизованный без района, авторизованный с районом)


