# 🧪 Тестирование функции "Повторить заказ"

**Дата:** 11.01.2026  
**Версия:** 1.0

---

## 🚀 Быстрый старт

### 1. Запуск приложения
```bash
cd /Users/sergejasasnev/Downloads/my-project\ \(1\)
npm run dev
```

Откройте: http://localhost:3000

### 2. Консоль браузера
Откройте DevTools (F12 или Cmd+Option+I) и следите за логами:
- 🔄 - Начало повтора
- 📋 - Меню загружено
- ✅ - Валидация завершена
- 🚀 - OrderModal открыта

---

## ✅ Сценарий 1: Успешный повтор (Happy Path)

**Цель:** Проверить что функция работает с доступными товарами.

**Шаги:**
1. Авторизуйтесь (или создайте заказ как гость и авторизуйтесь)
2. Создайте заказ:
   - Выберите дату (например, завтра)
   - Добавьте завтрак: Омлет с овощами (single)
   - Добавьте обед: Салат Цезарь, Борщ, Котлета с гречкой
   - Добавьте extra: Морс клюквенный
   - Сохраните заказ
3. Перейдите в "История заказов" (кнопка "История" в навигации)
4. Найдите созданный заказ
5. Нажмите "ПОВТОРИТЬ ЗАКАЗ"

**Ожидаемый результат:**
- ✅ Кнопка показывает "Загрузка..." с spinner
- ✅ Открывается меню выбора даты
- ✅ Доступные даты подсвечены синим (текущая неделя)
6. Выберите другую дату (например, послезавтра)

**Ожидаемый результат:**
- ✅ OrderModal открылась автоматически
- ✅ Все товары на месте:
  - Омлет с овощами
  - Салат Цезарь
  - Борщ
  - Котлета с гречкой
  - Морс клюквенный
- ✅ Цены актуальные (можно сравнить с меню)
- ✅ Total пересчитался правильно
- ✅ Можно редактировать заказ или сохранить

**Консоль браузера:**
```
🔄 [Repeat Order] Начало повтора заказа: {orderId: 123, ...}
📋 [Repeat Order] Меню загружено: {hasBreakfast: true, ...}
✅ [Repeat Order] Валидация завершена: {unavailableCount: 0, ...}
🚀 [Repeat Order] Открываем OrderModal с новым заказом
```

---

## ⚠️ Сценарий 2: Недоступные товары

**Цель:** Проверить работу с товарами, которых больше нет в меню.

**Подготовка (в NocoDB):**
1. Откройте NocoDB
2. Перейдите в таблицу Meals
3. Найдите блюдо "Сырники"
4. Снимите галочки "Current Week" и "Next Week"
5. Сохраните изменения

**Шаги тестирования:**
1. Создайте заказ с блюдом "Сырники"
2. Перейдите в "История заказов"
3. Нажмите "ПОВТОРИТЬ ЗАКАЗ" на этом заказе
4. Выберите новую дату

**Ожидаемый результат:**
- ✅ Показывается Toast уведомление:
  ```
  ⚠️ Некоторые товары недоступны
  Следующие позиции больше не в меню и будут пропущены: Сырники
  ```
- ✅ OrderModal открылась
- ✅ Блюда "Сырники" нет в заказе
- ✅ Остальные товары на месте

**Консоль браузера:**
```
✅ [Repeat Order] Валидация завершена: {
  unavailableCount: 1,
  unavailableItems: ["Сырники"]
}
```

**Восстановление:**
- Верните галочки "Current Week" и "Next Week" для блюда "Сырники" в NocoDB

---

## 💰 Сценарий 3: Изменившиеся цены

**Цель:** Проверить что используются текущие цены, а не исторические.

**Подготовка (в NocoDB):**
1. Откройте NocoDB → Meals
2. Найдите блюдо "Омлет с овощами"
3. Запомните текущую цену (например, 280₽)
4. Измените "Price (Single)" на 350₽
5. Сохраните

**Шаги тестирования:**
1. Создайте заказ с "Омлет с овощами" (будет сохранен с ценой 280₽)
2. Проверьте в истории заказов - цена 280₽
3. Нажмите "ПОВТОРИТЬ ЗАКАЗ"
4. Выберите новую дату
5. В открывшейся OrderModal проверьте цену Омлета

**Ожидаемый результат:**
- ✅ Цена Омлета = 350₽ (новая, актуальная!)
- ✅ Total пересчитался с новой ценой
- ✅ Старая цена 280₽ НЕ используется

**Проверка в коде:**
Блюдо должно иметь:
```typescript
{
  name: "Омлет с овощами",
  prices: { single: 350, ... },  // ✅ Актуальная цена из меню!
  portion: "single",
}
```

**Восстановление:**
- Верните цену "Price (Single)" обратно на 280₽

---

## 🔌 Сценарий 4: Ошибка API

**Цель:** Проверить обработку ошибок при недоступности API.

**Подготовка:**
Остановите dev сервер:
```bash
# В терминале где запущен npm run dev
Ctrl+C
```

**Шаги тестирования:**
1. На странице (где приложение еще работает) перейдите в историю
2. Нажмите "ПОВТОРИТЬ ЗАКАЗ"
3. Выберите дату

**Ожидаемый результат:**
- ✅ Показывается Toast с ошибкой:
  ```
  ❌ Ошибка
  Не удалось повторить заказ. Попробуйте позже.
  ```
- ✅ Loading state снимается
- ✅ OrderModal НЕ открывается
- ✅ Приложение не падает

**Консоль браузера:**
```
❌ [Repeat Order] Ошибка при повторе заказа: Failed to fetch
```

**Восстановление:**
```bash
npm run dev
```

---

## 🤖 Автотесты (E2E с Playwright)

### Установка Playwright (если еще не установлен)
```bash
npm install -D @playwright/test
npx playwright install chromium
```

### Пример теста
Создайте файл `tests/repeat-order.spec.ts`:

```typescript
import { test, expect } from '@playwright/test'

test('Кнопка "Повторить заказ" существует', async ({ page }) => {
  await page.goto('http://localhost:3000')
  
  // Войти и создать заказ (упрощенно)
  // ...
  
  // Перейти в историю
  await page.click('text=История')
  
  // Найти кнопку повтора
  const repeatBtn = page.locator('[data-testid="repeat-order-btn"]')
  await expect(repeatBtn).toBeVisible()
  await expect(repeatBtn).toHaveText(/ПОВТОРИТЬ ЗАКАЗ/)
})

test('Loading state при повторе заказа', async ({ page }) => {
  await page.goto('http://localhost:3000')
  
  // ... войти, создать заказ, перейти в историю
  
  const repeatBtn = page.locator('[data-testid="repeat-order-btn"]')
  await repeatBtn.click()
  
  // Проверяем что кнопка показывает "Загрузка..."
  await expect(repeatBtn).toContainText('Загрузка...')
  await expect(repeatBtn).toBeDisabled()
  
  // Выбираем дату
  await page.click('button:has-text("Завтра")')
  
  // Проверяем что OrderModal открылась
  await expect(page.locator('[role="dialog"]')).toBeVisible()
})
```

### Запуск тестов
```bash
npx playwright test tests/repeat-order.spec.ts --workers=1
```

---

## 📊 Чеклист проверки

После каждого обновления проверьте:

- [ ] Кнопка "ПОВТОРИТЬ ЗАКАЗ" видна в истории
- [ ] Иконка `RotateCcw` (стрелка по кругу) отображается
- [ ] При клике показывается loading spinner
- [ ] Меню выбора даты открывается
- [ ] Даты правильно отфильтрованы (нет занятых дат)
- [ ] При выборе даты OrderModal открывается автоматически
- [ ] Все доступные товары добавлены
- [ ] Цены актуальные (из меню, не из истории)
- [ ] Недоступные товары пропущены
- [ ] Toast показывается только если есть недоступные товары
- [ ] Loading снимается даже при ошибке
- [ ] Консольные логи с эмодзи работают
- [ ] `data-testid="repeat-order-btn"` присутствует

---

## 🐛 Частые проблемы

### Проблема 1: OrderModal не открывается
**Симптомы:** Клик на дату не открывает модалку

**Решение:**
1. Проверьте консоль на ошибки
2. Проверьте что `setSelectedDate(targetDate)` вызывается
3. Проверьте что `selectedDate` state обновляется
4. Проверьте что OrderModal привязана к `selectedDate`:
   ```typescript
   <OrderModal open={!!selectedDate} ... />
   ```

### Проблема 2: Старые цены в заказе
**Симптомы:** Цена из истории, а не из меню

**Решение:**
1. Проверьте что `validateOrderItems` возвращает товары из `currentMenu`
2. Проверьте что `total`, `subtotal` очищены в новом заказе
3. Проверьте логи в консоли - должны быть актуальные цены

### Проблема 3: Loading не снимается
**Симптомы:** Кнопка остается в состоянии "Загрузка..."

**Решение:**
1. Проверьте что есть блок `finally` в обработчике
2. Проверьте что `setRepeatLoading(null)` вызывается
3. Добавьте логирование в `finally` блок

### Проблема 4: Toast не показывается
**Симптомы:** Нет уведомления о недоступных товарах

**Решение:**
1. Проверьте что `showWarning` вызывается
2. Проверьте условие: `if (hasUnavailableItems)`
3. Проверьте импорт `showWarning` функции
4. Проверьте логи - должен быть список недоступных товаров

---

## 📞 Поддержка

Если нашли баг или есть вопросы:
1. Проверьте логи в консоли (ищите эмодзи 🔄📋✅🚀)
2. Проверьте Network tab (должен быть запрос к `/api/menu?week=...`)
3. Откройте `REPEAT_ORDER_IMPLEMENTATION_REPORT.md` для деталей
4. Проверьте `DATA_ARCHITECTURE_RULES.md` для архитектурных правил

---

**Удачного тестирования! 🚀**

**Автор:** Senior Frontend Developer  
**Дата:** 11.01.2026



