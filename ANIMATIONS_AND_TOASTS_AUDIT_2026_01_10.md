# 🎨 АУДИТ АНИМАЦИЙ И TOAST УВЕДОМЛЕНИЙ

**Дата:** 10.01.2026  
**Цель:** Убрать toast уведомления и добавить анимации в стиле сайта

---

## 📍 ПУТЬ КЛИЕНТА

### 1. ПЕРВЫЙ ВХОД НА САЙТ (Preloader)
**Текущее состояние:**
- ✅ Есть анимация: `<Preloader />` - желтый экран с логотипом
- ✅ Работает хорошо
- ✅ Оставляем как есть

### 2. ВЫБОР РАЙОНА (для гостей)
**Текущее состояние:**
- ✅ Модальное окно: `<DistrictSelectionModal />`
- ✅ Нет избыточных toast
- ✅ Оставляем как есть

**Toast уведомления:**
- `app/page.tsx:2043` - "Район сохранен" - 🚫 УБРАТЬ
- `app/page.tsx:2052` - "Ошибка сохранения района" - 🚫 ЗАМЕНИТЬ на inline error в модалке

### 3. ПРОСМОТР МЕНЮ
**Текущее состояние:**
- ✅ Нет анимаций (не требуется)
- ✅ Нет toast
- ✅ Оставляем как есть

### 4. СОЗДАНИЕ ЗАКАЗА (OrderModal)
**Текущее состояние:**
- ✅ Модальное окно: `<OrderModal />`
- ✅ Нет избыточных toast
- ✅ Оставляем как есть

**Toast уведомления:**
- `components/order-modal.tsx:468` - "Доставка недоступна" - ✅ ОСТАВИТЬ (важное предупреждение)

### 5. СОХРАНЕНИЕ ЗАКАЗА
**Текущее состояние:**
- ✅ Анимация: `<OrderLoadingDialog />` - "Создаем заказ"
- ✅ Работает хорошо

**Toast уведомления:**
- `app/page.tsx:748` - "Заказ обновлен" - 🚫 УБРАТЬ (есть анимация)
- `app/page.tsx:755` - "Ошибка обновления" - 🚫 ЗАМЕНИТЬ на inline error
- `app/page.tsx:958` - "Ошибка создания заказа" - 🚫 ЗАМЕНИТЬ на inline error
- `app/page.tsx:995` - "Заказ сохранен локально" - 🚫 УБРАТЬ (не нужен)
- `app/page.tsx:1041` - "Заказ сохранен" - 🚫 УБРАТЬ (не нужен)
- `app/page.tsx:1102` - "Заказ обновлен" - 🚫 УБРАТЬ (есть анимация)
- `app/page.tsx:1109` - "Ошибка обновления" - 🚫 ЗАМЕНИТЬ на inline error

### 6. АВТОРИЗАЦИЯ (для гостей)
**Текущее состояние:**
- ✅ Модальное окно: `<AuthModal />`
- ✅ Нет избыточных toast

**Toast уведомления:**
- `app/page.tsx:1676` - "Добро пожаловать! Ваш профиль создан" - 🚫 УБРАТЬ (не нужен, идет к оформлению)
- `app/page.tsx:1682` - "Добро пожаловать!" - 🚫 УБРАТЬ (не нужен, идет к оформлению)

### 7. ЗАПОЛНЕНИЕ ПРОФИЛЯ (для новых пользователей)
**Текущее состояние:**
- ✅ Модальное окно: `<ProfileModal />`
- ✅ Нет toast
- ✅ Оставляем как есть

### 8. ОФОРМЛЕНИЕ ЗАКАЗА (Checkout)
**Текущее состояние:**
- ✅ Нет отдельной анимации (автоматически открывается PaymentModal)
- ✅ Нет избыточных toast

**Toast уведомления:**
- `app/page.tsx:1844` - "Ошибка: Район доставки не указан" - ✅ ОСТАВИТЬ (важная ошибка)
- `app/page.tsx:1935` - "Заказ оформлен!" - 🚫 УБРАТЬ (сразу идет к оплате)
- `app/page.tsx:1943` - "Ошибка оформления" - 🚫 ЗАМЕНИТЬ на inline error

### 9. ОПЛАТА ЗАКАЗА ⚠️ **КРИТИЧЕСКАЯ ПРОБЛЕМА**
**Текущее состояние:**
- ✅ Модальное окно: `<PaymentModal />`
- ⚠️ Анимация: `<PaymentLoading />` - **"Идет оплата..." с иконкой карты**
  - **ПРОБЛЕМА:** Показывается ВСЕГДА (для card/sbp/cash)
  - **НУЖНО:** Показывать только для card/sbp
  - **НУЖНО:** Создать отдельную анимацию для cash

**Toast уведомления:**
- `app/page.tsx:1382` - "Ошибка оплаты" - 🚫 ЗАМЕНИТЬ на inline error
- `app/page.tsx:1488` - "Ошибка оплаты" - 🚫 ЗАМЕНИТЬ на inline error
- `app/page.tsx:1598` - "Ошибка оплаты" - 🚫 ЗАМЕНИТЬ на inline error

### 10. УСПЕШНАЯ ОПЛАТА
**Текущее состояние:**
- ✅ Анимация: `<SuccessOrderDialog />` - зеленая галочка + баллы
- ✅ Работает хорошо
- ✅ Оставляем как есть

### 11. ОТМЕНА ЗАКАЗА
**Текущее состояние:**
- ✅ Анимация: `<CancelOrderLoading />` - "Отменяем заказ..."
- ✅ Работает хорошо
- ✅ Оставляем как есть

---

## 📊 ИТОГОВАЯ СТАТИСТИКА

### Toast уведомления (всего найдено: 17)
- ✅ **Оставить:** 2 (важные ошибки/предупреждения)
- 🚫 **Убрать:** 8 (избыточные, есть анимации)
- 🔄 **Заменить на inline error:** 7

### Анимации
- ✅ **Работают хорошо:** 5 (Preloader, OrderLoading, SuccessDialog, CancelLoading, DistrictModal)
- ⚠️ **Требуют исправления:** 1 (PaymentLoading - показывается для всех методов)
- 🆕 **Нужно создать:** 1 (CashPaymentAnimation - для оплаты наличными)

---

## 🔧 ПЛАН ИСПРАВЛЕНИЙ

### Приоритет 1: Критические (исправить сейчас)

#### 1. Исправить PaymentLoading - показывать только для card/sbp
**Файл:** `app/page.tsx`
**Функция:** `handlePaymentComplete()`
**Строки:** 1497-1600

**Текущий код:**
```typescript
setShowPaymentLoading(true) // Показывается ВСЕГДА
```

**Нужно:**
```typescript
if (paymentMethod === 'card' || paymentMethod === 'sbp') {
  setShowPaymentLoading(true)
}
```

#### 2. Создать CashPaymentAnimation
**Новый файл:** `components/cash-payment-animation.tsx`
**Стиль:** В стиле сайта (желтый #FFEA00, brutal design)
**Содержание:** 
- Иконка монет/наличных (не карта!)
- Текст: "Подтверждаем заказ..."
- Без упоминания "оплаты" (оплата будет при получении)

#### 3. Добавить state для CashPaymentAnimation
**Файл:** `app/page.tsx`
**Добавить:**
```typescript
const [showCashPaymentAnimation, setShowCashPaymentAnimation] = useState(false)
```

**Использовать в handlePaymentComplete:**
```typescript
if (paymentMethod === 'cash') {
  setShowCashPaymentAnimation(true)
} else if (paymentMethod === 'card' || paymentMethod === 'sbp') {
  setShowPaymentLoading(true)
}
```

### Приоритет 2: Убрать toast уведомления (убрать сейчас)

#### Список toast для удаления:
1. `app/page.tsx:748` - "Заказ обновлен"
2. `app/page.tsx:995` - "Заказ сохранен локально"
3. `app/page.tsx:1041` - "Заказ сохранен"
4. `app/page.tsx:1102` - "Заказ обновлен"
5. `app/page.tsx:1676` - "Добро пожаловать! Ваш профиль создан"
6. `app/page.tsx:1682` - "Добро пожаловать!"
7. `app/page.tsx:1935` - "Заказ оформлен!"
8. `app/page.tsx:2043` - "Район сохранен"

#### Toast для замены на inline ошибки:
1. `app/page.tsx:755` - "Ошибка обновления заказа"
2. `app/page.tsx:958` - "Ошибка создания заказа"
3. `app/page.tsx:1109` - "Ошибка обновления заказа"
4. `app/page.tsx:1382` - "Ошибка оплаты"
5. `app/page.tsx:1488` - "Ошибка оплаты"
6. `app/page.tsx:1598` - "Ошибка оплаты"
7. `app/page.tsx:1943` - "Ошибка оформления"
8. `app/page.tsx:2052` - "Ошибка сохранения района"

**Вместо toast использовать:**
- Для критических ошибок: `<WarningDialog />` (уже есть в коде)
- Для inline ошибок в модалках: state с текстом ошибки внутри модального окна

### Приоритет 3: Добавить анимации загрузки (если нужно)

#### Места где может не хватать обратной связи:
1. Загрузка профиля при авторизации - ✅ **НЕ НУЖНО** (быстрая операция)
2. Обновление баланса баллов - ✅ **НЕ НУЖНО** (обновляется автоматически)
3. Загрузка истории заказов - ✅ **НЕ НУЖНО** (быстрая операция)

---

## 💡 ДОПОЛНИТЕЛЬНЫЕ УЛУЧШЕНИЯ

### 1. Использовать WarningDialog для критических ошибок
Вместо toast с `variant: "destructive"` использовать существующий `<WarningDialog />`

**Пример:**
```typescript
// Было:
toast({
  title: "Ошибка",
  description: "Не удалось оплатить заказ",
  variant: "destructive",
})

// Стало:
setWarningDialog({
  open: true,
  title: "Ошибка оплаты",
  description: "Не удалось оплатить заказ. Попробуйте еще раз.",
  variant: "destructive",
})
```

### 2. Добавить inline ошибки в модалки
В модалки `PaymentModal`, `OrderModal` добавить state для ошибок:

```typescript
const [error, setError] = useState<string | null>(null)

// В UI:
{error && (
  <div className="bg-red-50 border-2 border-red-500 text-red-700 px-4 py-3 rounded-lg">
    {error}
  </div>
)}
```

---

## ✅ ИТОГОВЫЙ ЧЕКЛИСТ

### Критические исправления (сделать сейчас)
- [ ] Исправить PaymentLoading - показывать только для card/sbp
- [ ] Создать CashPaymentAnimation компонент
- [ ] Добавить state showCashPaymentAnimation
- [ ] Обновить handlePaymentComplete с условием по paymentMethod

### Удаление toast (сделать сейчас)
- [ ] Удалить 8 избыточных toast уведомлений
- [ ] Заменить 7 toast ошибок на WarningDialog
- [ ] Убрать импорт useToast если он больше не используется

### Тестирование (после изменений)
- [ ] Протестировать оплату картой (должна быть анимация с картой)
- [ ] Протестировать оплату СБП (должна быть анимация с картой)
- [ ] Протестировать оплату наличными (должна быть анимация с монетами)
- [ ] Проверить что нет избыточных toast уведомлений
- [ ] Проверить что ошибки отображаются в WarningDialog

---

**Последнее обновление:** 10.01.2026 20:00



