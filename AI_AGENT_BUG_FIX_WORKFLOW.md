# 🤖 Инструкция для AI Агента: Исправление багов в ogfoody.ru

## 📋 ТЫ - AI АГЕНТ ПО ИСПРАВЛЕНИЮ БАГОВ

Твоя задача: найти баг-репорты, проанализировать, исправить и задеплоить.

---

## 🗂️ СТРУКТУРА ПРОЕКТА

### Локальное окружение:
```
Проект: /Users/sergejasasnev/Downloads/my-project (1)/
Git repo: https://github.com/yasashnev-sudo/ogfoody
Branch: main
```

### Production сервер:
```
Host: 5.129.194.168
User: root
Password: pULRoAvF@P-@4Y
Путь: /var/www/ogfoody
URL: https://ogfoody.ru
```

---

## 📁 ГДЕ ИСКАТЬ БАГ-РЕПОРТЫ

### 1. Локально (для dev-тестирования):
```bash
/Users/sergejasasnev/Downloads/my-project (1)/debug_reports/
```

### 2. Production (реальные баги от пользователей):
```bash
# Подключение к серверу
ssh root@5.129.194.168
# Пароль: pULRoAvF@P-@4Y

# Путь к отчётам
cd /var/www/ogfoody/debug_reports/

# Список отчётов
ls -lht *.txt | head -10

# Индекс всех отчётов (JSON)
cat index.json
```

### Формат отчёта:
```json
{
  "id": "2026-01-13T14-25-34-072Z",
  "userId": "guest",
  "userEmail": "N/A",
  "errorMessage": "Manual report submission",
  "timestamp": "2026-01-13T14:25:33.979Z",
  "url": "https://ogfoody.ru/",
  "userAgent": "Mozilla/5.0 (iPhone; CPU iPhone OS 18.7...)",
  "hasComment": true,
  "userComment": "Очень долго грузится логотип на старте",
  "files": {
    "log": "2026-01-13T14-25-34-072Z_user-guest_logs.txt",
    "screenshot": null
  }
}
```

---

## 🔍 WORKFLOW ИСПРАВЛЕНИЯ БАГОВ

### Шаг 1: Проверить новые отчёты
```bash
# Скрипт для быстрой проверки (создай его):
cat > check-bug-reports.sh << 'EOF'
#!/bin/bash
cat > /tmp/check_bugs.expect << 'EOFINNER'
#!/usr/bin/expect -f
set timeout 30
spawn ssh root@5.129.194.168
expect "password:"
send "pULRoAvF@P-@4Y\r"
expect "# "
send "echo '=== НОВЫЕ ОТЧЁТЫ ==='\r"
expect "# "
send "ls -lht /var/www/ogfoody/debug_reports/*.txt | head -5\r"
expect "# "
send "echo '\n=== INDEX.JSON ==='\r"
expect "# "
send "cat /var/www/ogfoody/debug_reports/index.json\r"
expect "# "
send "exit\r"
expect eof
EOFINNER
chmod +x /tmp/check_bugs.expect
/tmp/check_bugs.expect
rm /tmp/check_bugs.expect
EOF

chmod +x check-bug-reports.sh
./check-bug-reports.sh
```

### Шаг 2: Анализ бага
1. Прочитай `userComment` - что пользователь написал
2. Посмотри логи в `.txt` файле
3. Проверь `userAgent` - устройство/браузер
4. Найди паттерн ошибки в логах

### Шаг 3: Найти код с проблемой
```bash
# Используй codebase_search для семантического поиска
# Или grep для точного поиска
```

### Шаг 4: Исправить баг
- Редактируй файлы локально
- Тестируй если нужно (`npm run dev`)
- НЕ добавляй новые зависимости без необходимости

### Шаг 5: Документирование
Создай файл: `FIX_[ОПИСАНИЕ]_2026_MM_DD.md`
```markdown
# Исправление: [Название]

## Проблема:
[Цитата из userComment]

## Причина:
[Что нашёл в коде]

## Решение:
[Что изменил]

## Файлы:
- file1.ts
- file2.tsx

## Тестирование:
[Как проверить]
```

### Шаг 6: Коммит и пуш
```bash
cd "/Users/sergejasasnev/Downloads/my-project (1)"

git add -A
git commit -m "fix: [краткое описание]

- Исправлено: [проблема]
- Изменено: [что поменял]
- Closes: #[номер issue если есть]"

git push origin main
```

### Шаг 7: Деплой на production
```bash
# Быстрый деплой (создай скрипт):
cat > deploy-fix.sh << 'EOF'
#!/bin/bash
cat > /tmp/deploy.expect << 'EOFINNER'
#!/usr/bin/expect -f
set timeout 120
spawn ssh root@5.129.194.168
expect "password:"
send "pULRoAvF@P-@4Y\r"
expect "# "
send "cd /var/www/ogfoody && git fetch origin && git reset --hard origin/main && npm run build && pm2 restart ogfoody\r"
expect "# "
send "pm2 logs ogfoody --lines 20 --nostream\r"
expect "# "
send "exit\r"
expect eof
EOFINNER
chmod +x /tmp/deploy.expect
/tmp/deploy.expect
rm /tmp/deploy.expect
EOF

chmod +x deploy-fix.sh
./deploy-fix.sh
```

### Шаг 8: Проверка
```bash
# Проверь что всё работает
curl -I https://ogfoody.ru
# Status: 200 OK = хорошо

# Проверь логи
ssh root@5.129.194.168 "pm2 logs ogfoody --lines 50 --nostream"
```

---

## 📚 ВАЖНАЯ ДОКУМЕНТАЦИЯ

### Уже исправленные баги (читай перед работой):
```
BUG_FIXES_2026_01_13.md - список исправлений от 13 января
IPHONE_DEBUG_FIX_2026_01_13.md - исправление отправки баг-репортов
LOGO_OPTIMIZATION_2026_01_13.md - оптимизация логотипа
```

### Архитектура:
```
НАЧАЛО_РАБОТЫ.md - общая информация
AI_AGENT_WORKFLOW.md - workflow для разработки
SERVER_ACCESS_INFO.md - доступы к серверу
DATA_ARCHITECTURE_RULES.md - правила работы с данными
```

---

## 🛠️ ПОЛЕЗНЫЕ КОМАНДЫ

### Подключение к серверу:
```bash
ssh root@5.129.194.168
# Пароль: pULRoAvF@P-@4Y
```

### Просмотр логов:
```bash
# PM2 логи (приложение)
pm2 logs ogfoody --lines 100

# Error логи
tail -100 /var/www/ogfoody/logs/err.log

# Nginx логи
tail -100 /var/log/nginx/error.log
```

### Проверка статуса:
```bash
# Статус приложения
pm2 status

# Проверка порта
netstat -tlnp | grep 3000

# Использование памяти
free -h
```

### Перезапуск:
```bash
# Перезапуск приложения
pm2 restart ogfoody

# Перезапуск Nginx
systemctl restart nginx

# Полный перезапуск
pm2 restart ogfoody && systemctl restart nginx
```

---

## 🎯 ЧЕКЛИСТ ПЕРЕД ДЕПЛОЕМ

- [ ] Баг воспроизведён и понят
- [ ] Найдена причина в коде
- [ ] Исправление сделано локально
- [ ] Создана документация `FIX_*.md`
- [ ] Коммит с понятным сообщением
- [ ] Push в GitHub
- [ ] Деплой на production
- [ ] Проверка логов (нет новых ошибок)
- [ ] Проверка сайта работает
- [ ] Удалены временные скрипты (check-*, deploy-*)

---

## 📝 ШАБЛОНЫ

### Шаблон коммита:
```
fix: краткое описание проблемы

- Исправлено: подробное описание
- Причина: что было не так
- Решение: что изменено
- Файлы: list.tsx, component.tsx
```

### Шаблон документации:
```markdown
# Исправление: [Название] - YYYY-MM-DD

## 🐛 Проблема (из баг-репорта):
> "Цитата пользователя"

**User:** [ID]  
**Device:** [userAgent]  
**Time:** [timestamp]

## 🔍 Причина:
[Техническое объяснение]

## ✅ Решение:
[Что изменено]

### Код:
```typescript
// Изменения
```

## 📊 Результат:
- До: [что было]
- После: [что стало]

## 🚀 Деплой:
Время: [UTC]  
Статус: ✅ Успешно
```

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

### ❌ НИКОГДА НЕ ДЕЛАЙ:
1. **НЕ удаляй** существующую документацию
2. **НЕ меняй** структуру БД без согласования
3. **НЕ деплой** без тестирования
4. **НЕ коммить** без описания
5. **НЕ force push** на main

### ✅ ВСЕГДА ДЕЛАЙ:
1. **Читай** существующие `FIX_*.md` файлы
2. **Документируй** каждое исправление
3. **Проверяй** логи после деплоя
4. **Удаляй** временные скрипты после работы
5. **Создавай** понятные commit messages

### 🔧 Если что-то сломалось:
```bash
# Откат на предыдущую версию
ssh root@5.129.194.168
cd /var/www/ogfoody
git log --oneline -10  # Посмотри последние коммиты
git reset --hard [предыдущий-коммит]
npm run build
pm2 restart ogfoody
```

---

## 📞 КОНТАКТЫ

- **GitHub**: https://github.com/yasashnev-sudo/ogfoody
- **Production**: https://ogfoody.ru
- **NocoDB**: https://noco.povarnakolesah.ru

---

## 🎓 ПРИМЕРЫ ИСПРАВЛЕННЫХ БАГОВ

### Пример 1: Extra_id undefined
```
Проблема: "Order_Extras: extra_id undefined"
Файл: components/extras-selector.tsx
Решение: Добавил id: item.id при создании extra
Время: 30 минут
Документация: BUG_FIXES_2026_01_13.md
```

### Пример 2: Медленная загрузка логотипа
```
Проблема: "Очень долго грузится логотип на старте"
Файл: public/OGFooDY логотип.png
Решение: Создал logo-small.png (371KB → 30KB)
Время: 15 минут
Документация: LOGO_OPTIMIZATION_2026_01_13.md
```

### Пример 3: Skeleton анимация
```
Проблема: "Товары долго загружаются без индикации"
Файл: components/meal-card-skeleton.tsx (новый)
Решение: Добавил shimmer skeleton компоненты
Время: 45 минут
Документация: BUG_FIXES_2026_01_13.md
```

---

## 🚀 БЫСТРЫЙ СТАРТ

```bash
# 1. Проверь новые баги
./check-bug-reports.sh

# 2. Прочитай последние фиксы
cat BUG_FIXES_2026_01_13.md

# 3. Найди и исправь баг
# [твоя работа]

# 4. Задокументируй
# Создай FIX_*.md

# 5. Деплой
./deploy-fix.sh

# 6. Почисть временные файлы
rm -f check-*.sh deploy-*.sh

# 7. Готово! ✅
```

---

**Удачи в исправлении багов! 🐛 → ✅**


