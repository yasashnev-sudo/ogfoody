# 🐛 КРИТИЧЕСКИЙ БАГ: Создание нового заказа обновляет существующий

**Дата:** 2026-01-11  
**Файлы:** `app/page.tsx`

---

## 🐛 ПРОБЛЕМА

При попытке создать ВТОРОЙ заказ на ту же дату, что и первый заказ, система ОБНОВЛЯЛА первый заказ вместо создания нового!

### Симптомы:

**Пользователь #106:**

1. **ЛОГ 1 (16:21:20)** - Создал заказ #522 за наличные:
   ```javascript
   "Id": 522,
   "Order Number": "ORD-20260111-WO6H3R",
   "Payment Method": "cash",  // ✅ Создан за наличные
   "Start Date": "2026-01-12",
   ```

2. **ЛОГ 2 (16:22:08)** - Попытка создать второй заказ с оплатой картой:
   ```javascript
   "Id": 522,  // ❌ ТОТ ЖЕ ID!
   "Order Number": "ORD-20260111-WO6H3R",  // ❌ ТОТ ЖЕ НОМЕР!
   "Payment Method": "card",  // ❌ Изменился на карту!
   "Start Date": "2026-01-11T21:00:00.000Z",  // ❌ Формат даты изменился!
   ```

3. **База данных:** У пользователя #106 ОДИН заказ вместо двух!

---

## 🔍 ПРИЧИНА

### Проблема #1: Поиск существующего заказа по дате (строки 2765-2775)

```typescript
const existingOrder = selectedDate
  ? orders.find((o) => {
      const orderStartDate = new Date(o.startDate)
      orderStartDate.setHours(0, 0, 0, 0)

      const checkDate = new Date(selectedDate)
      checkDate.setHours(0, 0, 0, 0)

      return orderStartDate.getTime() === checkDate.getTime()
    })
  : undefined
```

Когда пользователь выбирает дату в календаре, система ищет **существующий** заказ на эту дату. Если находит - открывает модальное окно для редактирования.

### Проблема #2: Обновление заказа по дате вместо ID (строка 1896)

```typescript
// ❌ БАГ: Сравнение по дате!
if (getDateTimestamp(o.startDate) === orderTimestamp) {
  return {
    ...o,
    // ... обновление полей
  }
}
```

Когда вызывается `handlePaymentComplete`, заказ обновляется по **ДАТЕ**, а не по **ID**!

### Последовательность событий:

1. Пользователь создает заказ #522 на `2026-01-12` за наличные
2. Пользователь выбирает ту же дату `2026-01-12` для нового заказа
3. Система находит `existingOrder` (#522) по дате
4. Пользователь создает "новый" заказ → система открывает модальное окно редактирования существующего
5. Пользователь выбирает "Оплатить картой"
6. Вызывается `handlePaymentComplete(existingOrder, ...)` с `order.id = 522`
7. **БАГ:** `setOrders` обновляет заказ по **дате** вместо **ID**
8. Заказ #522 меняет метод оплаты с `cash` на `card`

---

## ✅ РЕШЕНИЕ

### Изменение 1: Обновление по ID вместо даты (app/page.tsx, ~строка 1894)

```typescript
// ✅ БЫЛО (БАГ):
if (getDateTimestamp(o.startDate) === orderTimestamp) {
  return { ...o, ... }
}

// ✅ СТАЛО (ИСПРАВЛЕНО):
if (o.id === order.id) {
  return {
    ...o,
    id: updatedOrderFromAPI.Id || updatedOrderFromAPI.id,
    paid: isPaid,
    paidAt: updatedOrderFromAPI.paid_at || updatedOrderFromAPI["Paid At"],
    paymentStatus: updatedOrderFromAPI.payment_status || paymentStatus,
    paymentMethod: paymentMethod, // ✅ ДОБАВЛЕНО: обновляем метод оплаты
    total: updatedOrderFromAPI.total || updatedOrderFromAPI.Total || 0,
    subtotal: updatedOrderFromAPI.subtotal || updatedOrderFromAPI.Subtotal || 0,
    loyaltyPointsEarned: updatedOrderFromAPI.loyalty_points_earned || 
                         updatedOrderFromAPI["Loyalty Points Earned"] || 
                         data.loyaltyPointsEarned || 0,
    loyaltyPointsUsed: updatedOrderFromAPI.loyalty_points_used || 
                       updatedOrderFromAPI["Loyalty Points Used"] || 0,
    // ✅ ДОБАВЛЕНО: Нормализуем startDate для предотвращения ISO timestamp
    startDate: toDate(updatedOrderFromAPI.start_date || updatedOrderFromAPI["Start Date"] || o.startDate),
  }
}
```

### Дополнительные улучшения:

1. **Добавлено обновление `paymentMethod`** - теперь метод оплаты корректно сохраняется
2. **Добавлена нормализация `startDate`** - предотвращает проблему с ISO timestamp в календаре

---

## 🎯 РЕЗУЛЬТАТ

Теперь:
- ✅ Заказы обновляются **строго по ID**, а не по дате
- ✅ Можно создать несколько заказов на одну дату
- ✅ Метод оплаты корректно сохраняется
- ✅ `startDate` всегда в правильном формате

---

## 📋 ТЕСТИРОВАНИЕ

### Сценарий 1: Два заказа на одну дату

1. Создать заказ #1 на `2026-01-15` за наличные
2. Создать заказ #2 на `2026-01-15` с оплатой картой
3. ✅ В базе данных должно быть **ДВА** заказа
4. ✅ Заказ #1 должен остаться за **наличные**
5. ✅ Заказ #2 должен быть с оплатой **картой**

### Сценарий 2: Изменение метода оплаты существующего заказа

1. Создать заказ #1 на `2026-01-16` за наличные
2. Открыть заказ #1
3. Изменить метод оплаты на карту
4. ✅ Заказ #1 должен обновиться на **карту**
5. ✅ ID и номер заказа должны остаться прежними

### Сценарий 3: Календарь

1. Создать заказ на `2026-01-17`
2. Изменить метод оплаты
3. ✅ Заказ должен отображаться в календаре
4. ✅ `startDate` должен быть в формате `YYYY-MM-DD` или Date объект, НЕ ISO timestamp

---

## ⚠️ ВАЖНО

Этот баг был **критическим** и мог привести к:
- Потере заказов пользователей
- Некорректным данным в базе данных
- Путанице с методами оплаты
- Проблемам с начислением баллов лояльности

---

## 🔄 СВЯЗАННЫЕ ИСПРАВЛЕНИЯ

- ✅ `FIX_CALENDAR_STARTDATE_FORMAT_2026_01_11.md` - Нормализация формата даты в API
- ✅ `FIX_LOYALTY_POINTS_DEDUCTION_CASH_2026_01_11.md` - Списание баллов для наличных
- ✅ `FIX_DOUBLE_DEDUCTION_PARTIAL_UPDATE_2026_01_11.md` - Двойное списание баллов

---

## ✅ СТАТУС

**ИСПРАВЛЕНО** - Заказы теперь обновляются по ID, предотвращая случайное изменение других заказов.


