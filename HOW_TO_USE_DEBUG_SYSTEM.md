# ✅ Система "Черный ящик" работает! Инструкция по использованию

## 🎉 Что произошло?

Ты только что **успешно создал первый отчёт отладки!**

```
Файл: debug_reports/2026-01-11T09-15-24-028Z_user-guest_logs.txt
Время: 11 января 2026, 12:15
Пользователь: guest (неавторизованный)
```

### ⚠️ О скриншотах

**Проблема**: html2canvas не поддерживает современные CSS функции (`lab()` цвета)  
**Решение**: ✅ Уже исправлено! Теперь:
- Если скриншот не удался → система продолжает работу с логами
- Логи всегда сохраняются (это главное!)
- Скриншот - бонус, но не обязателен

## 📝 Как использовать для отладки баллов

### Сценарий 1: Баллы не начислились

**Шаги:**
1. 🔐 Авторизуйся в приложении
2. 🍽️ Создай заказ и оплати его
3. ⚠️ Если баллы не начислились:
   - Кликни 🐞 (правый нижний угол)
   - Нажми "Send Debug Report"

**Что я увижу в отчёте:**
```bash
cat $(ls -t debug_reports/*_logs.txt | head -1)
```

Ищи строки:
- `💰 Начисляем баллы` - показывает расчёт
- `✅ Баллы начислены` - подтверждение
- `[ERROR]` - любые ошибки

**Пример хорошего лога:**
```
[LOG] 💰 Calculating points: total=1500, fee=200
[LOG] 💳 Points awarded: 170 (1700 * 0.1)
[LOG] ✅ User updated: loyaltyPoints=170, totalSpent=1700
```

**Пример плохого лога (баг):**
```
[ERROR] Failed to award points: totalSpent is NaN
[ERROR] User update failed: validation error
```

### Сценарий 2: Уведомление не показалось

**Шаги:**
1. Оплати заказ
2. ⚠️ Если уведомление не появилось:
   - Кликни 🐞
   - Нажми "Send Debug Report"

**Ищу в логах:**
```
[LOG] 🎉 Показываем SuccessDialog с баллами: 150
```

Или ошибку:
```
[ERROR] loyaltyPointsEarned is undefined, skipping dialog
```

### Сценарий 3: Неправильная сумма баллов

**Шаги:**
1. Запомни сумму заказа (например, 1500₽)
2. Оплати
3. Если баллы неправильные:
   - Кликни 🐞
   - Нажми "Send Debug Report"

**Ищу в логах:**
```bash
cat $(ls -t debug_reports/*_logs.txt | head -1) | grep -E "total|points|Calculating"
```

Увижу расчёт:
```
[LOG] Order total: 1500
[LOG] Delivery fee: 200
[LOG] Calculating points: (1500 + 200) * 0.1 = 170
```

Если баллы показали 150 вместо 170 → значит не учли доставку!

## 🤝 Работа со мной (AI)

### Вариант 1: Покажи мне файл

```bash
# Покажи последний отчёт
cat $(ls -t debug_reports/*_logs.txt | head -1)
```

Скопируй вывод и напиши мне:
```
"Вот отчёт о проблеме с баллами:

[ВСТАВЬ ВЕСЬ ТЕКСТ]

Помоги разобраться."
```

### Вариант 2: Дай мне имя файла

Напиши:
```
"Создался отчёт: 2026-01-11T09-15-24-028Z_user-guest_logs.txt
Прочитай и объясни, почему баллы не начислились."
```

Я прочитаю файл сам.

### Вариант 3: Опиши проблему

```
"Я оплатил заказ на 1500₽, должно было быть 150 баллов, но показало 0.
Отчёт создан. Что делать?"
```

Я найду последний отчёт и проанализирую.

## 🔍 Полезные команды

```bash
# Показать все отчёты
ls -la debug_reports/

# Последний отчёт (полностью)
cat $(ls -t debug_reports/*_logs.txt | head -1)

# Только ошибки из последнего отчёта
cat $(ls -t debug_reports/*_logs.txt | head -1) | grep ERROR

# Строки про баллы
cat $(ls -t debug_reports/*_logs.txt | head -1) | grep -i "points\|loyalty\|балл"

# Строки про payload (что отправили на API)
cat $(ls -t debug_reports/*_logs.txt | head -1) | grep PAYLOAD

# Удалить все старые отчёты
rm -rf debug_reports/*
```

## 💡 Что показывает отчёт

### 1. Метаданные
```
User ID: 123 (или guest)
User Email: user@example.com
URL: где произошла ошибка
Timestamp: точное время
```

### 2. Логи
Хронология событий:
```
[09:15:20] Пользователь создал заказ
[09:15:21] Отправлен запрос на сервер
[09:15:22] Получен ответ: success
[09:15:23] Начисляем баллы...
[09:15:24] ERROR: что-то пошло не так
```

### 3. Payload (самое важное!)
Точный JSON, отправленный на сервер:
```json
{
  "order": {
    "total": 1500,
    "deliveryFee": 200,
    "paid": true,
    "paymentMethod": "card"  ← проверю, что здесь
  },
  "userId": "123"
}
```

## 🎯 Конкретный пример

### Проблема: "Баллы показали 0"

**Ты делаешь:**
```bash
1. Воспроизвёл → кликнул 🐞 → Send Report
2. cat $(ls -t debug_reports/*_logs.txt | head -1)
3. Скопировал результат
```

**Пишешь мне:**
```
"Оплатил заказ на 1500₽, но баллы = 0. Вот лог:

[вставил текст]

Что не так?"
```

**Я отвечаю:**
```
Вижу проблему! В строке 15:

[ERROR] totalSpent is NaN

Это значит, что totalSpent не обновился после оплаты.
Проблема в файле app/api/orders/[id]/route.ts, строка 456.

Нужно исправить:
[показываю код]
```

**Результат:** За 2 минуты нашли и исправили баг! 🎉

## ⚡ Быстрая шпаргалка

| Что случилось | Что делать | Что искать в логе |
|---------------|------------|-------------------|
| Баллы = 0 | 🐞 → Send Report | `ERROR.*points`, `totalSpent` |
| Неправильная сумма | 🐞 → Send Report | `Calculating points`, `total=` |
| Нет уведомления | 🐞 → Send Report | `SuccessDialog`, `loyaltyPointsEarned` |
| Не знаю, что не так | 🐞 → Send Report | Всё, я сам разберусь |

## ✅ Система готова!

- ✅ API работает
- ✅ Отчёты создаются
- ✅ Логи сохраняются
- ✅ Скриншоты опциональны (не критично)
- ✅ Можно использовать прямо сейчас!

## 🚀 Следующий шаг

1. Воспроизведи проблему с баллами
2. Кликни 🐞 → Send Report
3. Покажи мне отчёт
4. Я найду проблему за минуты!

---

**P.S.** Даже если скриншот не создался - ничего страшного! Логи - это 90% информации. Скриншот - просто бонус для визуального контекста.

**Вопросы?** Просто напиши "помоги разобраться с отчётом" и покажи файл! 🎯


